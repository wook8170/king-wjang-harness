# ④ E2E 페르소나 시뮬레이션 🔴

정적 감사로는 안 되는 축. **빌드된 `bin/harness` 를 실제 표면(CLI 인자·훅 stdin JSON·
MCP JSON-RPC)에서 구동**했다. 스크립트는 재현 가능하게 남겼다.

전체 로그: `evidence/e2e.log` — **PASS=55 / FAIL=3**.

## 페르소나별 판정

| 페르소나 | 시나리오 | 판정 |
|---|---|---|
| A. 하네스를 안 쓰는 개발자 | 미초기화 프로젝트에서 4 이벤트 훅 발화 | **성공** — 전부 exit 0·stdout 0바이트·`.harness/` 미생성(비간섭) |
| B. 설계자 | 노드 등록 → 게이트 제출 → 리뷰 패킷 → 승인 → 페이즈 전환 | **성공** — 미승인 상태의 `phase set P1` 이 사유와 함께 거부됨 |
| C. 구현자 | 웨이브 생성 → 활성 → 턴 로그 → 완료 | **성공** — 유령 참조 거부, 증적 없는 UX 웨이브 완료 거부, 중복 활성 거부 |
| D. 출하 담당 | 결함 등록 → verdict → 체크리스트 → RTM/허브 | **성공** — open BLOCKER 가 있으면 NO-GO, 근거 없는 결함 등록 거부 |

**마찰(실패 아님, 기록):** 결함 등록이 `--evidence` 없이는 거부된다. 옳은 동작이지만
`--help` 가 없어(UX-24) 처음 쓰는 사람은 에러를 보고서야 안다. 실제로 이 감사에서도 그랬다.

## FAIL 3건 — 전부 스크립트 결함으로 규명

1·2. 샌드박스에 `docs/spec.md` 를 안 만들고 게이트를 제출 → 연쇄 실패 8건. 픽스처 추가 후 해소.
3. `wave update --text ""` 로 「빈 로그 거부」를 시험했는데 CLI 는 위치인자를 받는다 →
   `--text` 가 내용으로 기록됐다. **제품은 빈 로그를 실제로 거부한다**(`core/src/cli.ts:674`).
   다만 이 어긋남 자체를 [API-30] 으로 올렸다.

## 이 축만 잡은 것

없다 — 이번에는 ①·②·⑪ 이 각각 먼저 잡았다. 다만 **B/D 페르소나를 끝까지 해 보지 않았으면
[OPS-20]**(승인 후 doctor 상시 실패)**을 못 봤다** — 단위 테스트 584건이 전부 초록인 채로
남아 있는 결함이다.
