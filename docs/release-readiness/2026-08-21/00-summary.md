# king-wjang-harness 출하 검증 — 종합 리포트

**작성 2026-08-21** · 대상 `e860460` (feature/core-engine-v0) · 방식 11축 감사(단일 세션 직접 수행) + 실측 E2E
**매체**: CLI 도구 + Claude Code 플러그인 — `media-adapters.md` 의 CLI·라이브러리 표 적용

---

# 판정 (라운드 3-R — 2026-08-25, 7차 반영 2026-08-26) · 대상 `main`

> ## 출하 불가 (판정 유지) · 축 2·3·4 는 4.8 로 올린다
>
> **★ 7차 감정확인(2026-08-26) — 4.8 을 매긴 뒤 BLOCKER 를 둘 더 찾았다.** 봉인·상시화했다.
> - [SEC-297] **허용목록이 실경로 판정보다 앞서서, 「허용된 이름」 하나가 소스 전체의 문이
>   됐다.** P0 에서 `ln -s .. docs/up` 이 통과하고 `Write docs/up/src/app.ts` 도 통과해
>   **소스 파일을 실제로 덮었다**(같은 자리 직접 겨눔은 deny). 통과를 «착지 지점까지 안전할
>   때만» 주게 고쳤다 — 차단 6/6 · 과차단 0/5 · RED.
> - [SEC-298] **[SEC-296] 봉인이 문 하나만 닫았다** — 형제 `evidence spec/packet --out` ·
>   `tokens swap --out` 에 같은 판정이 없어 `--out /tmp/…` 가 루트 밖에 파일을 떨궜다.
>   판정을 한 벌(`assertOutputAllowed`)로 두고 네 표면이 쓴다 — 차단 8/8 · 착지 0건 ·
>   과차단 0/5 · RED. 두 각도 다 `bypass-corpus.test.ts` 에 넣었다.
>
> **★ 8차 감정확인(2026-08-26) — MCP 쓰기도구 인자 추출 갭 HIGH 1 ([SEC-299]).** 대상 추출이
> top-level string 하나만 봐서, `{note:ok.md, dst:core}`(디코이-우선, 전 페이즈)·`{paths:[core]}`·
> `{target:{path:core}}`(배열·중첩, P7~P9) 로 감싸면 코어·프로파일(POLICY_FILES) 쓰기가 훅을
> 통과했다 — [SEC-69]·[SEC-49/50/51] 재개통. **끝단 파일 덮임은 미관측**(실 MCP 서버 미연결)이라
> BLOCKER 아닌 HIGH([OPS-74] §5). 재귀 추출 + 전 대상 판정으로 봉인 — 차단 디코이·배열·중첩 P0·P7
> 전건 deny · 과차단 0 · RED(`blocker-3n.test.ts` 「SEC-299」).
> **축 2 실효성 2.0 → 4.8 · 축 3 엔지니어링 3.9 → 4.8 · 축 4 가치 3.0 → 4.8.**
> 감정서: `round3r/appraisal11-{2,3,4}-*.md`. 판정 자체는 **바뀌지 않는다** —
> 축 1·5·6·7 은 3-N 기준이고 재감정 전이며, 출하 판정은 7축 전부가 정해진다.
>
> **왜 올렸나 — 조건이 실측으로 충족됐고, 그 측정이 상시가 됐다.**
> - 축 2: 우회 코퍼스 **304 표기 전건 deny** · 잠금 19 전건 deny · **과차단 0/64**.
>   `bypass-corpus.test.ts` 로 **매 실행마다 다시 잰다**([OPS-284]).
> - 축 3: green ×3 동일(1364, skip 0) · tsc 0 · **빌드 바이트 재현** ·
>   **중복 규칙 0** — 이것도 `duplicate-rules.test.ts` 로 상시 측정([ENG-294]).
> - 축 4: open BLOCKER·HIGH 0 · lint R1–R13 · **게이트 13개 전부 3-R 값으로 재측정**.
>
> **★ 6차 감정확인이 이 4.8 을 매긴 «뒤에» BLOCKER 를 하나 더 찾았다.** 하네스 자신의
> CLI(`tokens gen --out`)가 훅을 우회하는 쓰기 원시명령이었다([SEC-296]) — P0 에서
> `echo x > src/tokens.ts` 는 deny 인데 그 명령은 통과해 **소스를 실제로 덮었다**.
> 봉인하고 그 각도를 상시 코퍼스에 넣었다. 점수는 유지하되 **읽는 법을 못 박는다**:
> 「스윕이 0건이었다」가 아니라 **「조건이 지금 실측으로 충족이고 매 실행마다 다시 잰다」**.
>
> **이번 라운드가 그 전 「0」을 깼다는 사실도 함께 적는다.** 적대 배터리로 실제 우회
> **9건**(BLOCKER 5)과 품질 결함 3건을 찾았고 전부 끝단까지 실증했다 — `SEC-290` 은
> `.git/hooks/` 지연 실행으로 **저널에 위조 줄이 실제로 들어갔다**. 직전 라운드가
> 「open 0」으로 닫혔는데도 그랬다. 그래서 이번 4.8 의 근거는 「내가 못 찾았다」가 아니라
> **「기계가 매번 다시 잰다」** 여야 하고, 실제로 그렇게 바꿨다.
>
> **이 감정의 한계**: 구현자와 감정자가 같다(`OPS-74`). 코퍼스 «밖» 표기로 다섯 번 쳐서
> 마지막 세 번이 0건이었으나, **신규 컨텍스트의 독립 적대 감정자**가 다시 쳐 0건일 때
> 확정된다. 각 감정서의 「이 감정의 한계」 절에 같은 고지를 남겼다.

---

# 판정 (라운드 3-Q — 2026-08-24) · 대상 `main` HEAD

> ## 출하 불가 (판정 유지)
> 🔴 **축 2 는 일곱 라운드째 2.0** 이고, 이번 라운드가 그 이유를 가장 분명히 보여 준다.
>
> **[SEC-276] 열네 번째 — `../` 등반 심링크.** `ln -s ../ d/u` 는 링크를 `d/` 안에 만들고,
> 그 안에서 `../` 는 **루트**를 가리킨다. 훅은 그것을 cwd 기준으로 풀어 「루트 밖」으로 읽었고
> 런타임은 코어에 착지했다 — **기준점이 달라 깊이만큼 어긋났다.** 아카이브도 스크립트도
> 필요 없이 `ln -s` 한 줄이다. 봉인했다(4/4 · 과차단 0/5).
>
> **★ 그리고 직전 라운드의 내 처방 하나를 되돌렸다([SEC-275]).** 「밖에서 들어온 내용 위에
> 쓰지 않는다」는 검사가 **막아야 할 것은 못 막고**(`mkdir` 한 단어로 우회) **막지 말아야 할
> 것은 막았다**(`git clone <url> y && echo x > y/config.txt`). 「과차단 0/7」이라 측정했으나
> **표본이 그 형태를 안 담고 있었다** — 측정이 틀린 게 아니라 표본이 좁았다.
> 순이익이 음수인 방어는 지운다([QUAL-229] 와 같은 판단).
>
> **이 라운드가 말하는 것**: 훅은 **실행 전 스냅샷**을 판정한다. 아카이브 안의 심링크처럼
> **명령문에도 그 시점 파일시스템에도 없는** 것은 원리상 볼 수 없다. 표기를 더 세는 것으로는
> 닫히지 않고, 닫으려 만든 넓은 규칙은 과차단으로 돌아온다. **완전한 봉인은 파일시스템 층의
> 몫**이고 그 도입은 제품 성격을 바꾸는 결정이라 사용자 판단으로 남겨 두었다.

---

# (이전) 판정 (라운드 3-P 진행 중 — 2026-08-24) · 대상 `main` HEAD

> ## 출하 불가 (판정 유지)
> 🔴 3-P 축 2 가 **열세 번째 부류**를 찾았다 — 이번엔 표기가 아니라 **층**이 달랐다.
>
> **[SEC-268] 판정-실행 TOCTOU.** `ln -s .harness ./h && echo x > ./h/config.yaml` —
> **한 명령 안에서** 별칭을 먼저 만들고 나중에 쓰면, 판정 시점에 그 별칭이 없어
> `realpath` 도 inode 대조도 코어를 못 본다. **심링크 방어와 하드링크 방어를 동시에 넘는다.**
> 여섯 라운드의 봉인이 전부 「경로·실체」라는 **공간** 방어였고, 이 부류는 **시간** 축에 있다.
>
> 처방은 파일시스템에 묻는 대신 **명령문을 읽는 것**이다 — 무엇을 무엇으로 별칭 짓는지는
> 텍스트에 다 적혀 있다. 8/8 차단 · 과차단 0/4. **[SEC-269]** `cp -l` 비대칭도 함께 닫았다.
>
> **정직한 한계**: 두 개의 다른 tool call 로 나누면 이 절은 못 본다 — 그때는 별칭이 실재하므로
> [SEC-263] 의 realpath·inode 대조가 잡는다. 순수 훅 접근에는 TOCTOU 하한이 있고,
> 감정자가 제안한 근본 처방(파일시스템 층 강제)은 별도 판단이 필요한 변경이다.
>
> **3-P 최종**: 축 2 **2.0** · 축 3 **3.9**(3.8 ▲) · 축 4 **3.0**.
> 축 4 가 **독립으로 또 하나**를 찾았다 — **[SEC-270]** `perl -e` 인라인 코드가 조회로 분류돼
> 저널 삭제·정책 덮어쓰기가 열려 있었다(`awk -i inplace`·`yq -i` 에 이은 **세 번째 재발**).
> 축 3 이 찾은 **[ENG-271] 열 번째 사본은 직전 웨이브가 만든 것**이다 — [SEC-265] 가 pre-tool 의
> 「쓰기 도구」 판정을 넓히면서 거울 자리인 post-tool 을 안 고쳐 MCP 쓰기 턴이 stop 가드
> 회계를 우회했다. **방어를 넓힐 때 그 개념을 쓰는 모든 자리를 같이 옮기지 않으면,
> 넓힌 만큼 다른 곳에서 갈린다.**

---

# (이전) 판정 (라운드 3-O 진행 중 — 2026-08-23) · 대상 `main` HEAD

> ## 출하 불가 (판정 유지)
> 🔴 3-N 이 4.0 미만이던 **축 2·3·4 를 재감정**했다(3-O). 축 2 **2.0 → 2.5** · 축 4 3.0 유지 ·
> 축 3 **3.5 → 3.8**. 세 축이 결함을 또 찾았고, 그것이 이 라운드의 소득이다.
>
> - **[SEC-263] 열두 번째 표기 — 하드링크 앨리어싱.** 여섯 라운드의 봉인이 전부 **경로 문자열
>   층**에서 이뤄졌는데, 하드링크는 그 **아래**(파일 실체)에서 우회한다 — 심링크를 `realpath` 로
>   막은 바로 그 방어가 원리상 닿지 못한다. 생성 차단 + inode 대조로 봉인(4/4 · 과차단 0/4).
> - **[SEC-264] 아홉 번째 변종 — 묶음 단축플래그**(`cp -rt .harness`). 직전 라운드가 고쳤다고
>   선언한 바로 그 부류가 한 표기 옆에서 다시 열렸다. **열거 대신 파싱 규칙**으로 봉인(11/11).
> - **[SEC-265]** MCP 쓰기 표면이 훅 매처 밖 — 파일시스템 MCP 서버 하나로 훅 전체가 우회됐다.
>   매처와 코어 판정이 **정본 상수 하나**를 공유하게 했다(7/7).
>
> **이 라운드가 말하는 것**: 열거로 세운 방어는 매번 다음 표기를 남긴다. 그래서 이번 처방은
> 전부 **열거를 규칙으로 바꾸는 쪽**이었다 — 파싱 규칙(SEC-264)·파일 실체(SEC-263)·정본 공유
> (SEC-265). 그럼에도 판정을 올리지 않는 이유는 같다: **판정은 결함 집계가 아니라 재감정이 정한다.**

---

# (이전) 판정 (라운드 3-N — 2026-08-23) · 대상 `feature/core-engine-v0` HEAD

> ## 출하 불가 (판정 유지)
> 🔴 **판정을 올리는 것은 결함 집계가 아니라 재감정이다.** 3-N 에서 여섯 축을 돌렸고
> **가성비 축(5)이 아직 안 돌았다** — 7축이 차기 전에는 판정을 움직이지 않는다.
>
> **3-N 점수 (7축 완주)**: 1 효용성 4.7 · 2 실효성 **2.0** · 3 엔지니어링 3.5 · 4 가치 3.0 ·
> **5 가성비 4.3** · 6 사용성 4.5 · 7 상품성 4.6.
>
> ### 이 라운드가 드러낸 것은 코드가 아니라 **절차**였다
>
> 3-N 가치 축이 「open BLOCKER 0 이 **거짓**」이라고 판정했고, 근거는 새 코드 결함이 아니라
> **감정 보고서 세 장이 대장에 인용 0건**이었다는 사실이다(3-L 사용성 · 3-M 가치 ·
> 3-M 사용성). 그중 3-M 가치 축이 낸 **BLOCKER 가 등재도 기각도 유예도 아닌 채 떨어졌고**,
> 그 상태로 대장·요약·README·커밋 메시지가 전부 「open BLOCKER 0」을 말했다.
>
> **이것이 라운드 3-H 에서 판정 철회를 부른 그 동작의 재발이다** — 그때는 *우리 목록*이
> ground truth 가 아니었고, 이번에는 *우리 목록에 넣지 않은 것*이 있었다. 처방도 같은 급으로
> 냈다: 빠진 결함 22건을 전건 등재하고, **보고서 한 장이 통째로 사라지면 테스트가 깨지게**
> 했다([QUAL-234]). 결함 하나하나를 기계가 세지는 않는다 — 그건 사람의 판단이고, 기계가
> 확실히 셀 수 있는 것은 「읽고 처리한 흔적이 있는가」다.
>
> ### 봉인한 것 (BLOCKER 2 · HIGH 1 · MED 2 + LOW 다수)
>
> - **[SEC-232] BLOCKER — 열한 번째 부류**: `cp -t .harness /tmp/config.yaml`. GNU 의 `-t` 는
>   목적지를 **앞**에 둔다. 위치가 아니라 **플래그**가 목적지를 정하게 했다(13/13, 과차단 0).
> - **[SEC-233] BLOCKER — 64KB 페이로드가 훅을 통째로 fail-open**. `process.stdin.isTTY` 접근이
>   fd 0 을 비블로킹으로 만들어 EAGAIN 이 났고, 그것이 「빈 입력 = 통과」로 흡수됐다.
>   **이 수정이 회귀를 만들었고 그것도 같은 라운드에 잡았다** — 블로킹으로 되돌리자 닫히지 않는
>   파이프에서 훅이 **영원히 멈췄다**(테스트 4시간 정지). 실제 훅이면 타임아웃 뒤 판정 없이
>   통과한다. 부작용을 **없애는 대신 감당하는** 쪽으로 다시 고쳤고, 회귀 테스트를 「판정」이
>   아니라 **「끝나는가」**로 넣었다 — 판정만 재면 멈추는 훅도 초록이다.
> - **[ENG-235] HIGH**: 일곱 번째 셸 목록 사본. `#!/bin/sh` 본문을 `x.fish` 로 이름만 바꾸면
>   [SEC-219] 의 본문 검사가 꺼졌다. **[ENG-237]** 로 여덟 번째 사본까지 정본 파생으로 정리.
> - **[ENG-236] MED**: `--dry-run` 예외가 두 벌 — 프로파일 경로만 명령 전체 단위라
>   `prisma migrate deploy --dry-run && prisma migrate deploy` 가 통과했다.
> - **사용성·상품성 LOW 18건 전건 처리** — 교차 그룹 플래그 무음 삼킴, 해제 시도 오진,
>   미지 훅 이벤트 침묵(구현자 자신이 빠진 함정), 배포본 `package-lock` 버전, 광고 문구 4건.
>
> ### 남은 것
>
> **7축 완주.** 가성비 축은 **4.3**(3-M 4.0 → 상승)이고, 그 축이 낸 결함 3건은 전건 봉인했다 —
> [COST-260](8KB 명령 하나로 훅 15초 정지 · 타임아웃 fail-open · **62배 개선**) ·
> [COST-261](stdin 상한이 타임아웃보다 커서 그 사이가 fail-open) ·
> [COST-262](벤치가 명령 파싱 경로를 안 봐서 2차 회귀가 CI 에 안 걸리던 것 — **게이트 신설**).
> 선제 조사로 [SEC-259](위치 가정 잔여 3도구)도 함께 닫았다. **open 0**.
> 대장: **verified 326 · open 0 · deferred 5 · open BLOCKER 0 · HIGH 0**.

---

# (이전) 판정 (라운드 3-I — 2026-08-22) · 대상 `feature/core-engine-v0` HEAD

> ## 출하 불가
> 🔴 **라운드 3 중반의 「출하 가능」 판정 철회는 그대로 유지된다.**
>
> **라운드 3-I 는 「막고 있는 것이 결함이 아니다」던 라운드 3-H 의 문장을 취소시켰다.**
> 대장 집계: **verified** 326 · **open** 0 (MED 0 · LOW 0) · deferred 5 ·
> open BLOCKER 0 · open HIGH 0 · 1041 tests green · tsc 0.
>
> 7축 독립 재감정(HEAD `45bde0c` 한 커밋에 고정)에서 **여섯 축이 오르거나 유지됐는데
> 가치 축만 4.8 → 3.5 로 무너졌다.** 이유는 새 결함이 아니라 **측정 범위**였다 —
> 라운드 3-H 가 낸 「open BLOCKER 0」은 ground truth 가 아니라 *우리 목록*의 산물이었고,
> 감정자가 목록 밖에서 **BLOCKER 3건**을 찾아 왔다. 전건 메인 세션에서 재현한 뒤 봉인했다
> (`SEC-135`·`SEC-136`·`SEC-137`, 커밋 `d065cf1`). **이것이 세 라운드 연속 같은 패턴이다.**
>
> | 축 | 3-I | 3-F/G | |
> |---|---|---|---|
> | 효용성 | 4.6 | 4.5 | ▲ |
> | 실효성 | 4.6 | 4.4 | ▲ |
> | 엔지니어링 | 4.6 | 4.5 | ▲ |
> | 가치(출하) | **3.5** | 4.8 | **▼1.3** |
> | 가성비 | 4.5 | 4.5 | ＝ |
> | 사용성 | 4.4 | 4.3 | ▲ |
> | 상품성 | 4.7 | 4.5 | ▲ |
>
> **봉인한 BLOCKER 3건 — 뿌리가 하나다:** 열거는 언제나 빠진 이름을 남기고,
> 「못 봤으면 통과」는 못 본 만큼 구멍이다.
> `SEC-135` 열거 밖 쓰기 도구가 위치 인자로 파일을 씀(`xxd`·`openssl`·`csplit`·`split`) →
> `SEC-136` 그것으로 `config.yaml` 을 덮어 **강제 자체가 풀림**([SEC-69] 재발) →
> `SEC-137` 64KB 넘는 스크립트는 본문을 **아예 안 읽고 통과**(비용 캡이 방어를 되돌림).
> 측정: 막아야 할 것 **11/11 DENY** · 설계 트랙 정상 20건 **오차단 0** · 구축 트랙 8건 0 ·
> 무장해제 사슬이 2단계에서 끊김.
>
> **그리고 이번엔 결함도 막고 있다.** 라운드 3-H 가 닫은 처방 다섯 곳에 **잔여**가 잡혀
> MED 12건이 새로 열렸다 — 1×1 PNG 가 UX 게이트를 통과하고(`QUAL-140`),
> `gate approve` 백스톱이 난독화 한 겹으로 열리고(`SEC-138`),
> **G9 는 표면 선택만으로 통과·불통과가 갈렸다**(`PERF-139`) — 같은 저널에서 인프로세스 p95 18.9ms,
> 프로세스 wall-time p95 162ms. 광고치 62/102ms 는 **둘 어느 쪽과도 안 맞았다.**
> 문턱을 **제품이 통제하는 몫**(폴백이 더하는 비용 < 50ms)으로 옮겼다 — 절대 wall-time 은 99ms 의
> Node 기동에 지배돼 제품이 아니라 측정 머신을 재기 때문이다. **기준을 바꾼 것이므로 감정자
> 재검증 전까지 이 근거로 판정을 올리지 않는다**([OPS-74]).
> 마지막 것은 `PERF-26` 의 close 근거까지 다시 열었다.
>
> 판정을 올리는 것은 여전히 결함 집계가 아니라 **재감정**이고, 지금은 그 앞에 **닫을 것이 있다.**
>
> 그 뒤 라운드 3-B/C/D 에서 BLOCKER 2건과 open HIGH 6건을 전부 닫았다.
> **그래도 판정은 바뀌지 않는다** — 판정을 올리는 것은 결함 집계가 아니라 **재감정**이고,
> 재감정 7항목(효용성 4.5 · 실효성 2.8 · 가치 2.7 · 가성비 4.3 · 사용성 4.2 · 상품성 3.5 ·
> 엔지니어링 4.2)은 **전부 4.8 미달인 채 아직 재실행되지 않았다.**
> 결함이 닫혔다고 판정을 올리면 그것이 바로 라운드 3에서 철회를 부른 그 동작이다.
>
> ### 라운드 3-D 에서 닫은 것 (6건)
>
> | ID | 심각도 | 무엇이었나 | 어떻게 닫혔나 |
> |---|---|---|---|
> | FEAT-73 | MED | SessionStart 첫 지시가 패키지에 없는 `/remote-control` 을 가리킴 | 번호 지시에서 빼고 맨 뒤 조건부 안내로. on↔off 출력 차이 정확히 1줄(과차단 0) |
> | OPS-76 | HIGH | 정책 변경이 탐지되지 않음 | 정책 해시 저널 고정 + doctor 대조. **워크플로 산출의 잠금은 개행 한 번에 풀렸고** `node …/cli.js` 로 탐지 장치가 꺼졌다 → 탐지 정규식 교체 + CLI 2차 게이트. 우회 12/12 deny · 과차단 0/8 |
> | SEC-75 | HIGH | 2바이트 파일 1장으로 13게이트 통과 | 제출 시점 실질성·구별성·페이즈 적합성 검사. 검증자가 반증한 **과차단 2건**(부분 등록 상태)을 함께 고쳤다. 잔존 부류는 [SEC-79] 로 분리 |
> | UX-71 | HIGH | 과차단 광범위 + 사유 문구가 사실과 다름 | deny-list 2단으로 교체. **워크플로 산출은 새 구멍 3건**(심링크로 실제 소스 덮어쓰기 · `test/` 6글자 우회 · 안내가 거짓이 됨)을 만들어 그대로 머지하지 않고 전부 닫은 뒤 들였다. 구현 차단 22/22 · 과차단 0/44 |
> | I18N-72 | HIGH | 배포되는 지시 계층(skills 11 · agents 5 · 매니페스트) 한글 15,569자 | 16개 파일 전량 영문화(한글 0, 저자 이름 제외) + **디렉토리 스캔 회귀 가드**(오염 주입으로 무는 것 확인) |
> | OPS-74 | HIGH | 자기 채점의 구조적 한계 | 절차로 못 박음 — 차단 측정은 과차단 목록과 짝, 자기 목록이 빠뜨리는 3축 강제, 구현자의 측정 목록은 근거가 아님. **이번 라운드 4건에 적용해 3건에서 결함을 잡았다** |
>
> ### 라운드 3-F — 7축 재감정을 실제로 돌렸다 · **전 축 4.8 미달**
>
> | 축 | 이번 | 직전 | 4.8 |
> |---|---|---|---|
> | 효용성 | 4.3 | 4.7 | ✗ |
> | 실효성 | 3.8 | 3.0 | ✗ |
> | 가치(출하) | 4.4 | 4.3 | ✗ |
> | 가성비 | 4.6 | 4.6 | ✗ |
> | 사용성 | 4.3 | 4.5 | ✗ |
> | 상품성 | 4.4 | 4.5 | ✗ |
> | 엔지니어링 | 4.4 | 4.5 | ✗ |
>
> **여러 축이 내려갔다.** 새 감정자가 자기 목록을 쓰면 지난 목록 밖이 보인다 — 지시문의
> 「우리 목록 쓰지 말고 네 목록을 직접 만들라」가 또 값을 했다. 나온 것을 **전건 재현한 뒤**
> BLOCKER 1 · HIGH 3 · MED 12 를 닫았다(아래 대장). **점수는 그 수정 이전 시점이다.**
>
> ### 라운드 3-H — **BLOCKER 2 · HIGH 1 · MED 16 전건 봉인**
>
> 라운드 3-G 가 찾은 19건을 전부 닫았다. 남은 것은 **LOW 30건뿐**이다.
> BLOCKER 둘은 뿌리가 같았다 — **무엇을 쓰는지 알 수 없는 실행**과 **이름으로 열거한 금지**.
> 포장을 하나씩 잡는 방식이 세 번 실패했으므로([SEC-49]→[SEC-A]→[SEC-100]) 이번엔 부류를 잡았다:
> 해석기가 프로그램을 파이프·통째 치환·프로세스치환·stdin 에서 받으면 페이즈 무관 deny 이고,
> 「지운다」를 이름으로 세는 대신 **보호 대상이 사라지는 것**을 본다.
>
> **닫는 과정에서 드러난 것도 함께 닫았다** — 이 리포가 반복해서 무는 두 부류였다:
> ① 픽스처가 규칙을 거짓말한다(세 번째) — `bashwrite.test.ts` 가 「mv 는 목적지만」으로 구멍을
>   고정하고 있었고, 3곳이 `'fake'` 를 `.png` 로 써서 UX 게이트의 가짜 증거 통과가 초록이었다.
> ② 같은 규칙 두 벌 — `bumpNode` 의 STALE 전파와 노드 병합이 CLI·MCP 에 각각 있었다
>   (`reviseNode`·`mergeNode` 한 벌로 합침).
>
> | 봉인 | 무엇이었나 | 어떻게 닫혔나 |
> |---|---|---|
> | [SEC-100] BLOCKER | `base64 -d ⏐ sh` 로 저널 위조 → 배포 게이트 개통 | 볼 수 없는 실행을 부류로 deny. 리터럴 프로그램·스크립트 파일은 통과 |
> | [SEC-101] BLOCKER | `mv .harness /tmp/x` 한 줄로 전체 무음 해제 | `mv` 원본·`find -delete`·`rmdir` 를 쓰기 대상으로 |
> | [UX-102] HIGH | 가장 막힌 순간의 안내가 유령 명령 | 문구 수정 + **부류를 막는 테스트 둘**(전수 대조 · 안내를 실제로 실행) |
> | MED 16 | 승인 잠금 0겹 · 가짜 PNG · STALE 되살리기 · 언급≠실행 · 규칙 두 벌 · 비간섭 비용 · 거짓 판정 · 배포 위생 · 메시지 3건 … | 대장 각 행 참조 |
>
> **측정(전건 measured)**: 막아야 할 것 16형태 **16/16 DENY**(전 페이즈) ·
> 막으면 안 될 것 **25건 중 오차단 0**(설계 트랙의 「구현 코드 실행 금지」 2건은 수정 이전
> 기준선에서도 동일한 기존 규칙) · 구축 트랙 **9건 중 0** · MCP 스키마 5,835B → **13B** ·
> 배포본 207 → **151 파일** · 뮤테이션 실증(자기호출 가드 제거 시 CAUGHT).
>
> **판정은 그대로 출하 불가다.** 판정을 올리는 것은 결함 집계가 아니라 **재감정**이고,
> 이번 수정 이후로는 아직 돌지 않았다 — 라운드 3-G 가 증명한 것이 정확히 그것이다
> (봉인이 점수를 올린다 · 새 감정자는 늘 목록 밖을 본다, **둘 다 참**).
>
> ### 남은 open 0건 — 대장의 모든 결함이 닫혔다
>
> **MED (0)** 
>
> **LOW (0)** 
> 전부 닫았다. 그중 둘은 **고치지 않고 `rejected`** 다 — 결함이 아니라 이미 내린
> 반대 결정이었기 때문이다([EFF-132] 브랜치 push 차단은 과차단 · [QUAL-E] 소스 트리 안
> 테스트 차단은 접미사 우회를 막는 의도된 절충). **`rejected` 는 회피가 아니라 판정이다.**
>
> deferred 5건 — dev 체인 취약점 3건(프로덕션 도달 경로 없음)에 라운드 3-J 의 둘이 더해졌다:
> [COST-178] 100퍼센트 상태전이 저널(도달 불가 — `gates.md` G9 절에 사유) · [QUAL-179] 외부 lint 사각(리포 밖 스킬).
>
> ### 그래도 판정은 **출하 불가**다
>
> 결함 집계가 0 이 된 것과 출하 가능은 다른 말이다. 판정을 올리는 것은 **재감정**이고,
> 이번 수정 이후로는 아직 돌지 않았다. 라운드 3-G 가 실측으로 보여 준 것이 정확히 그것이다 —
> **봉인은 점수를 올리고, 새 감정자는 늘 목록 밖을 본다. 둘 다 참이다.**
>
> 더구나 이번 라운드는 **게이트 하나(G9)의 기준 자체를 바꿨다.** [OPS-74](구현자의 측정
> 목록은 근거가 아니다)에 따라, 그 재정의를 감정자가 재검증하기 전까지 이 근거로 판정을
> 올리지 않는다. **라운드 3-J 감정 지시문에는 G9 재정의 자체를 검증 대상으로 넣어야 한다.**
>
>> 새로 찾아 닫은 것: [SEC-78](자기호출 정규식이 개행·접두 명령에 뚫림) ·
> [API-80](도움말이 광고하는 인자 형태가 먹지 않음).
>
> ### 독립 재감정이 찾아 닫은 것 (라운드 3-D 후반)
>
> 7축 독립 감정 중 3축(실효성·가치·사용성)이 돌아왔고, 그 발견을 **전건 메인에서 재현한 뒤** 닫았다.
> [USE-81](README 4개 언어가 있는 기능을 「없다」고 적음) · [USE-82](`backtrack` 이 사유 없이 exit 0,
> 영문 출력에 한국어 누출) · [API-83](`ship deploy` 도움말이 필수 `--sha` 누락) ·
> [API-84](`defect add` 는 `--id`, `update` 는 위치인자) · [UX-85](미초기화 시 내부 경로가 박힌 raw ENOENT) ·
> [UX-86](오류문의 `undefined` 노출 · 오타 제안 부재) · [LOGIC-87](댕글링 `--parent`) ·
> [SPEC-88](기본 프로파일에서 빌드 차단 무효 — 문서화) · [OPS-89](판정 블록·대장 드리프트 **3회 재발** → 테스트로 고정).
>
> ### 독립 재감정이 찾은 BLOCKER 1건 (에이전트 레인)
>
> [SEC-90] **접두 명령 하나로 쓰기 스캐너가 통째로 꺼졌다** — `sudo tee src/app.ts` ·
> `nohup cp /tmp/x .harness/state.json` 이 전 페이즈에서 ALLOW 였다. 소스만이 아니라
> **코어·정책 파일까지** 열려 [SEC-49]/[SEC-50]/[SEC-69] 가 닫은 표적이 접두어 한 조각으로
> 다시 열린 셈이다. 리다이렉트 형태는 계속 막혀 있었기 때문에 **명령 계열에서만 조용히**
> 성립했고, 리다이렉트만 재는 목록으로는 보이지 않았다. 함께 [SEC-91](디렉토리 대상 ·
> `find -exec`)도 닫았다.
>
> **위협 모델을 분명히 한다**: 여기서 세는 것은 **에이전트의 도구 호출**이 훅을 통과하는
> 경로다. 사람이 자기 터미널에서 정책 파일을 고치거나 `HARNESS_ALLOW_FORCE=1` 을 켜는 것은
> [SEC-69] 가 명시한 **의도된 탈출구**이므로 결함으로 세지 않는다.
>
> [SEC-92] **에이전트가 쓴 스크립트를 실행하면 훅이 그 안을 못 봤다** — `.sh` 는 설계 트랙에서
> 정당하게 허용되므로 `run.sh` 를 쓰고 `sh run.sh` 로 실행하면 잠금이 통째로 우회됐다.
> **P0 → P7 이 실제로 열리는 것까지 확인했다.** 훅이 실행되는 스크립트를 읽어 같은 규칙으로
> 판정하도록 고쳤다(루트 안·64KB·깊이 1).
>
> ### 독립 재감정 2차 — 엔지니어링 축이 찾은 **표면 비대칭 3건**
>
> 「같은 규칙이 표면마다 갈린다」는 이 리포의 반복 사고다([SEC-28]·[SEC-50]·[SEC-78]·[OPS-76]).
> 또 나왔다: [LOGIC-93] 부모 검증이 CLI 에만 있어 **MCP 로는 댕글링 부모가 원장에 기록**되고,
> [API-92] 목표 필수도 CLI 에만 있어 **MCP 로는 빈 껍데기 웨이브**가 생기고, [LOGIC-94] 접두
> 명령 목록이 두 벌이라 **이미 갈려** `timeout … harness wave update` 가 자기호출로 인식되지
> 않아 **stop 가드가 정산 직후 다시 차단**했다. 처방은 매번 같다 — **규칙을 도메인에 두고
> 표면이 상속하게 한다.** 죽은 중복 한 벌([LOGIC-95])도 지웠다.
>
> ### 재감정 7축 완료 — 전 축 4.8 미달
>
> | 축 | 점수 | 직전 | 4.8 |
> |---|---|---|---|
> | 효용성 | 4.7 | 4.5 | ✗ |
> | 실효성 | 3.0* | 2.8 | ✗ |
> | 가치(출하 품질) | 4.3 | 2.7 | ✗ |
> | 가성비 | 4.6 | 4.3 | ✗ |
> | 사용성 | 4.5 | 4.2 | ✗ |
> | 상품성 | 4.5 | 3.5 | ✗ |
> | 엔지니어링 | 4.5 | 4.2 | ✗ |
>
> \* 실효성은 [SEC-90]·[SEC-91]·[SEC-92] 를 닫은 뒤 **재측정했다: 2.5 → 3.0.** 그 재측정이
> **[SEC-97](래퍼 세 갈래 — `sh -c` · `git` 복원 계열 · 스크립트 깊이 2)** 을 새로 찾아 닫았으므로,
> 3.0 역시 **그 수정 이전 점수**다. 「닫으면 다음이 보인다」가 세 번째 반복이다(Iron Rule 4).
>
> 감정이 찾은 것 중 이번에 닫은 것: [SEC-96](코어 직접 호출로 정책 탐지 정지) ·
> [OPS-94](`state.json` 삭제 시 복구가 막다른 길 — [UX-85] 가 만든 회귀) ·
> [PROD-A](README 4개 언어가 출하된 기능을 「🔜 예정」으로 광고) · [UTIL-A](문서대로 하면
> 프로파일 오버라이드가 안 먹음).
>
> **게이트·측정 현황(라운드 3-H)**: tsc 0 · **1031 tests green** · 대장 **157행** lint 통과
> (칸 수·어휘·인용 줄·**인용한 절의 존재**까지) · **verified 124 · open 30 · deferred 3 ·
> open BLOCKER 0 · open HIGH 0**.
>
> ### 라운드 3-E — 잔가지 봉인 (10건)
>
> 재감정이 남긴 잔가지를 **재현부터** 다시 했다: 6건 중 5건 재현, [PROD-C] 는 재현 실패(오보).
> 닫는 과정에서 **MCP 표면에 같은 부류 2건**이 새로 나왔다([API-98]·[OPS-99]) — 「닫으면
> 다음이 보인다」가 네 번째로 반복됐다. [PERF-96] 은 **만들지 않기로 확정**했다: `events.jsonl`
> 은 감사 기록이라 압축 명령은 곧 삭제 원시연산이고, 대가는 열화 상태에서만 나는 20ms 다.
>
> | ID | 무엇이었나 | 어떻게 닫혔나 |
> |---|---|---|
> | [UTIL-D] | 미지 CLI 플래그를 조용히 무시 — 오타가 잘못된 레코드를 만든다 | 합집합 판정 + 편집거리 제안. 문서 241 호출 **과차단 0** |
> | [API-98] | 같은 부류가 MCP 에 — 스키마 밖 키를 무시 | 도구가 선언한 `inputSchema` 로 판정 |
> | [OPS-99] | MCP 가 `.harness/` 있는데 「관리하지 않는다 → init」 거짓 안내 | 열화/미초기화 분리 + **복구 경로가 통하는지까지 측정** |
> | [UTIL-B] | 토큰 스키마가 어디에도 없어 첫 시도 필패 | 골격을 코드에 단일 원천으로 — 도움말 예시 복사로 **첫 시도 성공** |
> | [UTIL-E] | `doc url` 이 claude.ai 아닌 주소도 수용 | 도메인에서 호스트 검증 |
> | [PROD-D] | `gate feedback` 이중 불릿 | 입력쪽 불릿 정규화 |
> | [PROD-B] | dist-없음 폴백 메시지 한국어 2곳 | 영문화 |
> | [PERF-95] | 비간섭 프로젝트도 훅 기동비 ~107ms | 셔뱅 셸 래퍼 — **-92.4%**, deny 동일 유지 |
> | [PROD-C] | (재현 실패 — 결함 아님) | 액면 금지 절차가 값을 한 사례 |
> | [PERF-96] | 저널 압축 수단 부재 | **의도적 미구현** — 4개 언어 「알려진 한계」에 논거·실측과 함께 |
>
> ### [SEC-79] — 마지막 open 을 닫았다
>
> 급소는 검사가 약한 것이 아니었다 — **승인 직전 사람이 읽는 문서(리뷰 패킷)가 실제 제출물을
> 아예 싣지 않았다.** 패킷은 레지스트리에 등록된 문서만 보여 줬고 `gate submit --paths` 로 올라간
> 경로는 어디에도 없었다. 세 갈래로 닫았다:
>
> 1. **산문이 아닌 것 거부** — 고유 글자 수와 낱말 수가 **동시에** 바닥일 때만. 하나만 보면
>    숫자 위주 표(자릿수 10종)나 띄어쓰기 없는 일본어·중국어 문서가 걸린다.
> 2. **새 텍스트의 절대량** — 이미 심사된 게이트들이 보지 않은 조각이 최소치(80자)만큼 있어야 한다.
>    처음엔 유사도 비율로 막았는데 **희석 경로**가 남았다(얇은 파일을 하나씩 덧붙이자 5게이트 개통).
>    **임계가 있으면 그 아래로 지나가는 길도 있다** — 그래서 비율이 아니라 양으로 답했다.
> 3. **패킷이 제출물을 측정해 보여 준다** — 경로별 실질 문자·고유 글자·낱말, 최소치 근처 알림,
>    읽을 수 없는 제출물 blocker.
>
> 측정: 필러 13장 **13→0** · 한 글자 사본 13장 **13→1** · 희석 사다리 **13→2** ·
> 정상 13페이즈 전 생애주기 **13/13 개통·승인 + GO + doctor 0**(과차단 0).
> **남는 것**: 게이트마다 진짜로 새 80자를 쓰면 열린다 — 그 80자가 *좋은가*는 코어 밖이다(§4-3).
>
> 계수 가드 자체의 구멍도 함께 닫았다 — 대장 행 정규식이 **글자로 끝나는 ID**(`UTIL-B`·`PROD-A`)를
> 세지 않아 8행이 집계 밖이었다. 집계 밖의 행은 open 이어도 헤더가 0 이라고 말한다.

## 왜 「출하 가능」이라고 잘못 냈나 — 측정 범위의 문제

새 결함이 생긴 게 아니다. **처음부터 있었고 내 측정 목록에 없었다.**

라운드 3에서 「우회 10계열 60종 전건 deny · 의도적 허용 18종 전건 allow(과차단 0)」을 냈다.
그 60종은 **내가 막힐 거라 예상한 것들**이었고, 18종은 **내가 통과할 거라 예상한 것들**이었다.
목록에 없던 것: **정책 파일 자체를 쓰는 것** · **설계 트랙 밖의 페이즈** · **막지 말아야 할 것을
막는지**. 자기 테스트 목록으로 자기 작업을 채점하면 이 셋은 구조적으로 안 보인다.

라운드 2의 「산출물 한국어 0」 과다 주장과 **같은 뿌리**다 — 부분 측정을 전수 측정이라 부른 것.
두 번 반복됐으므로 이건 실수가 아니라 **절차의 결함**이고, [OPS-74] 로 대장에 올렸다.
독립 적대 감사를 판정 전 필수 절차로 넣어야 한다.

## 재감정 점수 (7항목 독립 채점, rubric 은 착수 전 고정)

| 항목 | 점수 | 4.8 도달 |
|---|---|---|
| 효용성 | 4.5 | ❌ |
| 실효성 | **2.8** | ❌ |
| 가치 | **2.7** | ❌ |
| 가성비 | 4.3 | ❌ |
| 사용성 | 4.2 | ❌ |
| 상품성 | 3.5 | ❌ |
| 엔지니어링 품질 | 4.2 | ❌ |

**전 항목 미달.** 실효성·가치가 낮은 이유는 하나로 모인다 — 강제가 실제로는 설계 트랙
경로 화이트리스트 하나이고, 그마저 한 줄로 꺼진다.

## 그래도 사실인 것 (철회하지 않는 측정)

tsc 0 · **671 tests green ×3** · 빌드 바이트 재현 · `npm audit --omit=dev` 0 ·
gitleaks 82커밋 0 · 훅 폴백 p95 82~101ms < 150ms · **설계 트랙 안**에서의 우회 60종 deny ·
영문 기본 CLI 산출물 한국어 0. 이것들은 그대로 유효하다 — 문제는 **덮은 범위**이지 측정의 진실성이 아니다.

## 판정 밖 — 출시 결정 (사용자 몫)

기술 판정은 위에서 끝났다. 아래는 **출하해도 되는가**가 아니라 **출하할 것인가**의 문제다.

- CI 없음 · GitHub 리모트 없음 · push 금지 유지 · main 병합 보류 (이전 세션 결정 유지).

## 판정 밖 — 출하 후 백로그 (새 한계, 라운드 3에서 드러남)

- **번들 `profiles/` 는 영문 단일이다.** 코드 문자열과 달리 `lang: ko` 로 한국어판이 나오지
  않는다 — 파일을 언어별로 고르는 해석기가 없다. 한국어 사용자에게는 이 부분만 기본값
  변경이 아니라 **교체**다.
- **`profiles/*/guidance/`·`rules/` 를 아무것도 로드하지 않는다.** 스펙 §9는 「페이즈 스킬
  주입용」이라 적었지만 코어·스킬·에이전트 어디에도 로드 경로가 없다. 번역은 했으나 배선은
  없다. **README·스킬 문서가 이 로딩을 광고하지 않는 것을 확인**했으므로 「광고 중인데
  미구현」이 아니라 한계 기록으로 남긴다(광고했다면 FEAT 신규 행이 맞다).

# 판정 (라운드 2 이후 갱신 — 2026-08-21)

> ## 조건부 출하 가능
> 착수 시점 판정은 **출하 불가(NO-GO)** 였다 — 차단 2건.
> 라운드 1에서 확정 결함 16건 + 라운드 중 드러난 3건을 닫아 **open BLOCKER 0 · open HIGH 0**.
> 라운드 2에서 생성 문서 i18n([USE-59])과 그 과정에 드러난 CLI 플래그 결함 2건([API-57]·[API-58])을
> 닫았다 — 대장 **39행, open 0 · fixed 1 · deferred 1**.
> 아래 「남은 조건」 1건만 충족하면 무조건 출하 가능이다. (상세: `fixes-round1.md` · `fixes-round2.md`)

**남은 조건 ①** — [PERF-26] 열화 경로 지연의 **절대 p95 < 150ms 재측정**. (라운드 2에서도 미해결)
기제(상태 무변이 이벤트 미파싱)는 단위 테스트로 확인했으나, 라운드 1·2 두 세션 모두 머신 load 가
5~18(10 코어)이라 통제 측정이 정상 경로보다 높게 나오는 등 창이 무효였다.
**누가·무엇을**: 조용한 창(load < 2)에서 `evidence/latency.log` 의 절차를 그대로 재실행 →
p95 < 150ms 확인 → 대장 PERF-26 을 `fixed` → `verified` 로 올린다.

## 착수 시점 판정 원문 — 출하 불가 (NO-GO)

**차단 결함 2건.** 둘 다 같은 뿌리에서 나온다 — **훅이 Bash 표면에서 파일 쓰기를 보지 않는다.**

- **[SEC-50]** 설계 트랙 소스 쓰기 금지가 `echo "x" > src/app.ts` 한 줄로 풀린다.
  `Write` 는 deny 하면서 Bash 리다이렉트·heredoc·`touch` 는 통과한다.
- **[SEC-49]** `events.jsonl` 에 이벤트 한 줄을 append 하고 `doctor --repair` 를 부르면
  **페이즈와 게이트가 위조된다** — 사람이 클릭한 적 없는 승인 도장이 찍힌다.

이 제품의 공개된 핵심 약속은 *"AI가 설계 승인 전에 코드를 못 쓰게 **물리적으로** 막습니다 —
지침이 아니라 훅으로"* 다. MCP 어댑터는 그 약속을 지키려고 코드 수준 불변식까지 동원했고
(`core/src/mcp.ts:294`, 실측으로 뚫리지 않음을 확인 — [SEC-33]), 경로 우회·심링크·프롬프트
인젝션 방어는 이 범주에서 드물게 진지하다. **그런데 검사되지 않는 Bash 두 줄이 그 전부를
우회한다.** 잠근 문이 훌륭한 것과 옆에 열린 창이 있는 것은 별개다.

**나머지는 대체로 단단하다.** 584 테스트 3회 동일 · tsc 0 · 빌드 바이트 재현 · 맨 클론 설치 ·
롤백 리허설 · 이력 비밀 0 · 프로덕션 취약점 0. 차단 2건은 **국소적이고 수정 경로가 분명하다**
(⑥ 「완화 방향」). 이건 「기반이 부실하다」가 아니라 **「한 표면을 안 막았다」**다.

**착수 시점에 적은 조건 3건은 전부 닫혔다** — `--help` 구현(G11 ✅) · doctor 정합성 회복(G12 ✅) ·
미구현 명령 2건 구현(①축 ✅). 근거는 `evidence/round1-verify.log`.

---

# 게이트 실측

목표 열은 **측정 전에 `gates.md` 로 확정**했다(Iron Rule 1). 임계값 완화 없음.

| 게이트 | 목표 | 최종 실측 | 판정 |
|---|---|---|---|
| G1 테스트 | 전건 pass·fail 0·skip 0·3회 동일 | **1372 passed ×3, 57 파일** (라운드 3-R 재측정 · 배포본 1355 + 리포 전용 17) | ✅ |
| G2 타입 | 오류 0 | `tsc --noEmit` exit 0 (라운드 3-R 재측정) | ✅ |
| G3 빌드·자체완결 | 맨 클론서 `--version`·`init`·`status` exit 0 | `git archive` 로 뽑은 맨 트리에서 전부 exit 0 (라운드 3-R 재측정) | ✅ |
| G4 훅 무해 | 4이벤트 × 4입력 exit 0 · 미초기화 0바이트 | **32/32 exit 0** · 미초기화 출력 **0/16** (라운드 3-R 재측정, 초기화·미초기화 두 트리) | ✅ |
| G5 훅 강제력 | 소스·코어파일·경로우회·배포Bash deny · stop block | **우회 코퍼스 304 표기 전건 deny** + 잠금 19 전건 deny + **허용 64 전건 통과**(`core/test/bypass-corpus.test.ts`, 매 실행마다 재측정) · stop 가드 **미정산 block → `wave update` 후 통과** 실측 (라운드 3-R) | ✅ |
| G6 🔴 MCP 승인 불가 | 승인 시도 후 상태 불변 | `callTool(harness_gate_approve)` **4형태 전부 거절**(`force:true` 포함) · phase·gates 불변 · 저널 `gate-approved` **0줄** (라운드 3-R 재측정) | ✅ |
| G7 결정성 | 3회 동일·`Math.random` 0·마이그레이션 양방향 | 1364 ×3 동일 · `Math.random` **실사용 0**(2건 모두 주석) · 왕복 성공 (라운드 3-R 재측정) | ✅ |
| G8 공급망 | 프로덕션 도달 crit/high 0 | `npm audit --omit=dev` **0 vulnerabilities** (라운드 3-R 재측정) | ✅ |
| G9 훅 지연 | 폴백이 **더하는** p95 비용 < 50ms — **저널 부류마다** | 10만 줄 저널 3부류 재측정(라운드 3-R): 현실 분포 **+24.5ms** · 50% 손상 **+16.8ms** · 100% 손상 **+11.0ms** — 전부 문턱 안 | ✅ |
| G10 이력 비밀 | 0 | gitleaks **226 커밋 no leaks** (라운드 3-R 재측정) | ✅ |
| G11 CLI 계약 | `--help` exit 0 · 침묵 성공 0 | `--help` exit 0 + **20 명령군** 나열 · 침묵 성공 0 (라운드 3-R 재측정) | ✅ |
| G12 관측성 | 조용한 실패 0 · doctor 가 상태 반영 | `doctor` **ok=true · issues 0 · warnings 0** (라운드 3-R 재측정) | ✅ |
| G13 패키징 | 선언 산출물 누락 0 | `plugin.json` · dist 2 · **스킬 11 · 에이전트 5** 전부 존재 (라운드 3-R 재측정) | ✅ |

---

# 축별 결과

| 축 | 결과 | 파일 |
|---|---|---|
| ① 기능 완성도 | 문서화된 53개 명령 중 **2개 미구현**(`trace`·`gate feedback`) | `01-features.md` |
| ② 계약(CLI·MCP·훅) | `--help` 부재 · 하위명령 안내 절반만 · `wave create` 침묵 성공 | `02-backend.md` |
| ③ 출력 표면 | 결함 0 — ANSI 0·비TTY JSON·EPIPE 내성 | `03-ui.md` |
| ④ E2E 페르소나 | 4 페르소나 완주, PASS 55 / FAIL 3(전부 스크립트 결함) | `04-usability.md` |
| ⑤ 성능 | 정상 경로는 저널 크기 무관 · 폴백만 게이트 초과 | `05-perf.md` |
| ⑥ 보안 | **BLOCKER 2** — Bash 표면 미검사. 그 밖(인젝션·경로·비밀)은 견고 | `06-security.md` |
| ⑦ 공급망 | 프로덕션 도달 0 · dev 5건은 도달 경로 추적 후 deferred | `07-supply-chain.md` |
| ⑧ 논리 불변식 | 12개 선언 불변식 중 11 통과 · `doctor --repair` 데이터 손실 1 | `08-logic.md` |
| ⑨ 결정성 | 584×3 동일 · 빌드 바이트 재현 · 시각 주입 규율 부분 적용 | `09-determinism.md` |
| ⑩ 배포·롤백 | 맨 클론·롤백 리허설 실제 성공 · `--force` 자기해제 경로 | `10-deploy.md` |
| ⑪ 관측성 | 침묵 catch 5/5 문서화 · **유일한 건강검진이 상시 빨강** | `11-ops.md` |

---

# 감정서 대조 — `docs/appraisal/2026-08-21-plugin-appraisal.html`

세션 중 사용자가 붙인 감정서를 **현재 코드로 재확인**했다. 이 문서는 커밋 `bbbb9b6`
(198 tests, 코어 1,550줄) 기준이라 **핵심 판정 몇 개가 낡았다.**

| 감정서 주장 | 현재 코드 실측 |
|---|---|
| 「페이즈 게이트 미구현 — `phase set` 이 자기해제 구멍」 | **낡음.** 게이트 구현됨 — `phase set P7` 이 「직전 게이트 미승인」으로 거부된다. 단 **`--force` 로 이름만 바뀌어 남아 있다** → [SHIP-52] |
| 「테스트 198건 · 코어 1,550줄」 | 낡음 — **584건 · 7,574줄** |
| 「슬래시 커맨드 없어 발견성 부족」 | 부분 낡음 — **스킬 11종** 존재. 그러나 **CLI `--help` 는 여전히 없다** → [UX-24] 로 유지 |
| 「한국어 전용 README」 | **해소됨** — README 4개 언어(EN/KO/JA/ZH). CLI·훅 출력도 라운드 1에서 영문 기본(`lang: ko` 로 전환), **생성 문서까지 라운드 2에서 완료** → [USE-59] |
| 「배포 캐시 67MB — devDeps 동봉」 | **해당 없음** — `git archive HEAD` 는 **1.9MB**, node_modules 0건 |
| 「PostToolUse matcher `*` 로 도구 호출마다 node 기동, p95 104ms」 | **유효** — 재측정 103ms. 도구 호출당 pre+post 2회 발화 |
| 「`.harness/` 가 git 대상이라 브랜치 전환 시 되감김, `events.jsonl` 병합 충돌 미해결」 | **유효·미검증** — `.gitignore` 에 `.harness/` 없음(커밋 대상 확인). 병합 충돌 시나리오는 이번에 **보지 않았다** |
| 「의식 비용·온보딩 골짜기」 | **유효** — ④ 축에서 같은 마찰을 관측 |

**감정서가 놓친 것**: Bash 표면 미검사([SEC-49][SEC-50]). 감정서는 「경로 우회를 진지하게
막았다」고 평가했는데, 그건 `Write` 표면 이야기다. 소스를 전량 읽고도 못 봤다 —
**실제로 Bash 로 써 보기 전까지는 안 보이는 결함**이다(Iron Rule 3).

---

# 이번에 보지 않은 축

## 가. 의도적 제외 (결정)
- **①의 경쟁 벤치마크** — 같은 범주 제품의 체험 수단이 없다. 시늉 감사는 미감사보다 나쁘므로 뺐다.
  자사 스펙·README 대비 완성도로 대체했다.
- **⑥ 의 `/security-review`·`/cso` 위임** — 축을 서브에이전트로 분리하지 않고 직접 수행했다
  (세션 지침상 Agent 도구 미사용). 도구가 없어서가 아니라 **방식 선택**이다.

## 나. 확인 불가 (볼 수단이 없었다)
- **실제 마켓플레이스 설치 경로** — GitHub 리모트가 없다. 사용자가 리모트를 만들면
  `claude plugin install` 로 1회 실측할 수 있다.
- **다른 장비에서의 설치** — 이 장비 1대에서만 확인했다.
- **`.harness/` 브랜치 병합 충돌** — 감정서가 지적한 시나리오. `events.jsonl` 을 양쪽 브랜치에서
  append 한 뒤 병합하는 실험이 필요하다. 이번 범위에 넣지 못했다.
- **동시 세션 경합** — 같은 `.harness/` 에 두 세션이 동시에 쓸 때의 저널 원자성. 코드상
  append-only 이나 **실측하지 않았다.**
- **장기 상주 관찰** — 수백 턴 누적 시 `hook-errors.log`·저널 증가 거동.

---

# 사용자·제품 결정 사항 (기술 판정 아님)

1. ~~**LICENSE 파일이 없다.**~~ → **결정·완료.** 사용자 결정은 **MIT** — `LICENSE` 추가됨(라운드 1).
2. ~~**CLI·훅 출력이 한국어 전용.**~~ → **결정·완료.** 사용자 결정은 **영문 기본 + `lang: ko`**
   (없애는 게 아니라 기본값을 바꾼다). 예외 메시지는 라운드 1, **생성 문서 5종은 라운드 2**에서
   닫혔다 — 영문 기본에서 산출물 한국어 **0** 실측, ko 회귀 유지([USE-59]).
3. **CI 가 없다.** 테스트·타입·audit 이 사람 손으로만 돈다.
4. **GitHub 리모트 없음 · main 병합 보류 · push 금지** — 이전 세션 결정 유지.
5. ~~**`--force` 를 남길 것인가.**~~ → **결정·완료.** 훅에서 deny + `HARNESS_ALLOW_FORCE=1` 로만
   허용(사람이 터미널에서 켜는 탈출구). [SHIP-52] verified.

**남은 결정**: 위 3·4 (CI 없음 · 리모트 없음/push 금지/main 병합 보류) 만 유효하다.

---

# 출하 후 백로그 (deferred)

- **[DEP-32]** dev 체인 취약점 5건(critical 1) — 전부 「리스닝 dev 서버」 전제라 도달 경로 없음.
  vitest 3.x 업그레이드는 파괴적 변경. `evidence/gates.log` · `07-supply-chain.md`.

---

# 이 감사에서 배운 것

1. **정적 감사·소스 전량 열람으로도 안 보이는 결함이 있다.** 감정서는 코어 11모듈을 전부 읽고
   「경로 우회를 진지하게 막았다」고 평가했다. 사실이다 — `Write` 표면에서는. **막힌 다음에
   무엇을 시도하게 되는지**를 실제로 해 봐야 옆문이 보인다.
2. **측정 도구가 결함을 지어낸다.** zsh 는 무인용 확장을 단어 분리하지 않는다 — 이 한 가지로
   「미구현 명령 48건」이라는 가짜 결함이 나왔다. 실측 결과가 너무 극적이면 도구부터 의심하라.
3. **상시 빨간 진단은 위조를 숨긴다.** [OPS-20](doctor 상시 오탐)이 없었다면 [SEC-49] 의 저널
   위조가 `doctor` 의 `ok:false` 로 드러났을 것이다. 오탐 하나가 진짜 경보를 무의미하게 만든다 —
   Iron Rule 6 이 테스트뿐 아니라 **운영 도구에도** 적용된다.
4. **같은 규율의 부분 적용이 결함의 뿌리다.** 시각 주입(`usage`·`migrate`·`tokens` 에는 있고
   `gate` 에는 없음 → [OPS-20]), 하위명령 안내(절반만 → [API-27]), 인젝션 방어(두 벌 → [SEC-28]),
   코어 파일 보호(Write 만 → [SEC-51]). **네 결함이 전부 같은 모양이다.**
