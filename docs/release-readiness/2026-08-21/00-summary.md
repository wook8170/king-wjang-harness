# king-wjang-harness 출하 검증 — 종합 리포트

**작성 2026-08-21** · 대상 `e860460` (feature/core-engine-v0) · 방식 11축 감사(단일 세션 직접 수행) + 실측 E2E
**매체**: CLI 도구 + Claude Code 플러그인 — `media-adapters.md` 의 CLI·라이브러리 표 적용

---

# 판정 (라운드 3-D — 2026-08-21) · 대상 `eba259e`

> ## 출하 불가
> 🔴 **라운드 3 중반의 「출하 가능」 판정 철회는 그대로 유지된다.**
>
> 그 뒤 라운드 3-B/C/D 에서 BLOCKER 2건과 open HIGH 6건을 전부 닫았다.
> **그래도 판정은 바뀌지 않는다** — 판정을 올리는 것은 결함 집계가 아니라 **재감정**이고,
> 재감정 7항목(효용성 4.5 · 실효성 2.8 · 가치 2.7 · 가성비 4.3 · 사용성 4.2 · 상품성 3.5 ·
> 엔지니어링 4.2)은 **전부 4.8 미달인 채 아직 재실행되지 않았다.**
> 결함이 닫혔다고 판정을 올리면 그것이 바로 라운드 3에서 철회를 부른 그 동작이다.
>
> ### 라운드 3-D 에서 닫은 것 (6건)
>
> | ID | 심각도 | 무엇이었나 | 어떻게 닫혔나 |
> |---|---|---|---|
> | FEAT-73 | MED | SessionStart 첫 지시가 패키지에 없는 `/remote-control` 을 가리킴 | 번호 지시에서 빼고 맨 뒤 조건부 안내로. on↔off 출력 차이 정확히 1줄(과차단 0) |
> | OPS-76 | HIGH | 정책 변경이 탐지되지 않음 | 정책 해시 저널 고정 + doctor 대조. **워크플로 산출의 잠금은 개행 한 번에 풀렸고** `node …/cli.js` 로 탐지 장치가 꺼졌다 → 탐지 정규식 교체 + CLI 2차 게이트. 우회 12/12 deny · 과차단 0/8 |
> | SEC-75 | HIGH | 2바이트 파일 1장으로 13게이트 통과 | 제출 시점 실질성·구별성·페이즈 적합성 검사. 검증자가 반증한 **과차단 2건**(부분 등록 상태)을 함께 고쳤다. 잔존 부류는 [SEC-79] 로 분리 |
> | UX-71 | HIGH | 과차단 광범위 + 사유 문구가 사실과 다름 | deny-list 2단으로 교체. **워크플로 산출은 새 구멍 3건**(심링크로 실제 소스 덮어쓰기 · `test/` 6글자 우회 · 안내가 거짓이 됨)을 만들어 그대로 머지하지 않고 전부 닫은 뒤 들였다. 구현 차단 22/22 · 과차단 0/44 |
> | I18N-72 | HIGH | 배포되는 지시 계층(skills 11 · agents 5 · 매니페스트) 한글 15,569자 | 16개 파일 전량 영문화(한글 0, 저자 이름 제외) + **디렉토리 스캔 회귀 가드**(오염 주입으로 무는 것 확인) |
> | OPS-74 | HIGH | 자기 채점의 구조적 한계 | 절차로 못 박음 — 차단 측정은 과차단 목록과 짝, 자기 목록이 빠뜨리는 3축 강제, 구현자의 측정 목록은 근거가 아님. **이번 라운드 4건에 적용해 3건에서 결함을 잡았다** |
>
> ### 남은 open 1건
>
> | ID | 심각도 | 내용 |
> |---|---|---|
> | SEC-79 | MED | 실 산출물 0장(필러 13장, 총 1KB)으로 13게이트 + `ship verdict` GO. 내용의 **질** 판정은 코어 밖이라 「닫았다」고 쓰지 않았다 — 남은 방어는 사람 승인(§4-3) |
>
> 새로 찾아 닫은 것: [SEC-78](자기호출 정규식이 개행·접두 명령에 뚫림) ·
> [API-80](도움말이 광고하는 인자 형태가 먹지 않음).
>
> ### 독립 재감정이 찾아 닫은 것 (라운드 3-D 후반)
>
> 7축 독립 감정 중 3축(실효성·가치·사용성)이 돌아왔고, 그 발견을 **전건 메인에서 재현한 뒤** 닫았다.
> [USE-81](README 4개 언어가 있는 기능을 「없다」고 적음) · [USE-82](`backtrack` 이 사유 없이 exit 0,
> 영문 출력에 한국어 누출) · [API-83](`ship deploy` 도움말이 필수 `--sha` 누락) ·
> [API-84](`defect add` 는 `--id`, `update` 는 위치인자) · [UX-85](미초기화 시 내부 경로가 박힌 raw ENOENT) ·
> [UX-86](오류문의 `undefined` 노출 · 오타 제안 부재) · [LOGIC-87](댕글링 `--parent`) ·
> [SPEC-88](기본 프로파일에서 빌드 차단 무효 — 문서화) · [OPS-89](판정 블록·대장 드리프트 **3회 재발** → 테스트로 고정).
>
> ### 독립 재감정이 찾은 BLOCKER 1건 (에이전트 레인)
>
> [SEC-90] **접두 명령 하나로 쓰기 스캐너가 통째로 꺼졌다** — `sudo tee src/app.ts` ·
> `nohup cp /tmp/x .harness/state.json` 이 전 페이즈에서 ALLOW 였다. 소스만이 아니라
> **코어·정책 파일까지** 열려 [SEC-49]/[SEC-50]/[SEC-69] 가 닫은 표적이 접두어 한 조각으로
> 다시 열린 셈이다. 리다이렉트 형태는 계속 막혀 있었기 때문에 **명령 계열에서만 조용히**
> 성립했고, 리다이렉트만 재는 목록으로는 보이지 않았다. 함께 [SEC-91](디렉토리 대상 ·
> `find -exec`)도 닫았다.
>
> **위협 모델을 분명히 한다**: 여기서 세는 것은 **에이전트의 도구 호출**이 훅을 통과하는
> 경로다. 사람이 자기 터미널에서 정책 파일을 고치거나 `HARNESS_ALLOW_FORCE=1` 을 켜는 것은
> [SEC-69] 가 명시한 **의도된 탈출구**이므로 결함으로 세지 않는다.
>
> [SEC-92] **에이전트가 쓴 스크립트를 실행하면 훅이 그 안을 못 봤다** — `.sh` 는 설계 트랙에서
> 정당하게 허용되므로 `run.sh` 를 쓰고 `sh run.sh` 로 실행하면 잠금이 통째로 우회됐다.
> **P0 → P7 이 실제로 열리는 것까지 확인했다.** 훅이 실행되는 스크립트를 읽어 같은 규칙으로
> 판정하도록 고쳤다(루트 안·64KB·깊이 1).
>
> ### 독립 재감정 2차 — 엔지니어링 축이 찾은 **표면 비대칭 3건**
>
> 「같은 규칙이 표면마다 갈린다」는 이 리포의 반복 사고다([SEC-28]·[SEC-50]·[SEC-78]·[OPS-76]).
> 또 나왔다: [LOGIC-93] 부모 검증이 CLI 에만 있어 **MCP 로는 댕글링 부모가 원장에 기록**되고,
> [API-92] 목표 필수도 CLI 에만 있어 **MCP 로는 빈 껍데기 웨이브**가 생기고, [LOGIC-94] 접두
> 명령 목록이 두 벌이라 **이미 갈려** `timeout … harness wave update` 가 자기호출로 인식되지
> 않아 **stop 가드가 정산 직후 다시 차단**했다. 처방은 매번 같다 — **규칙을 도메인에 두고
> 표면이 상속하게 한다.** 죽은 중복 한 벌([LOGIC-95])도 지웠다.
>
> ### 재감정 7축 완료 — 전 축 4.8 미달
>
> | 축 | 점수 | 직전 | 4.8 |
> |---|---|---|---|
> | 효용성 | 4.7 | 4.5 | ✗ |
> | 실효성 | 3.0* | 2.8 | ✗ |
> | 가치(출하 품질) | 4.3 | 2.7 | ✗ |
> | 가성비 | 4.6 | 4.3 | ✗ |
> | 사용성 | 4.5 | 4.2 | ✗ |
> | 상품성 | 4.5 | 3.5 | ✗ |
> | 엔지니어링 | 4.5 | 4.2 | ✗ |
>
> \* 실효성은 [SEC-90]·[SEC-91]·[SEC-92] 를 닫은 뒤 **재측정했다: 2.5 → 3.0.** 그 재측정이
> **[SEC-97](래퍼 세 갈래 — `sh -c` · `git` 복원 계열 · 스크립트 깊이 2)** 을 새로 찾아 닫았으므로,
> 3.0 역시 **그 수정 이전 점수**다. 「닫으면 다음이 보인다」가 세 번째 반복이다(Iron Rule 4).
>
> 감정이 찾은 것 중 이번에 닫은 것: [SEC-96](코어 직접 호출로 정책 탐지 정지) ·
> [OPS-94](`state.json` 삭제 시 복구가 막다른 길 — [UX-85] 가 만든 회귀) ·
> [PROD-A](README 4개 언어가 출하된 기능을 「🔜 예정」으로 광고) · [UTIL-A](문서대로 하면
> 프로파일 오버라이드가 안 먹음).
>
> **게이트·측정 현황**: tsc 0 · **893 tests green** · 대장 **91행** lint 통과
> (칸 수·어휘·인용 줄·**인용한 절의 존재**까지) · **verified 88 · open 1 · deferred 2 ·
> open BLOCKER 0 · open HIGH 0**.
>
> ### 라운드 3-E — 잔가지 봉인 (10건)
>
> 재감정이 남긴 잔가지를 **재현부터** 다시 했다: 6건 중 5건 재현, [PROD-C] 는 재현 실패(오보).
> 닫는 과정에서 **MCP 표면에 같은 부류 2건**이 새로 나왔다([API-98]·[OPS-99]) — 「닫으면
> 다음이 보인다」가 네 번째로 반복됐다. [PERF-96] 은 **만들지 않기로 확정**했다: `events.jsonl`
> 은 감사 기록이라 압축 명령은 곧 삭제 원시연산이고, 대가는 열화 상태에서만 나는 20ms 다.
>
> | ID | 무엇이었나 | 어떻게 닫혔나 |
> |---|---|---|
> | [UTIL-D] | 미지 CLI 플래그를 조용히 무시 — 오타가 잘못된 레코드를 만든다 | 합집합 판정 + 편집거리 제안. 문서 241 호출 **과차단 0** |
> | [API-98] | 같은 부류가 MCP 에 — 스키마 밖 키를 무시 | 도구가 선언한 `inputSchema` 로 판정 |
> | [OPS-99] | MCP 가 `.harness/` 있는데 「관리하지 않는다 → init」 거짓 안내 | 열화/미초기화 분리 + **복구 경로가 통하는지까지 측정** |
> | [UTIL-B] | 토큰 스키마가 어디에도 없어 첫 시도 필패 | 골격을 코드에 단일 원천으로 — 도움말 예시 복사로 **첫 시도 성공** |
> | [UTIL-E] | `doc url` 이 claude.ai 아닌 주소도 수용 | 도메인에서 호스트 검증 |
> | [PROD-D] | `gate feedback` 이중 불릿 | 입력쪽 불릿 정규화 |
> | [PROD-B] | dist-없음 폴백 메시지 한국어 2곳 | 영문화 |
> | [PERF-95] | 비간섭 프로젝트도 훅 기동비 ~107ms | 셔뱅 셸 래퍼 — **-92.4%**, deny 동일 유지 |
> | [PROD-C] | (재현 실패 — 결함 아님) | 액면 금지 절차가 값을 한 사례 |
> | [PERF-96] | 저널 압축 수단 부재 | **의도적 미구현** — 4개 언어 「알려진 한계」에 논거·실측과 함께 |
>
> 계수 가드 자체의 구멍도 함께 닫았다 — 대장 행 정규식이 **글자로 끝나는 ID**(`UTIL-B`·`PROD-A`)를
> 세지 않아 8행이 집계 밖이었다. 집계 밖의 행은 open 이어도 헤더가 0 이라고 말한다.

## 왜 「출하 가능」이라고 잘못 냈나 — 측정 범위의 문제

새 결함이 생긴 게 아니다. **처음부터 있었고 내 측정 목록에 없었다.**

라운드 3에서 「우회 10계열 60종 전건 deny · 의도적 허용 18종 전건 allow(과차단 0)」을 냈다.
그 60종은 **내가 막힐 거라 예상한 것들**이었고, 18종은 **내가 통과할 거라 예상한 것들**이었다.
목록에 없던 것: **정책 파일 자체를 쓰는 것** · **설계 트랙 밖의 페이즈** · **막지 말아야 할 것을
막는지**. 자기 테스트 목록으로 자기 작업을 채점하면 이 셋은 구조적으로 안 보인다.

라운드 2의 「산출물 한국어 0」 과다 주장과 **같은 뿌리**다 — 부분 측정을 전수 측정이라 부른 것.
두 번 반복됐으므로 이건 실수가 아니라 **절차의 결함**이고, [OPS-74] 로 대장에 올렸다.
독립 적대 감사를 판정 전 필수 절차로 넣어야 한다.

## 재감정 점수 (7항목 독립 채점, rubric 은 착수 전 고정)

| 항목 | 점수 | 4.8 도달 |
|---|---|---|
| 효용성 | 4.5 | ❌ |
| 실효성 | **2.8** | ❌ |
| 가치 | **2.7** | ❌ |
| 가성비 | 4.3 | ❌ |
| 사용성 | 4.2 | ❌ |
| 상품성 | 3.5 | ❌ |
| 엔지니어링 품질 | 4.2 | ❌ |

**전 항목 미달.** 실효성·가치가 낮은 이유는 하나로 모인다 — 강제가 실제로는 설계 트랙
경로 화이트리스트 하나이고, 그마저 한 줄로 꺼진다.

## 그래도 사실인 것 (철회하지 않는 측정)

tsc 0 · **671 tests green ×3** · 빌드 바이트 재현 · `npm audit --omit=dev` 0 ·
gitleaks 82커밋 0 · 훅 폴백 p95 82~101ms < 150ms · **설계 트랙 안**에서의 우회 60종 deny ·
영문 기본 CLI 산출물 한국어 0. 이것들은 그대로 유효하다 — 문제는 **덮은 범위**이지 측정의 진실성이 아니다.

## 판정 밖 — 출시 결정 (사용자 몫)

기술 판정은 위에서 끝났다. 아래는 **출하해도 되는가**가 아니라 **출하할 것인가**의 문제다.

- CI 없음 · GitHub 리모트 없음 · push 금지 유지 · main 병합 보류 (이전 세션 결정 유지).

## 판정 밖 — 출하 후 백로그 (새 한계, 라운드 3에서 드러남)

- **번들 `profiles/` 는 영문 단일이다.** 코드 문자열과 달리 `lang: ko` 로 한국어판이 나오지
  않는다 — 파일을 언어별로 고르는 해석기가 없다. 한국어 사용자에게는 이 부분만 기본값
  변경이 아니라 **교체**다.
- **`profiles/*/guidance/`·`rules/` 를 아무것도 로드하지 않는다.** 스펙 §9는 「페이즈 스킬
  주입용」이라 적었지만 코어·스킬·에이전트 어디에도 로드 경로가 없다. 번역은 했으나 배선은
  없다. **README·스킬 문서가 이 로딩을 광고하지 않는 것을 확인**했으므로 「광고 중인데
  미구현」이 아니라 한계 기록으로 남긴다(광고했다면 FEAT 신규 행이 맞다).

# 판정 (라운드 2 이후 갱신 — 2026-08-21)

> ## 조건부 출하 가능
> 착수 시점 판정은 **출하 불가(NO-GO)** 였다 — 차단 2건.
> 라운드 1에서 확정 결함 16건 + 라운드 중 드러난 3건을 닫아 **open BLOCKER 0 · open HIGH 0**.
> 라운드 2에서 생성 문서 i18n([USE-59])과 그 과정에 드러난 CLI 플래그 결함 2건([API-57]·[API-58])을
> 닫았다 — 대장 **39행, open 0 · fixed 1 · deferred 1**.
> 아래 「남은 조건」 1건만 충족하면 무조건 출하 가능이다. (상세: `fixes-round1.md` · `fixes-round2.md`)

**남은 조건 ①** — [PERF-26] 열화 경로 지연의 **절대 p95 < 150ms 재측정**. (라운드 2에서도 미해결)
기제(상태 무변이 이벤트 미파싱)는 단위 테스트로 확인했으나, 라운드 1·2 두 세션 모두 머신 load 가
5~18(10 코어)이라 통제 측정이 정상 경로보다 높게 나오는 등 창이 무효였다.
**누가·무엇을**: 조용한 창(load < 2)에서 `evidence/latency.log` 의 절차를 그대로 재실행 →
p95 < 150ms 확인 → 대장 PERF-26 을 `fixed` → `verified` 로 올린다.

## 착수 시점 판정 원문 — 출하 불가 (NO-GO)

**차단 결함 2건.** 둘 다 같은 뿌리에서 나온다 — **훅이 Bash 표면에서 파일 쓰기를 보지 않는다.**

- **[SEC-50]** 설계 트랙 소스 쓰기 금지가 `echo "x" > src/app.ts` 한 줄로 풀린다.
  `Write` 는 deny 하면서 Bash 리다이렉트·heredoc·`touch` 는 통과한다.
- **[SEC-49]** `events.jsonl` 에 이벤트 한 줄을 append 하고 `doctor --repair` 를 부르면
  **페이즈와 게이트가 위조된다** — 사람이 클릭한 적 없는 승인 도장이 찍힌다.

이 제품의 공개된 핵심 약속은 *"AI가 설계 승인 전에 코드를 못 쓰게 **물리적으로** 막습니다 —
지침이 아니라 훅으로"* 다. MCP 어댑터는 그 약속을 지키려고 코드 수준 불변식까지 동원했고
(`core/src/mcp.ts:294`, 실측으로 뚫리지 않음을 확인 — [SEC-33]), 경로 우회·심링크·프롬프트
인젝션 방어는 이 범주에서 드물게 진지하다. **그런데 검사되지 않는 Bash 두 줄이 그 전부를
우회한다.** 잠근 문이 훌륭한 것과 옆에 열린 창이 있는 것은 별개다.

**나머지는 대체로 단단하다.** 584 테스트 3회 동일 · tsc 0 · 빌드 바이트 재현 · 맨 클론 설치 ·
롤백 리허설 · 이력 비밀 0 · 프로덕션 취약점 0. 차단 2건은 **국소적이고 수정 경로가 분명하다**
(⑥ 「완화 방향」). 이건 「기반이 부실하다」가 아니라 **「한 표면을 안 막았다」**다.

**착수 시점에 적은 조건 3건은 전부 닫혔다** — `--help` 구현(G11 ✅) · doctor 정합성 회복(G12 ✅) ·
미구현 명령 2건 구현(①축 ✅). 근거는 `evidence/round1-verify.log`.

---

# 게이트 실측

목표 열은 **측정 전에 `gates.md` 로 확정**했다(Iron Rule 1). 임계값 완화 없음.

| 게이트 | 목표 | 최종 실측 | 판정 |
|---|---|---|---|
| G1 테스트 | 전건 pass·fail 0·skip 0·3회 동일 | **626 passed ×3, 27 파일** | ✅ |
| G2 타입 | 오류 0 | `tsc --noEmit` exit 0 | ✅ |
| G3 빌드·자체완결 | 맨 클론서 `--version`·`init`·`status` exit 0 | 전부 exit 0 (+`doctor`) | ✅ |
| G4 훅 무해 | 4이벤트 × 4입력 exit 0 · 미초기화 0바이트 | **12/12 exit 0**, 0바이트 확인 | ✅ |
| G5 훅 강제력 | 소스·코어파일·경로우회·배포Bash deny · stop block | **Write/Edit + Bash 표면 전건 deny** | ✅ (라운드1) |
| G6 🔴 MCP 승인 불가 | 승인 시도 후 상태 불변 | **submitted 불변**, 우회 3종 거절 · 저널 위조 경로도 차단 | ✅ (라운드1) |
| G7 결정성 | 3회 동일·`Math.random` 0·마이그레이션 양방향 | 동일 · 실사용 0 · 왕복 성공 | ✅ |
| G8 공급망 | 프로덕션 도달 crit/high 0 | `npm audit --omit=dev` **0** | ✅ |
| G9 훅 지연 | pre-tool p95 < 150ms | 정상 경로 ✅ / **폴백은 재측정 대기**(측정 창 무효) | ⚠ 조건 ① |
| G10 이력 비밀 | 0 | gitleaks 71 커밋 **no leaks** | ✅ |
| G11 CLI 계약 | `--help` exit 0 · 침묵 성공 0 | `--help` exit 0 + 20군 나열 · 침묵 성공 0 | ✅ (라운드1) |
| G12 관측성 | 조용한 실패 0 · doctor 가 상태 반영 | 침묵 catch 5/5 ✅ · 승인 후 doctor `ok:true` · 복구 거부 해소 | ✅ (라운드1) |
| G13 패키징 | 선언 산출물 누락 0 | 매니페스트·스킬 11·에이전트 5·dist 전부 존재 | ✅ |

---

# 축별 결과

| 축 | 결과 | 파일 |
|---|---|---|
| ① 기능 완성도 | 문서화된 53개 명령 중 **2개 미구현**(`trace`·`gate feedback`) | `01-features.md` |
| ② 계약(CLI·MCP·훅) | `--help` 부재 · 하위명령 안내 절반만 · `wave create` 침묵 성공 | `02-backend.md` |
| ③ 출력 표면 | 결함 0 — ANSI 0·비TTY JSON·EPIPE 내성 | `03-ui.md` |
| ④ E2E 페르소나 | 4 페르소나 완주, PASS 55 / FAIL 3(전부 스크립트 결함) | `04-usability.md` |
| ⑤ 성능 | 정상 경로는 저널 크기 무관 · 폴백만 게이트 초과 | `05-perf.md` |
| ⑥ 보안 | **BLOCKER 2** — Bash 표면 미검사. 그 밖(인젝션·경로·비밀)은 견고 | `06-security.md` |
| ⑦ 공급망 | 프로덕션 도달 0 · dev 5건은 도달 경로 추적 후 deferred | `07-supply-chain.md` |
| ⑧ 논리 불변식 | 12개 선언 불변식 중 11 통과 · `doctor --repair` 데이터 손실 1 | `08-logic.md` |
| ⑨ 결정성 | 584×3 동일 · 빌드 바이트 재현 · 시각 주입 규율 부분 적용 | `09-determinism.md` |
| ⑩ 배포·롤백 | 맨 클론·롤백 리허설 실제 성공 · `--force` 자기해제 경로 | `10-deploy.md` |
| ⑪ 관측성 | 침묵 catch 5/5 문서화 · **유일한 건강검진이 상시 빨강** | `11-ops.md` |

---

# 감정서 대조 — `docs/appraisal/2026-08-21-plugin-appraisal.html`

세션 중 사용자가 붙인 감정서를 **현재 코드로 재확인**했다. 이 문서는 커밋 `bbbb9b6`
(198 tests, 코어 1,550줄) 기준이라 **핵심 판정 몇 개가 낡았다.**

| 감정서 주장 | 현재 코드 실측 |
|---|---|
| 「페이즈 게이트 미구현 — `phase set` 이 자기해제 구멍」 | **낡음.** 게이트 구현됨 — `phase set P7` 이 「직전 게이트 미승인」으로 거부된다. 단 **`--force` 로 이름만 바뀌어 남아 있다** → [SHIP-52] |
| 「테스트 198건 · 코어 1,550줄」 | 낡음 — **584건 · 7,574줄** |
| 「슬래시 커맨드 없어 발견성 부족」 | 부분 낡음 — **스킬 11종** 존재. 그러나 **CLI `--help` 는 여전히 없다** → [UX-24] 로 유지 |
| 「한국어 전용 README」 | **해소됨** — README 4개 언어(EN/KO/JA/ZH). CLI·훅 출력도 라운드 1에서 영문 기본(`lang: ko` 로 전환), **생성 문서까지 라운드 2에서 완료** → [USE-59] |
| 「배포 캐시 67MB — devDeps 동봉」 | **해당 없음** — `git archive HEAD` 는 **1.9MB**, node_modules 0건 |
| 「PostToolUse matcher `*` 로 도구 호출마다 node 기동, p95 104ms」 | **유효** — 재측정 103ms. 도구 호출당 pre+post 2회 발화 |
| 「`.harness/` 가 git 대상이라 브랜치 전환 시 되감김, `events.jsonl` 병합 충돌 미해결」 | **유효·미검증** — `.gitignore` 에 `.harness/` 없음(커밋 대상 확인). 병합 충돌 시나리오는 이번에 **보지 않았다** |
| 「의식 비용·온보딩 골짜기」 | **유효** — ④ 축에서 같은 마찰을 관측 |

**감정서가 놓친 것**: Bash 표면 미검사([SEC-49][SEC-50]). 감정서는 「경로 우회를 진지하게
막았다」고 평가했는데, 그건 `Write` 표면 이야기다. 소스를 전량 읽고도 못 봤다 —
**실제로 Bash 로 써 보기 전까지는 안 보이는 결함**이다(Iron Rule 3).

---

# 이번에 보지 않은 축

## 가. 의도적 제외 (결정)
- **①의 경쟁 벤치마크** — 같은 범주 제품의 체험 수단이 없다. 시늉 감사는 미감사보다 나쁘므로 뺐다.
  자사 스펙·README 대비 완성도로 대체했다.
- **⑥ 의 `/security-review`·`/cso` 위임** — 축을 서브에이전트로 분리하지 않고 직접 수행했다
  (세션 지침상 Agent 도구 미사용). 도구가 없어서가 아니라 **방식 선택**이다.

## 나. 확인 불가 (볼 수단이 없었다)
- **실제 마켓플레이스 설치 경로** — GitHub 리모트가 없다. 사용자가 리모트를 만들면
  `claude plugin install` 로 1회 실측할 수 있다.
- **다른 장비에서의 설치** — 이 장비 1대에서만 확인했다.
- **`.harness/` 브랜치 병합 충돌** — 감정서가 지적한 시나리오. `events.jsonl` 을 양쪽 브랜치에서
  append 한 뒤 병합하는 실험이 필요하다. 이번 범위에 넣지 못했다.
- **동시 세션 경합** — 같은 `.harness/` 에 두 세션이 동시에 쓸 때의 저널 원자성. 코드상
  append-only 이나 **실측하지 않았다.**
- **장기 상주 관찰** — 수백 턴 누적 시 `hook-errors.log`·저널 증가 거동.

---

# 사용자·제품 결정 사항 (기술 판정 아님)

1. ~~**LICENSE 파일이 없다.**~~ → **결정·완료.** 사용자 결정은 **MIT** — `LICENSE` 추가됨(라운드 1).
2. ~~**CLI·훅 출력이 한국어 전용.**~~ → **결정·완료.** 사용자 결정은 **영문 기본 + `lang: ko`**
   (없애는 게 아니라 기본값을 바꾼다). 예외 메시지는 라운드 1, **생성 문서 5종은 라운드 2**에서
   닫혔다 — 영문 기본에서 산출물 한국어 **0** 실측, ko 회귀 유지([USE-59]).
3. **CI 가 없다.** 테스트·타입·audit 이 사람 손으로만 돈다.
4. **GitHub 리모트 없음 · main 병합 보류 · push 금지** — 이전 세션 결정 유지.
5. ~~**`--force` 를 남길 것인가.**~~ → **결정·완료.** 훅에서 deny + `HARNESS_ALLOW_FORCE=1` 로만
   허용(사람이 터미널에서 켜는 탈출구). [SHIP-52] verified.

**남은 결정**: 위 3·4 (CI 없음 · 리모트 없음/push 금지/main 병합 보류) 만 유효하다.

---

# 출하 후 백로그 (deferred)

- **[DEP-32]** dev 체인 취약점 5건(critical 1) — 전부 「리스닝 dev 서버」 전제라 도달 경로 없음.
  vitest 3.x 업그레이드는 파괴적 변경. `evidence/gates.log` · `07-supply-chain.md`.

---

# 이 감사에서 배운 것

1. **정적 감사·소스 전량 열람으로도 안 보이는 결함이 있다.** 감정서는 코어 11모듈을 전부 읽고
   「경로 우회를 진지하게 막았다」고 평가했다. 사실이다 — `Write` 표면에서는. **막힌 다음에
   무엇을 시도하게 되는지**를 실제로 해 봐야 옆문이 보인다.
2. **측정 도구가 결함을 지어낸다.** zsh 는 무인용 확장을 단어 분리하지 않는다 — 이 한 가지로
   「미구현 명령 48건」이라는 가짜 결함이 나왔다. 실측 결과가 너무 극적이면 도구부터 의심하라.
3. **상시 빨간 진단은 위조를 숨긴다.** [OPS-20](doctor 상시 오탐)이 없었다면 [SEC-49] 의 저널
   위조가 `doctor` 의 `ok:false` 로 드러났을 것이다. 오탐 하나가 진짜 경보를 무의미하게 만든다 —
   Iron Rule 6 이 테스트뿐 아니라 **운영 도구에도** 적용된다.
4. **같은 규율의 부분 적용이 결함의 뿌리다.** 시각 주입(`usage`·`migrate`·`tokens` 에는 있고
   `gate` 에는 없음 → [OPS-20]), 하위명령 안내(절반만 → [API-27]), 인젝션 방어(두 벌 → [SEC-28]),
   코어 파일 보호(Write 만 → [SEC-51]). **네 결함이 전부 같은 모양이다.**
