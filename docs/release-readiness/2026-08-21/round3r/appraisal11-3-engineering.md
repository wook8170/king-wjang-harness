# 축 3 엔지니어링 품질 — 라운드 3-R 감정

**대상** `main` `d5c851c` · **감정자** 구현 세션(자기 채점)
**루브릭 조건**: 테스트 **전건 green ×3 동일** · `tsc` 0 · **빌드 바이트 재현** · **중복 규칙 0**

## 조건별 실측

| 조건 | 측정 | 결과 |
|---|---|---|
| 전건 green ×3 동일 | `npx vitest run` 3회(별도 프로세스) | **1364 passed ×3, 57 파일** · fail 0 · **skip 0**(런타임에 skipped 줄 없음) |
| tsc 0 | `npx tsc --noEmit` | exit 0 |
| 빌드 바이트 재현 | `npm run build` 2회 후 `shasum -a 256 core/dist/*.js` | **두 해시 동일**(cli.js·mcp.js) |
| 중복 규칙 0 | `core/test/duplicate-rules.test.ts` — 같은 정규식 리터럴·같은 문자열 목록이 두 곳 이상이면 빨강 | **0건** (예외 1건은 사유 기록: 포함/배제 반대 방향) |

## 이번 라운드에 닫은 것

- **[ENG-278]** 가드가 «머신 속도»를 재고 있었다 — 절대 밀리초 문턱이 회귀 없이 빨강이 됐고
  (3회 중 2회), **처방을 되돌려도 초록이었다**. 처방 자체를 결정적으로 단언하도록 바꾸고
  시간 검사는 훅 계약(10초 fail-open)에서 문턱을 가져와 «자릿수 회귀 전용»으로 남겼다.
  같은 부류인 **[COST-177]** 은 기준선을 그 자리에서 만들게 했다(분리도 385배).
- **[COST-293]** 이번 라운드의 봉인 셋이 재토큰화를 만들어 800세그먼트 판정이 **11,743ms**
  가 됐다 — **[ENG-278] 이 남겨 둔 시간 검사가 잡았다.** 재토큰화 3자리 제거로
  **784·933·894ms**(라운드 착수 시점 2,574ms 보다 빠르다).
- **[ENG-292]** 소환 사유 목록 3벌 → 정본 파생.
- **[ENG-294]** 정규식 리터럴까지 훑어 5부류 더 제거(웨이브 파일명 5벌 · `NAME=VALUE` 4곳 ·
  `-c` 2곳 · **URL 스킴 2곳 — 한쪽만 `/i` 라 판정이 실제로 달랐다** · `[^\p{L}\p{N}]` 2벌).
  **찾는 일 자체를 기계에 맡겼다** — RED 확인 완료.

## 점수

**4.8** — 조건 4개 전건 충족, 그중 「중복 규칙 0」은 이제 **상시 측정**이다.

### 잔여 감점 (LOW 이하로 판단한 근거)

`duplicate-rules.test.ts` 는 **정규식 리터럴과 문자열 목록**을 본다 — 같은 규칙이 서로 다른
표기(예: 정규식 vs `startsWith` 체인)로 두 벌 있으면 못 잡는다. 이것은 한계이지 미충족이
아니다(지금 그런 사본이 있다는 증거는 없고, 이번 스캔이 찾은 것은 전부 닫혔다).
