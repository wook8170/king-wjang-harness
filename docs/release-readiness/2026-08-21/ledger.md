# 결함 대장 — king-wjang-harness `e860460` (feature/core-engine-v0)

**갱신** 2026-08-22 (라운드 3-D — 독립 재감정 반영) · **판정** 출하 불가 · **open BLOCKER** 0 · **open HIGH** 0 · **open 전체** 1 (MED 1 — [SEC-79]) · **fixed(재측정 대기)** 0 (+ deferred 1)

🔴 **라운드 3 중반에 낸 「출하 가능」 판정을 철회한다.** 42 에이전트 독립 적대 감사가
**BLOCKER 2건**을 찾았고 전건 재현했다: 판정 정책 파일이 피판정자 쓰기 영역 안에 있어
**한 줄로 자기 무장해제**가 되고([SEC-69]), 구축·출하 트랙 P7~P12 에 **강제가 아예 없다**([SEC-70]).

철회 사유는 새 결함이 아니라 **측정 범위**다 — 라운드 3의 「우회 60/60 deny · 과차단 0」은
*막힐 거라 예상한 목록*만 담았고, 정책 파일 자체·설계 트랙 밖 페이즈·과차단 여부가 빠져 있었다.
라운드 2 과다 주장과 같은 뿌리다([OPS-74]).

라운드 1 수정 완료 — 상세는 `fixes-round1.md`, 닫은 증거는 `evidence/round1-verify.log`.
라운드 2(생성 문서 i18n + CLI 플래그 정합성) — 상세는 `fixes-round2.md`, 닫은 증거는 `evidence/round2-verify.log`.
라운드 3 — PERF 분편은 `fixes-round3-perf.md`(피어 세션), 본편은 `fixes-round3.md` / `evidence/round3-verify.log`.
**라운드 3 본편은 라운드 2의 USE-59 결론을 정정한다** — 「산출물 한국어 0」은 부분 스윕에 근거한
과다 주장이었고, 잔여는 [I18N-62] 로 이관해 닫았다(Iron Rule 7: 자기 보고를 의심한다).

ID 는 **20 번부터** 시작한다 — `docs/release-readiness/readiness.md`(커밋 `bbbb9b6`, 198 tests)의
1~19 번대와 겹치지 않게 하기 위해서다. 그 대장은 **대상 커밋이 다르므로 이 감사에서 승계하지 않았다**
(코드가 3배로 늘었고 `verified` 행은 전부 낡았다 — 해당 불변식은 여기서 재측정해 다시 올렸다).

| ID | 심각도 | 축 | 한 줄 | 상태 | 근거등급 | 근거 | 닫은 증거 |
|---|---|---|---|---|---|---|---|
| OPS-20 | HIGH | 11 | 게이트를 한 번이라도 승인하면 `doctor` 가 영구히 `gates 불일치`·exit 1 — 유일한 건강검진이 상시 빨강이라 진짜 드리프트를 덮는다 | verified | measured | `core/src/gate.ts:412` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| LOGIC-21 | HIGH | 08 | `doctor --repair` 가 게이트의 `evidence`·`submittedAt` 을 삭제한다 — 저널은 갖고 있는데 재생 리듀서가 반영하지 않는다 | verified | measured | `core/src/events.ts:78` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| FEAT-22 | HIGH | 01 | `harness trace <노드ID>` 가 CLI 에 없다 — 스펙과 `wave-verifier` 에이전트 지시문이 호출한다(MCP 도구로만 존재) | verified | measured | `agents/wave-verifier.md:34` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| FEAT-23 | HIGH | 01 | `harness gate feedback` 미구현 — 공개 README 4개 언어 모두가 기능으로 광고한다 | verified | measured | `README.md:88` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| UX-24 | HIGH | 02 | `--help`·`-h`·`help`·무인자가 전부 exit 1 「알 수 없는 명령」 — 13개 명령군·60여 하위명령의 진입점이 0 | verified | measured | `core/src/cli.ts:806` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-25 | MED | 06 | 게이트 산출물 경로가 루트 밖(`../../../etc/passwd`·`/etc/hosts`)이어도 제출·승인된다 — 웨이브 id 는 검증하면서 산출물 경로는 안 한다 | verified | measured | `core/src/gate.ts:330` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| PERF-26 | MED | 05 | state.json 부재(저널 폴백) + 저널 10만 건에서 pre-tool p95 169ms — G9 목표 150ms 초과 | verified | measured | `docs/release-readiness/2026-08-21/evidence/latency.log` | 라운드 3 재측정 — 폴백 p95 82~101ms < 150ms(2창×정순·역순, 통제 무이상). load<2 조건 편차·채택 사유는 `fixes-round3-perf.md`·latency.log 라운드3 절 |
| API-27 | MED | 02 | 명령군 절반(gate·adr·doc·wave·node)은 하위명령 목록을 안 알려주고 나머지 절반은 알려준다 | verified | measured | `core/src/cli.ts:219` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-28 | MED | 06 | 인젝션 방어 규칙 `sanitizeUntrusted` 가 두 벌이고 구현이 서로 다르다(정규식 vs 코드포인트 루프) | verified | measured | `core/src/loop.ts:100` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| API-29 | MED | 02 | `wave create` 를 인자 없이 부르면 exit 0 으로 목표·마일스톤 없는 `wave-001` 이 생긴다(침묵 성공) | verified | measured | `core/src/cli.ts:818` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| API-30 | LOW | 02 | CLI `wave update` 는 위치인자, MCP 는 `text` 파라미터 — `--text "x"` 를 쓰면 「--text x」가 그대로 로그된다 | verified | measured | `core/src/cli.ts:704` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SHIP-31 | LOW | 10 | `schemaVersion: 99` 인 미래 상태 파일을 경고 없이 그대로 읽는다 | verified | measured | `core/src/state.ts:10` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| DEP-32 | LOW | 07 | dev 체인 취약점 5건(critical 1) — 전부 「리스닝 dev 서버」 전제라 프로덕션·빌드 도달 없음 | deferred | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | 출하 후 백로그 — vitest 3.x 는 파괴적 변경이고 도달 경로가 없다 |
| SEC-49 | BLOCKER | 06 | 저널에 `phase-set`/`gate-approved` 한 줄을 Bash 로 append + `doctor --repair` 만으로 페이즈·게이트가 위조된다 — **사람 승인 없이 게이트가 approved 가 된다** | verified | measured | `core/src/hook.ts:412` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-50 | BLOCKER | 06 | 설계 트랙 소스 쓰기 금지가 Bash 로 무력화 — `Write` 는 deny 하면서 `echo "x" > src/app.ts`·heredoc·`touch` 는 허용 | verified | measured | `core/src/hook.ts:412` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-51 | HIGH | 06 | 코어 파일 손편집 차단이 Write/Edit 표면에만 적용 — `echo x > .harness/state.json`·`sed -i` 는 통과 | verified | measured | `core/src/hook.ts:50` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SHIP-52 | HIGH | 10 | `harness phase set <P> --force` 가 게이트 검사를 건너뛴다. 훅이 이 Bash 를 막지 않아 에이전트가 스스로 설계 트랙을 풀 수 있다(의도된 탈출구이나 접근 제한이 없다) | verified | measured | `core/src/cli.ts:254` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| DET-53 | — | 09 | `replayState` 가 비결정적이었다 — 같은 저널을 두 번 재생하면 `updatedAt` 이 달랐다(전체 스위트에서만 재현되던 플레이키) | verified | measured | `core/src/events.ts:63` | `fixes-round1.md` · 3회 반복 동일 |
| OPS-55 | HIGH | 11 | 하네스가 쓰는 이벤트 타입 18종이 미등록 → doctor 가 저널을 불신해 **`doctor --repair` 가 복구를 거부**(정상 사용만으로 잠김) | verified | measured | `core/src/events.ts:15` | `fixes-round1.md` · `evidence/round1-verify.log` OPS-55 절 repaired:true |
| LOGIC-56 | HIGH | 08 | `gate-invalidated` 미폴드 — 산출물이 바뀌어 무효가 된 게이트를 `doctor --repair` 가 승인으로 되살렸다 | verified | measured | `core/src/events.ts:78` | `fixes-round1.md` · `core/test/bashwrite.test.ts:159` |
| SEC-33 | — | 06 | 🔴 **MCP 경로로는** 게이트 승인 불가 — 3종 우회 시도 후에도 `submitted` 불변. **단 저널 위조 경로는 열려 있다 → [SEC-49]** | verified | measured | `core/src/mcp.ts:299` | `evidence/e2e.log` G6 절 10건 PASS |
| SEC-34 | — | 06 | 셸 인젝션 없음(인자가 셸을 경유하지 않음) · 심링크 우회 deny · 프로토타입 오염 입력에 무사 | verified | measured | `core/src/hook.ts:526` | `evidence/contract2.log` · `evidence/e2e.log` |
| SEC-35 | — | 06 | 이력 비밀 0 — 71 커밋 전수 스캔 | verified | measured | `docs/release-readiness/2026-08-21/evidence/secrets.log` | gitleaks 8.30.1 `no leaks found` |
| LOGIC-36 | — | 08 | 훅 무해·비간섭 — 미초기화 4 이벤트 0바이트·exit 0, 깨진/빈/미지/null/9000자/유니코드 입력 전부 exit 0 | verified | measured | `core/src/hook.ts:161` | `evidence/e2e.log` 12건 PASS |
| LOGIC-37 | — | 08 | 훅 강제력 — 소스쓰기·코어파일·경로우회·심링크·배포Bash deny, docs·일반Bash 허용 | verified | measured | `core/src/hook.ts:526` | `evidence/e2e.log` 7건 PASS |
| LOGIC-38 | — | 08 | 상태 복원력 — state.json 삭제·형태손상에도 훅 exit 0, 저널 손상은 doctor 가 거부로 알린다 | verified | measured | `core/src/hook.ts:148` | `evidence/e2e.log` 3건 PASS |
| USE-39 | — | 04 | 페르소나 4종(신규·설계자·구현자·출하) 전 시나리오 완주 — 유령 참조·증적 없는 UX 웨이브·중복 활성 전부 정상 거부 | verified | measured | `docs/release-readiness/2026-08-21/evidence/e2e.log` | PASS=55 FAIL=3(전부 스크립트 오류로 규명) |
| DET-40 | — | 09 | 584 tests ×3 동일 · tsc 오류 0 · 결정 경로에 `Math.random`·`Date.now()` 실사용 0(주석뿐) | verified | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | 3회 모두 `584 passed (584)` |
| DET-41 | — | 09 | 빌드 재현성 — 재빌드 산출물이 커밋된 `core/dist` 와 바이트 동일 | verified | measured | `core/dist/cli.js` | `git diff --stat core/dist` 빈 출력 |
| SHIP-42 | — | 10 | 맨 클론(빌드·node_modules 없음)에서 CLI·훅 4종·MCP tools/list 전부 동작 | verified | measured | `docs/release-readiness/2026-08-21/evidence/deploy.log` | `--version`·`init`·`status`·`doctor` exit 0 |
| SHIP-43 | — | 10 | 롤백 리허설 — 구버전(`f8b1516`)이 신버전 상태 위에서 동작하고, 이후 신버전이 데이터 보존 상태로 읽는다 | verified | measured | `docs/release-readiness/2026-08-21/evidence/rollback.log` | 구버전 훅 deny 정상 · 신버전 `gate status` 복원 |
| SHIP-44 | — | 10 | dist 부재 클론 — 일반 명령 exit 1 + 빌드 안내, 훅 exit 0(세션 불파괴), MCP 는 빈 도구 목록으로 생존 | verified | measured | `bin/harness:5` | `evidence/deploy.log` |
| SHIP-45 | — | 10 | 패키징 — 플러그인 매니페스트·훅 4이벤트·스킬 11·에이전트 5·프로파일 2·dist 2 전부 아카이브에 포함, 버전 일치 | verified | measured | `docs/release-readiness/2026-08-21/evidence/contract.log` | manifest ↔ 파일 대조 누락 0 |
| UX-46 | — | 03 | CLI 출력 — ANSI 0 · NO_COLOR 무영향 · 비TTY stdout JSON 파싱 가능 · EPIPE 내성 | verified | measured | `docs/release-readiness/2026-08-21/evidence/contract.log` | md5 동일 · `JSON.parse` OK |
| OPS-47 | — | 11 | 침묵 catch 5건 — 전부 「세션을 깨지 않는다」 계약 경로이고 대체 관측 경로(`hook-errors.log`)가 존재. 무처리·무문서 catch 0 | verified | code | `core/src/hook.ts:161` | 전 소스 스캔 결과 5/5 문서화 |
| DEP-48 | — | 07 | 프로덕션 도달 취약점 0 — 런타임 의존은 `yaml` 하나 | verified | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | `npm audit --omit=dev` → `found 0 vulnerabilities` |
| API-57 | HIGH | 02 | `wave create --acceptance` 가 조용히 무시된다 — `--help` 가 광고하는데 파서는 `--accept` 만 읽어, 도움말대로 치면 수용 기준이 빈 채로 웨이브가 생기고 검증자 브리프가 「판정 불가」를 낸다 | verified | measured | `core/src/cli.ts:786` | `fixes-round2.md` · `docs/release-readiness/2026-08-21/evidence/round2-verify.log` — 수정 전 `[]` → 후 `['login returns 200']`, 회귀 테스트 `core/test/cli.test.ts:42` |
| API-58 | MED | 02 | `design baseline <UX-x> --png <file>` 가 성립 불가 — 파서가 위치 인자만 읽어 `--png` 문자열 자체를 경로로 삼는다(도움말대로 친 호출이 exit 1) | verified | measured | `core/src/cli.ts:533` | `fixes-round2.md` · `docs/release-readiness/2026-08-21/evidence/round2-verify.log` — 수정 전 exit 1 → 후 exit 0, 회귀 테스트 `core/test/cli.test.ts:59` |
| USE-59 | HIGH | 04 | 생성 문서 5종(리뷰 패킷·RTM·허브 / 결함 대장·릴리스 체크리스트 / 웨이브 브리프·소환문 / Playwright 사양·비교 패킷 / 정본 HTML)이 한국어 전용 — 마켓플레이스 배포 시 첫 산출물이 읽히지 않는다 **⚠ 라운드 2의 verified 는 30개 명령 부분 스윕에 근거한 과다 주장이었다 → 잔여는 [I18N-62] 로 이관** | verified | measured | `core/src/report.ts:103` | `fixes-round2.md` · `evidence/round2-verify.log` — **5종 자체는 실측 확인. 다만 「산출물 한국어 0」 결론은 [I18N-62] 로 대체한다** |
| SPEC-60 | HIGH | 06 | 스펙 §12 가 명시한 「init 시 allowlist 무력화 경고 고지」 미구현 — 사용자가 `harness gate approve` 를 permission allowlist 에 넣으면 §4-3 「승인의 최종 클릭은 사람」이 통째로 무력화되는데 아무도 말해주지 않았다 | verified | measured | `core/src/cli.ts:170` | `fixes-round3.md` · `evidence/round3-verify.log` — init 이 stderr 로 고지, 건별 테스트 |
| SPEC-61 | HIGH | 01 | 스펙 §10 token-guard 흡수 미완 — 티어(90/95/99)를 코어가 계산만 하고 **세션에 전달하는 경로가 없었다**(수동 `harness usage` 뿐). 95% 에서 세션이 갈리면 새 세션은 임계 근처인 줄 모른 채 크게 벌인다 | verified | measured | `core/src/hook.ts:227` | `fixes-round3.md` · `evidence/round3-verify.log` — SessionStart 가 현재 티어 주입, 테스트 3건 |
| LOGIC-63 | HIGH | 08 | 턴 로그 파싱 앵커가 **언어에 의존** — `hook.ts` 가 `'## 턴 로그'` 문자열로 찾는데 지시서 본문은 생성 시점 `lang` 을 따라간다. 영문 프로젝트에서 발췌가 **조용히 빔**(이어받기가 가장 중요한 순간에 무음 실패). `lang` 전환 시 과거 파일도 전부 안 읽힘. **라운드 2가 만든 회귀** | verified | measured | `core/src/hook.ts:59` | `fixes-round3.md` · `evidence/round3-verify.log` — 두 언어 매칭 정규식, 건별 테스트 2건(수정 전 레드 실증) |
| I18N-62 | HIGH | 04 | 라운드 2의 「산출물 한국어 0」이 과다 주장 — 잔여 표면 8모듈(adr 렌더 패킷·doctor 진단 전량·gate verifyGate 사유·wave 예외 3·usage 티어 지침·migrate 안내 전문·profile 진단 전량·hook 발췌 펜스)과 번들 `profiles/` 전량(profile/commands yaml·guidance 3종·raw-values 룰팩) | verified | measured | `core/test/i18n-en-default.test.ts:140` | `fixes-round3.md` · `evidence/round3-verify.log` — 전량 처리 + **회귀 가드 자동화**(6절, 오염 주입으로 무는 것 실증) |
| OPS-64 | MED | 11 | 「한국어 0」을 사람이 매번 손으로 재는 구조 자체가 결함 — 라운드 2의 과다 주장이 그 산물이다. 측정 범위가 코드에 없으면 새 명령·새 오류 경로가 사정권 밖으로 조용히 빠진다 | verified | measured | `core/test/i18n-en-default.test.ts:45` | `fixes-round3.md` — help 레지스트리 순회라 **새 명령이 자동 편입**, 고장 상태 주입 절 포함 |
| SEC-65 | HIGH | 06 | noclobber 무시 리다이렉트(`>` 뒤에 파이프를 붙인 형태)가 쓰기 대상 추출을 빠져나간다 — `echo x >PIPE src/app.ts` 로 설계 트랙 소스 차단이, 같은 형태로 `.harness/events.jsonl` 코어 파일 보호가 한 글자 차이로 풀린다 (표에 파이프 문자를 쓸 수 없어 `PIPE` 로 표기) | verified | measured | `core/src/bashwrite.ts:83` | `fixes-round3.md` · `evidence/round3-verify.log` §7 — 우회 매트릭스 31종 전건 재측정, 회귀 테스트 3건 |
| SEC-66 | HIGH | 06 | 변형 명령 안전망이 **비대칭** — `python3 -c "open('x','w')"` 류가 `.harness/` 코어 파일에는 막히는데 **설계 트랙 소스에는 통과**했다. 방어가 대칭이 아니면 뚫리는 쪽이 정본이 된다 | verified | measured | `core/src/hook.ts:504` | `fixes-round3.md` · `evidence/round3-verify.log` §7 — `pathLikeMentions` 안전망을 소스에도 적용, 과차단 0(조회·루트밖 쓰기 전건 allow) |
| SEC-67 | HIGH | 06 | **받아쓰기·간접 실행이 판정 자체를 비껴간다** — `curl -o src/app.ts <url>` (참조 구현을 소스로 받아오기)는 리다이렉트도 알려진 쓰기 명령도 아니라 대상 추출이 없었다. `wget -O` · `xargs` 로 감싼 cp · `prettier --write` · `eslint --fix` 도 같은 이유로 통과 | verified | measured | `core/src/bashwrite.ts:154` | `fixes-round3.md` · `evidence/round3-verify.log` §8 — 수정 전 19종 중 9건 통과 → 후 전건 기대대로(과차단 0: 조회·복원·루트밖 6종 allow 유지), 회귀 테스트 7건 |
| SEC-68 | HIGH | 06 | **위치가 경로임을 말해 주는 자리에서 경로 판별을 요구해 생긴 구멍** — `cp -r /tmp/x src` 의 `src` 는 슬래시·확장자가 없어 대상에서 빠졌다(rubric 이 명시적으로 덮는다고 한 cp/mv 규칙의 구멍). 디렉토리 이름 하나로 소스 트리를 통째로 덮어쓸 수 있었다. `git clone <url> src` · `rsync` · `sponge` · 배치 편집기 · `tar -C` · `unzip -d` · `patch` · `git apply` 도 같은 라운드에서 닫음 | verified | measured | `core/src/bashwrite.ts:179` | `fixes-round3.md` · `evidence/round3-verify.log` §9 — 우회 10계열 **60/60 deny**, 의도적 허용 **18/18 allow**(과차단 0), 회귀 테스트 23건 |
| SEC-69 | BLOCKER | 06 | 🔴 **자기 무장해제** — 판정 정책 파일 `.harness/config.yaml` 이 훅에서 allow 였다(코어 보호는 state/events/ledger 3종뿐). 설계 트랙은 그 파일의 `design_allowed_prefixes` **allow-list** 로 판정하므로 빈 문자열 접두사 한 줄이면 모든 경로에 매치돼 **강제가 통째로 꺼졌다** | verified | measured | `core/src/hook.ts:67` | `evidence/round3-verify.log` §10 — 정책 파일(`config.yaml` + `.harness/profile/`)을 상태 파일과 같은 등급으로 올려 Write·셸 전 경로 deny. 재현 절차 재실행: 정책 쓰기 6종 전건 deny · **과차단 0**(design/packets/waves/docs/README 전건 allow) |
| SEC-70 | BLOCKER | 06 | 🔴 **구축·출하 트랙(P7~P12) 강제가 0** — 스펙 §4-2 매트릭스 2·3행 미구현. 정상 게이트 경로로 재측정: P8 에서 배포 명령·설계 문서 직접 수정 allow, P12 에서 신규 기능 코드 allow. 13페이즈 중 6페이즈에 강제 없음 | verified | measured | `core/src/hook.ts:590` | `evidence/round3-verify.log` §11 — P7~P9 배포 3종·설계 문서 수정 deny(backtrack 중이면 허용), P10~P12 신규 파일·미승인 배포 deny(기존 파일 수정·`.harness/` 산출물은 allow). **과차단 0**: 소스·테스트·npm test 는 구축 트랙에서 그대로 통과 |
| UX-71 | HIGH | 03 | **과차단이 광범위하고 사유 문구가 사실과 다르다** — 설계 트랙에서 `notes.txt` · `.gitignore` · `.env.example` · `assets/logo.svg` · `package.json` · `test/a.test.ts` 가 전부 deny 되고 사유는 "Source code cannot be written". 허용은 `.harness/` · `docs/` · 루트 `*.md` 뿐이라 **소스가 아닌 파일도 막힌다** — 사용자가 하네스를 끄게 만드는 방향 | verified | measured | `core/src/hook.ts:295` | allow-list 단일 규칙 → **deny-list 2단**(프로파일 `source_globs` → 소스 확장자 44종 바닥). 사유는 실제로 걸린 규칙을 말한다. **워크플로 산출은 그대로 머지하지 않았다** — 적대적 검증이 새 구멍 3건을 E2E 로 실증했고 메인에서 전부 닫은 뒤 들였다: ① **심링크 비대칭**(`config/settings.yaml -> src/app.ts` 로 실제로 소스를 덮어썼다 — 최종 판정이 `rel`·`realRel` 중 한쪽만 봤다) → 양쪽을 보고 걸린 경로를 사유에 표시. ② **테스트 디렉토리 예외가 은닉 통로**(`test/app.ts`·`tests/server.go`·`spec/engine.py` 전부 allow — 접두사 6글자로 강제가 꺼졌다) → 예외를 **파일 이름 규칙**으로 좁혔다(`*.test.*`·`*_test.*`·`test_*`·`conftest.py`·`FooTest.java`). ③ **안내가 새로 거짓**(「설정은 쓸 수 있다」인데 `next.config.js`·`vite.config.ts` 등 35종이 deny) → 설정 파일 예외(`*.config.*`·`*.conf.*`·`.*rc.js`·`gulpfile`)를 추가하고 SessionStart·deny 문구를 실제 규칙과 일치시켰다. 측정(P2): 구현 차단 **22/22** · 테스트 접두 구멍 **6/6 차단** · **과차단 0/44**(설정 16종·테스트 8종 포함) · 심링크 Write·Bash 양 표면 DENY(소스 아닌 대상은 ALLOW) · 706 → **791 tests green**. 대가로 이름 없는 보조 파일(`test/helpers/factory.ts`)은 막힌다 — **의도된 계약**으로 테스트에 고정했다. 전문 `evidence/round3-verify.log §16` |
| I18N-72 | HIGH | 04 | **i18n 이 절반이었다** — CLI·코어만 영문화했고 실제 배포되어 모델에게 지시를 내리는 계층(skills 11 · agents 5 · marketplace.json · plugin.json)에 **한글 15,569자**가 남아 있다. 비한국어 사용자에게 워크플로 지침 계층이 통째로 불가용 | verified | measured | `core/test/i18n-en-default.test.ts:199` | skills 11 · agents 5 · `.claude-plugin/*.json` 2 = **16개 파일 전량 영문화**(한글 15,569 → **0**, 남은 6자는 저자 이름 「장욱」 — 고유명사라 번역 대상이 아니다). 번역만 하지 않고 **낡은 서술을 실측으로 교정**했다: 「`harness` 엔 `--help` 가 없다」(UX-24 로 생겼다) · 「캔버스 URL 필드가 코어에 없다」(`harness design link` 가 있다) · 「게이트 피드백 수집 명령이 아직 없다」(FEAT-23 이 넣었다) · 설계 트랙 허용 목록 서술(UX-71 로 deny-list 2단이 됐다) · 옛 한국어 deny 문구 인용(현재 영문 문구로). **회귀 가드**를 `i18n-en-default.test.ts` 에 추가했다 — 목록이 아니라 `skills/`·`agents/`·`.claude-plugin/` **디렉토리 전체**를 훑으므로 새 스킬이 자동으로 사정권에 든다. 오염 주입(`agents/researcher.md` 에 한글 1줄)으로 **실제로 무는 것**을 확인했다(RED → 복구 후 GREEN). 796 tests green 전문 `evidence/round3-verify.log` §17 |
| FEAT-73 | MED | 01 | SessionStart 첫 지시가 **패키지에 없는 명령**을 가리킨다 — `remote_control` 기본 on 이라 매 세션 `/remote-control` 실행을 지시하는데 `commands/` 디렉토리 자체가 없다 | verified | measured | `core/src/hook.ts:328` | 번호 붙은 지시에서 빼고 **맨 뒤 조건부 안내 1줄**로 내렸다(「있으면 실행, 없으면 건너뛴다」 + 폴백 채널 + 끄는 법). 첫 지시는 활성 웨이브 이어받기. 기본값 on·요구 15 유지 — 훅은 슬래시 명령 존재를 알 수 없어 탐침은 거짓 음성을 낳는다. 스펙 `evidence/round3-verify.log` §13-6a 「첫 행동으로 지시」와 어긋나므로 스펙에 개정 블록 명시. 측정: en/ko 41/41 PASS, **과차단 0**(on→off 차이가 그 한 줄뿐 · 지시 번호 1부터 연속 · pre-tool deny 4/allow 6 불변 · `.harness/` 없으면 침묵), 회귀 테스트 7건 |
| OPS-74 | HIGH | 11 | **자기 채점의 구조적 한계가 실증됐다** — 라운드 3의 「우회 60/60 deny · 과차단 0」 보고는 *내가 막힐 거라 예상한 목록*만 담았다. 정책 파일 쓰기 · 설계 트랙 밖 페이즈 · 「막지 말아야 할 것을 막는지」가 목록에 없었다. 42 에이전트 독립 적대 감사가 BLOCKER 2건을 찾았다 | verified | measured | `agents/readiness-auditor.md:43` | 절차로 못 박았다 — 「더 열심히」로는 안 나오는 종류의 결함이라 **규칙**이 필요했다. ① 차단 측정은 **「막으면 안 되는 것」 목록과 짝**으로만 보고한다(한쪽만 있는 측정은 반쪽이다) ② 자기 목록이 구조적으로 빠뜨리는 **세 축을 강제**한다: 판정의 **입력**(정책·설정)을 쓰는 것 · 작업한 트랙 **밖**의 상태 · **반대 방향**(막지 말아야 할 것) ③ **구현자가 쓴 측정 목록은 근거가 아니다** — 검증자가 자기 목록을 쓴다 ④ 재현 실패가 곧 반증이 아니다(규칙 소스를 읽고 정확한 레버를 찾은 뒤 판정) ⑤ **중간 계층만 보고 판정 금지**(「훅이 allow」 ≠ 「잠금이 열렸다」 — 끝까지 실행해 상태 변화를 관측). 배포되는 `agents/readiness-auditor.md` 철칙 6~8 과 리포 자체 절차 `.claude/skills/verify/SKILL.md` 양쪽에 넣었다. **실효 실측(라운드 3-D)**: 워크플로 구현 4건에 이 절차를 적용해 **3건에서 구현자가 못 본 결함**을 잡았다 — UX-71 새 구멍 3건(심링크로 실제 소스 덮어쓰기 성공) · OPS-76 우회 2건(개행 1회·`node` 직접 실행으로 탐지 장치 정지) · SEC-75 과차단 2건. 나머지 1건(FEAT-73)은 「과장 없음」 확인. **검증 없이 머지했으면 넷 다 「닫힘」으로 대장에 올라갔을 것이다.** 전문 `evidence/round3-verify.log §18` |
| SEC-75 | HIGH | 06 | **게이트가 산출물의 내용·구별성을 전혀 검사하지 않는다** — 2바이트 파일 1장으로 `gate submit --evidence measured` + `approve` 가 rc=0 으로 통과하고, 같은 파일 하나로 12개 게이트를 전부 열 수 있다. 근거 등급 `measured` 도 자기 신고라 검증되지 않는다 | verified | measured | `core/src/gate.ts:162` | 제출 시점 검사 3종: **최소 실질성**(공백 제외 80자 — 이 리포 실산출물 최소 213자·정당한 한국어 3문장 91자 아래, 공격의 40배 위 / 바이트가 아니라 **문자**로, 파일별이 아니라 **집합 전체**로), **구별성**(경로가 아니라 **내용** 다이제스트 — `cp a.md b.md` 를 실측으로 잡아 경로 기반을 폐기), **페이즈 적합성**(레지스트리가 아는 경로는 제 페이즈에서만). **워크플로 산출을 그대로 받지 않았다** — 검증자가 잡은 과차단 2건(부분 등록 상태에서 미등록 P6 리포트+등록 P0~P5 동반이 차단)을 메인에서 재현해 `assertPhaseFit` 을 고쳤다: **미등록 경로가 하나라도 섞이면 면제**, 남는 차단은 「제출 전부가 등록됐고 이 페이즈가 하나도 없는 경우」뿐. 측정: 과차단 2건 → 전건 ALLOW · 잡으려던 케이스는 그대로 DENY · 2바이트 1장 DENY(전: 13/13 통과, `ship verdict` GO) · 뮤테이션 9/9 RED · 724 → **770 tests green**. **닫히지 않은 부류는 [SEC-79] 로 분리했다** — 「한 글자 덧붙이기」가 아니라 실 산출물 0장으로도 열린다는 것이 실측됐고, 코드 머리말의 경계 서술을 그 실측으로 정정했다. 전문 `evidence/round3-verify.log §15` |
| OPS-76 | HIGH | 11 | **정책 변경이 탐지되지 않는다** — [SEC-69] 로 에이전트 경로는 닫았지만, 사람이 터미널에서 `config.yaml` 을 직접 고치면(의도된 탈출구) `doctor` 는 여전히 exit 0 · issues 0 이다. 정책이 바뀐 사실이 어디에도 남지 않아, 리뷰어가 「왜 이 프로젝트는 설계 트랙에서 소스를 쓸 수 있나」를 알 방법이 없다 | verified | measured | `core/src/policy.ts:96` | 정책 파일 집합(`config.yaml` + `.harness/profile/**`)의 SHA-256 을 저널에 고정(`policy-pinned`)하고 doctor 가 대조한다 — 드리프트는 **warning**(issue 아님: 재생으로 못 고치고, 정당할 수 있다). 정당한 정산은 `doctor --accept-policy`. **워크플로 산출을 그대로 받지 않고 메인에서 다시 쟀다** — 검증자가 짚은 대로 그 잠금이 `HARNESS_CMD_RE`(자기호출 제외용)를 쓰고 있어 개행·`env`·`sudo`·`bash -c` 로 풀렸다 → SEC-78 의 **탐지 전용** 패턴 + `(?![\w-])` 경계로 교체. 훅을 타지 않는 `node core/dist/cli.js doctor --accept-policy` 도 E2E 로 통했으므로 **CLI 2차 게이트 `HARNESS_ACCEPT_POLICY=1` 을 추가**했다(SHIP-52 의 `HARNESS_ALLOW_FORCE` 와 같은 형태 — 한 겹만으로는 방어 심층이 아니다). 측정: 우회 12종 전건 deny · 과차단 0/8 · node 직접 실행 exit 1(경고 유지) · env 켜면 exit 0(경고 정산) · 732 tests green. **의도된 과차단**: 하네스 명령과 함께 인용부호 안에 그 명령을 적으면 막힌다(`--force` 잠금이 이미 치르는 값 — 좁히면 `bash -c` 가 열린다). 전문 `evidence/round3-verify.log §14` |
| SEC-77 | HIGH | 06 | 스펙 §4-2 **1행** 「P0~P6 빌드·배포 명령 차단」이 반쪽 — 배포 목록이 리터럴 5개뿐이라 `npm publish`·`terraform apply`·`wrangler deploy` 등 최빈 배포 명령이 통과했고, **빌드 명령 차단은 아예 없었다** | verified | measured | `core/src/config.ts:13` | `evidence/round3-verify.log` §11 — 배포 목록을 계열별 21종으로 넓히고(컨테이너·PaaS·레지스트리·인프라) 프로파일이 정의한 `build` 명령을 설계 트랙에서 차단. config·번들 프로파일·코드 내장 바닥값 **3곳 동기화**(기존 테스트가 불일치를 잡았다). 과차단 0 |
| SEC-78 | HIGH | 06 | **자기호출 인식 정규식이 개행·접두 명령에 뚫린다** — `cd /tmp` 뒤 개행, `env`/`sudo`/`nohup`/`time` 접두, 백틱·`$()`·`bash -c` 안에서 `phase set --force` 가 훅을 통과했다(9종 중 8종). 실제 페이즈 변경은 CLI 의 `HARNESS_ALLOW_FORCE` env 게이트가 2차로 막아 **열리지는 않았으나**, 방어 심층의 한 겹이 이미 뚫린 상태였다 | verified | measured | `core/src/hook.ts:64` | `evidence/round3-verify.log` §12 — 탐지 전용 패턴을 stop 가드용과 **분리**(두 용도는 틀려야 안전한 방향이 반대다). 11종 전건 deny · 과차단 0(`git commit -m "harness"` 등 6종 allow 유지). 워크플로 적대적 검증자가 발견 |
| SEC-79 | MED | 06 | **실 산출물 0장으로 13게이트가 열린다** — [SEC-75] 가 2바이트 1장을 닫았지만, 서로 다른 **필러 파일 13장**(각 80자, 총 1KB)이면 `gate submit` 13건 전건 통과 → 승인 시 `ship verdict` **GO** · `doctor` issues 0. 비용이 40배 올랐을 뿐 부류는 그대로다. 최저가 경로가 SEC-75 가 **과차단 가드로 고정한 조합의 상위집합**이라 「막으면 안 되는 것」과 우회로가 같은 메커니즘을 쓴다 | open | measured | `core/src/gate.ts:23` | — 재현: `docs/f0..f12.md = "a"*80+i` → 13/13 개통 · GO · doctor clean(§12). 내용의 **질**을 재는 것은 코어(순수·로컬·결정적) 밖이므로 여기서 닫을 수 있다고 쓰지 않는다 — 코드 머리말 경계를 실측대로 정정했고, 남은 방어는 사람 승인(§4-3)이다. 닫으려면 게이트 밖(리뷰 패킷 제시 방식·승인 UX)에서 다뤄야 한다 |
| API-80 | LOW | 02 | **도움말이 광고하는 인자 형태가 먹지 않는다** — `harness doc url` 도움말은 `--url <주소>` 를 적는데 구현은 위치인자만 받아서, 도움말대로 친 사람이 「artifact URL 이 https 가 아니다: "--url"」로 exit 1 을 본다. API-30 과 같은 계열(CLI·MCP·도움말 간 인자 불일치) | verified | measured | `core/src/cli.ts:751` | — I18N-72 중 P0 스킬 절차를 실측하다 발견. API-30 과 **같은 처방으로 둘 다 받게** 했다(도움말과 구현이 갈리면 고칠 곳은 「사람이 친 것이 먹게 만드는 것」이다) + 도움말을 실제 형태로 정정. 측정: 위치인자·`--url` 양쪽 exit 0 · URL 없으면 exit 1(침묵 성공 없음) · 회귀 4건 |
| USE-81 | MED | 04 | **공개 README 4개 언어 전부가 있는 기능을 「없다」고 적는다** — 「`--help` 는 없다; 이 표가 레퍼런스다」인데 [UX-24] 로 `--help` 가 생겼고 전 명령군에서 동작한다. [FEAT-23](README 가 없는 기능을 광고)의 **반대 방향**이며, 사용자는 있는 진입점을 못 찾는다 | verified | measured | `README.md:187` | — 재감정 감정자(효용성 축)가 `README.ja.md` 에서 처음 짚었고 4개 언어 전건 재현. 문장을 실제 동작(`harness --help` · `harness <군> --help`)으로 교체하고, `doctor` 행이 4개 언어 모두 `--accept-policy` 를 빠뜨린 것도 함께 채웠다(번역본만 낡는 것이 이 리포의 반복 실패 모드다). 측정: 문서(README 4 + skills 11 + agents 5)에서 `harness ...` 호출 **45조합 추출 → MISSING 0** |
| USE-82 | MED | 04 | **`backtrack` 이 사유 없이 성공한다** — `harness backtrack P0` 가 exit 0 으로 `reason: "(미기재)"` 를 상태·저널에 기록한다. 「침묵 성공 0」 위반이자 **영문 기본 출력에 한국어가 박히는 경로**(i18n 가드의 명령 목록에 없어서 안 잡혔다) | verified | measured | `core/src/cli.ts:966` | — 독립 재감정(사용성 축) 발견, 메인에서 재현. `--reason` 을 필수로 하고 빈 문자열·공백도 거부. 역행은 승인된 설계로 되돌아가는 결정이라 「왜」가 없으면 나중에 재구성할 수 없다. 측정: 사유 없음 exit 1 + **상태 파일 바이트 불변** · 공백만 exit 1 · 사유 있으면 기록 · **영문 출력 한국어 0**. 회귀 3건(`cli-contract.test.ts`) |
| API-83 | LOW | 02 | `ship deploy` 도움말이 **필수 `--sha` 를 광고하지 않는다** — 광고된 형태 그대로 치면 exit 1. [API-80]과 같은 부류의 재발 | verified | measured | `core/src/help.ts:161` | — 도움말을 실제 필수 인자로 정정. 「도움말대로 쳤는데 실패한다」는 사용자가 자기 실수라고 오해하는 종류의 결함이다 |
| API-84 | MED | 02 | **같은 명령군 안에서 인자 형태가 갈린다** — `ship defect add` 는 `--id` 만, `update` 는 위치인자만 받는다(`update --id X` → 「No such defect id: --id」). [API-30]·[API-80]과 같은 뿌리 | verified | measured | `core/src/cli.ts:365` | — 양쪽 다 받게 했다(API-30 관례). 측정: add 위치인자·`--id` 각각 exit 0 · update 위치인자·`--id` 각각 exit 0 · 회귀 2건 |
| UX-85 | MED | 04 | **미초기화 가드가 명령마다 갈린다** — `status` 만 「run `harness init`」 을 안내하고 `wave create`·`gate status`·`phase set`·`node upsert`·`ship verdict`·`loop next` 는 **내부 tmp 경로가 박힌 raw ENOENT** 를 뱉는다. `loop next` 는 exit 0 으로 `wave create` 를 권하는데 그대로 치면 ENOENT — **안내가 실행 불가능** | verified | measured | `core/src/cli.ts:176` | — 가드를 **한 곳으로** 모았다(명령마다 복제하면 새 명령이 생길 때마다 빠진다 — SEC-50 이 그 사고였다). **아는 명령에만** 걸어 오타는 UX-24 계약대로 「알 수 없는 명령」이 먼저다. 측정: 10개 명령 전건 init 안내 · ENOENT 0 · **내부 경로 노출 0** · 미지 명령은 init 안내가 아님 · `--help`/`--version` 은 init 전에도 exit 0. 회귀 12건 |
| UX-86 | LOW | 04 | **필수 인자를 빼면 `undefined` 가 오류문에 박힌다** — `adr show` → 「No such ADR: undefined」, `wave activate` → 「No instruction sheet for wave undefined」. 사람이 「내가 등록을 안 했나」로 **오진**한다. 오타에 가까운 명령 제안도 없다 | verified | measured | `core/src/cli.ts:160` | — 값이 흘러들기 전에 잡는 `req()` 를 도입하고 6개 경로에 적용, 오타에는 편집거리 기반 제안을 붙였다(임계 = 이름 길이/3, 넘으면 제안 안 함 — **틀린 제안은 제안 없음보다 나쁘다**). 측정: 6종 전건 `undefined` 0 + 사용법 문자열 · `stauts`→`status`·`wav`→`wave` 제안 · `xyzzy` 무제안. 회귀 7건 |
| LOGIC-87 | LOW | 08 | **`node upsert --parent` 가 원장에 없는 부모를 무경고 수용**한다 — `wave create --refs` 는 검증하는데 여기만 안 해서 댕글링 부모가 조용히 생기고 RTM 사슬이 끊긴다 | verified | measured | `core/src/cli.ts:871` | — `--refs` 와 같은 계약으로 검증하고 자기 자신을 부모로 두는 것도 막았다. 측정: 없는 부모 exit 1 + 등록 방법 안내 · 자기참조 exit 1 · 있는 부모/부모 없음 exit 0. 회귀 3건 |
| SPEC-88 | LOW | 09 | **기본 프로파일에서는 빌드 차단이 무효다** — `generic` 의 `commands.yaml` 이 `build: ''` 라 `commandFor` 가 undefined 를 주고 설계 트랙에서 `npm run build`·`make`·`tsc` 가 ALLOW. 배포 차단(코어 내장 22종)은 유효하나, [SEC-77] 문구를 읽은 사람이 「빌드도 막힌다」로 과대평가한다 | verified | measured | `profiles/generic/commands.yaml:10` | — **고치지 않고 문구를 정확히 했다.** 빈 값 = 「지어내지 말고 사람에게 물어라」는 의도된 설계이고(모르는 스택의 빌드 명령을 박으면 P7 「CI 그린」이 「실행된 적 없는 명령이 실패하지 않았다」로 통과한다), 설계 트랙에는 애초에 새 구현 코드가 없어 빌드 차단의 보안 가치가 낮은 반면 과차단 위험은 실재한다. 프로파일 주석에 강제 결과를 명시. 측정: deploy 계열 DENY · `npm run build`/`make`/`tsc` ALLOW(문서화된 한계) |
| OPS-89 | MED | 11 | **판정 블록과 대장 집계가 갈린다 — 세 번째 재발** — 대장 행을 고치고 `00-summary.md` 판정 블록을 안 고쳐 「verified 58 · open 6」 대 실제가 어긋났다. 두 번은 피어가, 세 번째는 독립 감정자가 잡았다. **사람이 매번 손으로 세는 구조**가 원인 | verified | measured | `core/test/ledger-summary-sync.test.ts:99` | — 손으로 고치지 않고 **테스트로 고정**했다([OPS-64]와 같은 처방). 대장 헤더 집계 대 실제 행 · 판정 블록의 verified/open 수 · open 항목 전건 나열 · **닫힌 항목이 open 표에 남아 있는지**를 검사한다. 커밋 해시는 검사하지 않는다 — 커밋마다 필연적으로 낡고 그 사실은 정보가 아니다. 도입 즉시 실제 드리프트(58 대 59)를 잡아 RED 로 시작했다. 회귀 4건 |
| SEC-90 | BLOCKER | 06 | 🔴 **접두 명령 하나로 쓰기 스캐너가 통째로 꺼진다** — `sudo tee src/app.ts` · `nohup cp /tmp/x .harness/state.json` · `time sed -i "" s/a/b/ .harness/config.yaml` 이 **전 페이즈에서 ALLOW**. `commandName` 이 벗기는 접두가 env 대입뿐이라 명령 이름이 `sudo`/`nohup`/`time`/`env`/`command`/`nice`/`exec`/`doas` 로 잡혀 `tee`·`cp`·`sed -i` 규칙을 아예 안 탔다. **소스만이 아니라 코어·정책 파일까지** 열려 SEC-49/50/69 가 닫은 표적이 접두어 한 조각으로 재개방됐다 | verified | measured | `core/src/bashwrite.ts:70` | — 독립 재감정(실효성 축) 발견, 메인 전건 재현. **에이전트 레인**이다(사용자가 터미널에서 치는 것은 의도된 탈출구라 결함이 아니다). 리다이렉트 형태는 원문 스캔이 따로 잡아 계속 DENY 였고, 그래서 **명령 계열에서만 조용히 성립**해 리다이렉트만 재는 목록으로는 안 보였다([OPS-74] 가 말한 축). 고침: `PREFIX_COMMANDS` 와 그 플래그·값(`sudo -u me`·`nice -n 10`·`timeout 5`)까지 벗기고 진짜 명령 이름을 잡는다. 측정: 접두 12종 × 표적 5종 **60/60 DENY** · **과차단 0/15**(`sudo ls`·`time npm test`·증적 쓰기 유지) · 회귀 2건 |
| SEC-91 | HIGH | 06 | **디렉토리를 대상으로 준 쓰기가 판정을 빠져나간다** — `mv /tmp/x src` · `cp -r /tmp/x .harness` · `tar -C src` · `unzip -d src` · `git clone <url> src` 가 ALLOW. 스캐너는 대상을 정확히 뽑아 왔는데 **판정이 파일 이름 일치만** 봐서 `.harness` ≠ `.harness/state.json`, `src` ∉ `src/**` 로 빠졌다. `find -exec sed -i` 도 안쪽 명령을 안 봤다. 생성물을 만들어 `mv` 로 제자리에 옮기는 것은 **모델이 자연히 가는 경로**다 | verified | measured | `core/src/profile.ts:474` | — 보호 경로가 대상 **아래에 있으면** 같은 판정을 주도록 `coversPath` 를 두고, 소스 글롭은 리터럴 접두로 `isSourceTree` 를 새로 만들었다. `find -exec` 는 xargs 와 같은 처방(안쪽 명령을 다시 판정). 측정: 우회 17종 **전건 DENY** · **과차단 0/11**(`cp -r /tmp/dir docs`·증적 디렉토리·읽기 전용 `find` 유지) · **페이즈 계약 불변**(P8 에서 `mv /tmp/x src` ALLOW, `.harness` 는 계속 DENY) · 회귀 3건 |
