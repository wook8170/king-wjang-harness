# 결함 대장 — king-wjang-harness `e860460` (feature/core-engine-v0)

**갱신** 2026-08-22 (라운드 3-K 진행 — 3-J·3-K 결함 등재·봉인 중) · **판정** 출하 불가 · **open BLOCKER** 0 · **open HIGH** 0 · **open 전체** 25 (MED 7 · LOW 18) · **verified** 248 · **deferred** 5 · **fixed(재측정 대기)** 0

🔴 **라운드 3 중반에 낸 「출하 가능」 판정을 철회한다.** 42 에이전트 독립 적대 감사가
**BLOCKER 2건**을 찾았고 전건 재현했다: 판정 정책 파일이 피판정자 쓰기 영역 안에 있어
**한 줄로 자기 무장해제**가 되고([SEC-69]), 구축·출하 트랙 P7~P12 에 **강제가 아예 없다**([SEC-70]).

철회 사유는 새 결함이 아니라 **측정 범위**다 — 라운드 3의 「우회 60/60 deny · 과차단 0」은
*막힐 거라 예상한 목록*만 담았고, 정책 파일 자체·설계 트랙 밖 페이즈·과차단 여부가 빠져 있었다.
라운드 2 과다 주장과 같은 뿌리다([OPS-74]).

라운드 1 수정 완료 — 상세는 `fixes-round1.md`, 닫은 증거는 `evidence/round1-verify.log`.
라운드 2(생성 문서 i18n + CLI 플래그 정합성) — 상세는 `fixes-round2.md`, 닫은 증거는 `evidence/round2-verify.log`.
라운드 3 — PERF 분편은 `fixes-round3-perf.md`(피어 세션), 본편은 `fixes-round3.md` / `evidence/round3-verify.log`.
**라운드 3 본편은 라운드 2의 USE-59 결론을 정정한다** — 「산출물 한국어 0」은 부분 스윕에 근거한
과다 주장이었고, 잔여는 [I18N-62] 로 이관해 닫았다(Iron Rule 7: 자기 보고를 의심한다).

ID 는 **20 번부터** 시작한다 — `docs/release-readiness/readiness.md`(커밋 `bbbb9b6`, 198 tests)의
1~19 번대와 겹치지 않게 하기 위해서다. 그 대장은 **대상 커밋이 다르므로 이 감사에서 승계하지 않았다**
(코드가 3배로 늘었고 `verified` 행은 전부 낡았다 — 해당 불변식은 여기서 재측정해 다시 올렸다).

| ID | 심각도 | 축 | 한 줄 | 상태 | 근거등급 | 근거 | 닫은 증거 |
|---|---|---|---|---|---|---|---|
| OPS-20 | HIGH | 11 | 게이트를 한 번이라도 승인하면 `doctor` 가 영구히 `gates 불일치`·exit 1 — 유일한 건강검진이 상시 빨강이라 진짜 드리프트를 덮는다 | verified | measured | `core/src/gate.ts:681` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| LOGIC-21 | HIGH | 08 | `doctor --repair` 가 게이트의 `evidence`·`submittedAt` 을 삭제한다 — 저널은 갖고 있는데 재생 리듀서가 반영하지 않는다 | verified | measured | `core/src/events.ts:78` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| FEAT-22 | HIGH | 01 | `harness trace <노드ID>` 가 CLI 에 없다 — 스펙과 `wave-verifier` 에이전트 지시문이 호출한다(MCP 도구로만 존재) | verified | measured | `agents/wave-verifier.md:34` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| FEAT-23 | HIGH | 01 | `harness gate feedback` 미구현 — 공개 README 4개 언어 모두가 기능으로 광고한다 | verified | measured | `README.md:88` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| UX-24 | HIGH | 02 | `--help`·`-h`·`help`·무인자가 전부 exit 1 「알 수 없는 명령」 — 13개 명령군·60여 하위명령의 진입점이 0 | verified | measured | `core/src/cli.ts:278` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-25 | MED | 06 | 게이트 산출물 경로가 루트 밖(`../../../etc/passwd`·`/etc/hosts`)이어도 제출·승인된다 — 웨이브 id 는 검증하면서 산출물 경로는 안 한다 | verified | measured | `core/src/gate.ts:330` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| PERF-26 | MED | 05 | state.json 부재(저널 폴백) + 저널 10만 건에서 pre-tool p95 169ms — G9 목표 150ms 초과 | verified | measured | `docs/release-readiness/2026-08-21/evidence/latency.log` | 라운드 3 재측정 — 폴백 p95 82~101ms < 150ms(2창×정순·역순, 통제 무이상). load<2 조건 편차·채택 사유는 `fixes-round3-perf.md`·latency.log 라운드3 절 |
| API-27 | MED | 02 | 명령군 절반(gate·adr·doc·wave·node)은 하위명령 목록을 안 알려주고 나머지 절반은 알려준다 | verified | measured | `core/src/cli.ts:286` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-28 | MED | 06 | 인젝션 방어 규칙 `sanitizeUntrusted` 가 두 벌이고 구현이 서로 다르다(정규식 vs 코드포인트 루프) | verified | measured | `core/src/loop.ts:100` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| API-29 | MED | 02 | `wave create` 를 인자 없이 부르면 exit 0 으로 목표·마일스톤 없는 `wave-001` 이 생긴다(침묵 성공) | verified | measured | `core/src/cli.ts:1132` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| API-30 | LOW | 02 | CLI `wave update` 는 위치인자, MCP 는 `text` 파라미터 — `--text "x"` 를 쓰면 「--text x」가 그대로 로그된다 | verified | measured | `core/src/cli.ts:964` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SHIP-31 | LOW | 10 | `schemaVersion: 99` 인 미래 상태 파일을 경고 없이 그대로 읽는다 | verified | measured | `core/src/state.ts:10` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| DEP-32 | LOW | 07 | dev 체인 취약점 5건(critical 1) — 전부 「리스닝 dev 서버」 전제라 프로덕션·빌드 도달 없음 | deferred | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | 출하 후 백로그 — vitest 3.x 는 파괴적 변경이고 도달 경로가 없다 |
| SEC-49 | BLOCKER | 06 | 저널에 `phase-set`/`gate-approved` 한 줄을 Bash 로 append + `doctor --repair` 만으로 페이즈·게이트가 위조된다 — **사람 승인 없이 게이트가 approved 가 된다** | verified | measured | `core/src/hook.ts:597` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-50 | BLOCKER | 06 | 설계 트랙 소스 쓰기 금지가 Bash 로 무력화 — `Write` 는 deny 하면서 `echo "x" > src/app.ts`·heredoc·`touch` 는 허용 | verified | measured | `core/src/hook.ts:597` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SEC-51 | HIGH | 06 | 코어 파일 손편집 차단이 Write/Edit 표면에만 적용 — `echo x > .harness/state.json`·`sed -i` 는 통과 | verified | measured | `core/src/hook.ts:81` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| SHIP-52 | HIGH | 10 | `harness phase set <P> --force` 가 게이트 검사를 건너뛴다. 훅이 이 Bash 를 막지 않아 에이전트가 스스로 설계 트랙을 풀 수 있다(의도된 탈출구이나 접근 제한이 없다) | verified | measured | `core/src/cli.ts:452` | `fixes-round1.md` · `docs/release-readiness/2026-08-21/evidence/round1-verify.log` |
| DET-53 | — | 09 | `replayState` 가 비결정적이었다 — 같은 저널을 두 번 재생하면 `updatedAt` 이 달랐다(전체 스위트에서만 재현되던 플레이키) | verified | measured | `core/src/events.ts:63` | `fixes-round1.md` · 3회 반복 동일 |
| OPS-55 | HIGH | 11 | 하네스가 쓰는 이벤트 타입 18종이 미등록 → doctor 가 저널을 불신해 **`doctor --repair` 가 복구를 거부**(정상 사용만으로 잠김) | verified | measured | `core/src/events.ts:15` | `fixes-round1.md` · `evidence/round1-verify.log` OPS-55 절 repaired:true |
| LOGIC-56 | HIGH | 08 | `gate-invalidated` 미폴드 — 산출물이 바뀌어 무효가 된 게이트를 `doctor --repair` 가 승인으로 되살렸다 | verified | measured | `core/src/events.ts:78` | `fixes-round1.md` · `core/test/bashwrite.test.ts:159` |
| SEC-33 | — | 06 | 🔴 **MCP 경로로는** 게이트 승인 불가 — 3종 우회 시도 후에도 `submitted` 불변. **단 저널 위조 경로는 열려 있다 → [SEC-49]** | verified | measured | `core/src/mcp.ts:309` | `evidence/e2e.log` G6 절 10건 PASS |
| SEC-34 | — | 06 | 셸 인젝션 없음(인자가 셸을 경유하지 않음) · 심링크 우회 deny · 프로토타입 오염 입력에 무사 | verified | measured | `core/src/hook.ts:638` | `evidence/contract2.log` · `evidence/e2e.log` |
| SEC-35 | — | 06 | 이력 비밀 0 — 71 커밋 전수 스캔 | verified | measured | `docs/release-readiness/2026-08-21/evidence/secrets.log` | gitleaks 8.30.1 `no leaks found` |
| LOGIC-36 | — | 08 | 훅 무해·비간섭 — 미초기화 4 이벤트 0바이트·exit 0, 깨진/빈/미지/null/9000자/유니코드 입력 전부 exit 0 | verified | measured | `core/src/hook.ts:197` | `evidence/e2e.log` 12건 PASS |
| LOGIC-37 | — | 08 | 훅 강제력 — 소스쓰기·코어파일·경로우회·심링크·배포Bash deny, docs·일반Bash 허용 | verified | measured | `core/src/hook.ts:638` | `evidence/e2e.log` 7건 PASS |
| LOGIC-38 | — | 08 | 상태 복원력 — state.json 삭제·형태손상에도 훅 exit 0, 저널 손상은 doctor 가 거부로 알린다 | verified | measured | `core/src/hook.ts:204` | `evidence/e2e.log` 3건 PASS |
| USE-39 | — | 04 | 페르소나 4종(신규·설계자·구현자·출하) 전 시나리오 완주 — 유령 참조·증적 없는 UX 웨이브·중복 활성 전부 정상 거부 | verified | measured | `docs/release-readiness/2026-08-21/evidence/e2e.log` | PASS=55 FAIL=3(전부 스크립트 오류로 규명) |
| DET-40 | — | 09 | 584 tests ×3 동일 · tsc 오류 0 · 결정 경로에 `Math.random`·`Date.now()` 실사용 0(주석뿐) | verified | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | 3회 모두 `584 passed (584)` |
| DET-41 | — | 09 | 빌드 재현성 — 재빌드 산출물이 커밋된 `core/dist` 와 바이트 동일 | verified | measured | `core/dist/cli.js` | `git diff --stat core/dist` 빈 출력 |
| SHIP-42 | — | 10 | 맨 클론(빌드·node_modules 없음)에서 CLI·훅 4종·MCP tools/list 전부 동작 | verified | measured | `docs/release-readiness/2026-08-21/evidence/deploy.log` | `--version`·`init`·`status`·`doctor` exit 0 |
| SHIP-43 | — | 10 | 롤백 리허설 — 구버전(`f8b1516`)이 신버전 상태 위에서 동작하고, 이후 신버전이 데이터 보존 상태로 읽는다 | verified | measured | `docs/release-readiness/2026-08-21/evidence/rollback.log` | 구버전 훅 deny 정상 · 신버전 `gate status` 복원 |
| SHIP-44 | — | 10 | dist 부재 클론 — 일반 명령 exit 1 + 빌드 안내, 훅 exit 0(세션 불파괴), MCP 는 빈 도구 목록으로 생존 | verified | measured | `bin/harness:5` | `evidence/deploy.log` |
| SHIP-45 | — | 10 | 패키징 — 플러그인 매니페스트·훅 4이벤트·스킬 11·에이전트 5·프로파일 2·dist 2 전부 아카이브에 포함, 버전 일치 | verified | measured | `docs/release-readiness/2026-08-21/evidence/contract.log` | manifest ↔ 파일 대조 누락 0 |
| UX-46 | — | 03 | CLI 출력 — ANSI 0 · NO_COLOR 무영향 · 비TTY stdout JSON 파싱 가능 · EPIPE 내성 | verified | measured | `docs/release-readiness/2026-08-21/evidence/contract.log` | md5 동일 · `JSON.parse` OK |
| OPS-47 | — | 11 | 침묵 catch 5건 — 전부 「세션을 깨지 않는다」 계약 경로이고 대체 관측 경로(`hook-errors.log`)가 존재. 무처리·무문서 catch 0 | verified | code | `core/src/hook.ts:197` | 전 소스 스캔 결과 5/5 문서화 |
| DEP-48 | — | 07 | 프로덕션 도달 취약점 0 — 런타임 의존은 `yaml` 하나 | verified | measured | `docs/release-readiness/2026-08-21/evidence/gates.log` | `npm audit --omit=dev` → `found 0 vulnerabilities` |
| API-57 | HIGH | 02 | `wave create --acceptance` 가 조용히 무시된다 — `--help` 가 광고하는데 파서는 `--accept` 만 읽어, 도움말대로 치면 수용 기준이 빈 채로 웨이브가 생기고 검증자 브리프가 「판정 불가」를 낸다 | verified | measured | `core/src/cli.ts:977` | `fixes-round2.md` · `docs/release-readiness/2026-08-21/evidence/round2-verify.log` — 수정 전 `[]` → 후 `['login returns 200']`, 회귀 테스트 `core/test/cli.test.ts:42` |
| API-58 | MED | 02 | `design baseline <UX-x> --png <file>` 가 성립 불가 — 파서가 위치 인자만 읽어 `--png` 문자열 자체를 경로로 삼는다(도움말대로 친 호출이 exit 1) | verified | measured | `core/src/cli.ts:533` | `fixes-round2.md` · `docs/release-readiness/2026-08-21/evidence/round2-verify.log` — 수정 전 exit 1 → 후 exit 0, 회귀 테스트 `core/test/cli.test.ts:59` |
| USE-59 | HIGH | 04 | 생성 문서 5종(리뷰 패킷·RTM·허브 / 결함 대장·릴리스 체크리스트 / 웨이브 브리프·소환문 / Playwright 사양·비교 패킷 / 정본 HTML)이 한국어 전용 — 마켓플레이스 배포 시 첫 산출물이 읽히지 않는다 **⚠ 라운드 2의 verified 는 30개 명령 부분 스윕에 근거한 과다 주장이었다 → 잔여는 [I18N-62] 로 이관** | verified | measured | `core/src/report.ts:103` | `fixes-round2.md` · `evidence/round2-verify.log` — **5종 자체는 실측 확인. 다만 「산출물 한국어 0」 결론은 [I18N-62] 로 대체한다** |
| SPEC-60 | HIGH | 06 | 스펙 §12 가 명시한 「init 시 allowlist 무력화 경고 고지」 미구현 — 사용자가 `harness gate approve` 를 permission allowlist 에 넣으면 §4-3 「승인의 최종 클릭은 사람」이 통째로 무력화되는데 아무도 말해주지 않았다 | verified | measured | `core/src/cli.ts:172` | `fixes-round3.md` · `evidence/round3-verify.log` — init 이 stderr 로 고지, 건별 테스트 |
| SPEC-61 | HIGH | 01 | 스펙 §10 token-guard 흡수 미완 — 티어(90/95/99)를 코어가 계산만 하고 **세션에 전달하는 경로가 없었다**(수동 `harness usage` 뿐). 95% 에서 세션이 갈리면 새 세션은 임계 근처인 줄 모른 채 크게 벌인다 | verified | measured | `core/src/hook.ts:263` | `fixes-round3.md` · `evidence/round3-verify.log` — SessionStart 가 현재 티어 주입, 테스트 3건 |
| LOGIC-63 | HIGH | 08 | 턴 로그 파싱 앵커가 **언어에 의존** — `hook.ts` 가 `'## 턴 로그'` 문자열로 찾는데 지시서 본문은 생성 시점 `lang` 을 따라간다. 영문 프로젝트에서 발췌가 **조용히 빔**(이어받기가 가장 중요한 순간에 무음 실패). `lang` 전환 시 과거 파일도 전부 안 읽힘. **라운드 2가 만든 회귀** | verified | measured | `core/src/hook.ts:59` | `fixes-round3.md` · `evidence/round3-verify.log` — 두 언어 매칭 정규식, 건별 테스트 2건(수정 전 레드 실증) |
| I18N-62 | HIGH | 04 | 라운드 2의 「산출물 한국어 0」이 과다 주장 — 잔여 표면 8모듈(adr 렌더 패킷·doctor 진단 전량·gate verifyGate 사유·wave 예외 3·usage 티어 지침·migrate 안내 전문·profile 진단 전량·hook 발췌 펜스)과 번들 `profiles/` 전량(profile/commands yaml·guidance 3종·raw-values 룰팩) | verified | measured | `core/test/i18n-en-default.test.ts:140` | `fixes-round3.md` · `evidence/round3-verify.log` — 전량 처리 + **회귀 가드 자동화**(6절, 오염 주입으로 무는 것 실증) |
| OPS-64 | MED | 11 | 「한국어 0」을 사람이 매번 손으로 재는 구조 자체가 결함 — 라운드 2의 과다 주장이 그 산물이다. 측정 범위가 코드에 없으면 새 명령·새 오류 경로가 사정권 밖으로 조용히 빠진다 | verified | measured | `core/test/i18n-en-default.test.ts:45` | `fixes-round3.md` — help 레지스트리 순회라 **새 명령이 자동 편입**, 고장 상태 주입 절 포함 |
| SEC-65 | HIGH | 06 | noclobber 무시 리다이렉트(`>` 뒤에 파이프를 붙인 형태)가 쓰기 대상 추출을 빠져나간다 — `echo x >PIPE src/app.ts` 로 설계 트랙 소스 차단이, 같은 형태로 `.harness/events.jsonl` 코어 파일 보호가 한 글자 차이로 풀린다 (표에 파이프 문자를 쓸 수 없어 `PIPE` 로 표기) | verified | measured | `core/src/bashwrite.ts:224` | `fixes-round3.md` · `evidence/round3-verify.log` §7 — 우회 매트릭스 31종 전건 재측정, 회귀 테스트 3건 |
| SEC-66 | HIGH | 06 | 변형 명령 안전망이 **비대칭** — `python3 -c "open('x','w')"` 류가 `.harness/` 코어 파일에는 막히는데 **설계 트랙 소스에는 통과**했다. 방어가 대칭이 아니면 뚫리는 쪽이 정본이 된다 | verified | measured | `core/src/hook.ts:810` | `fixes-round3.md` · `evidence/round3-verify.log` §7 — `pathLikeMentions` 안전망을 소스에도 적용, 과차단 0(조회·루트밖 쓰기 전건 allow) |
| SEC-67 | HIGH | 06 | **받아쓰기·간접 실행이 판정 자체를 비껴간다** — `curl -o src/app.ts <url>` (참조 구현을 소스로 받아오기)는 리다이렉트도 알려진 쓰기 명령도 아니라 대상 추출이 없었다. `wget -O` · `xargs` 로 감싼 cp · `prettier --write` · `eslint --fix` 도 같은 이유로 통과 | verified | measured | `core/src/bashwrite.ts:432` | `fixes-round3.md` · `evidence/round3-verify.log` §8 — 수정 전 19종 중 9건 통과 → 후 전건 기대대로(과차단 0: 조회·복원·루트밖 6종 allow 유지), 회귀 테스트 7건 |
| SEC-68 | HIGH | 06 | **위치가 경로임을 말해 주는 자리에서 경로 판별을 요구해 생긴 구멍** — `cp -r /tmp/x src` 의 `src` 는 슬래시·확장자가 없어 대상에서 빠졌다(rubric 이 명시적으로 덮는다고 한 cp/mv 규칙의 구멍). 디렉토리 이름 하나로 소스 트리를 통째로 덮어쓸 수 있었다. `git clone <url> src` · `rsync` · `sponge` · 배치 편집기 · `tar -C` · `unzip -d` · `patch` · `git apply` 도 같은 라운드에서 닫음 | verified | measured | `core/src/bashwrite.ts:179` | `fixes-round3.md` · `evidence/round3-verify.log` §9 — 우회 10계열 **60/60 deny**, 의도적 허용 **18/18 allow**(과차단 0), 회귀 테스트 23건 |
| SEC-69 | BLOCKER | 06 | 🔴 **자기 무장해제** — 판정 정책 파일 `.harness/config.yaml` 이 훅에서 allow 였다(코어 보호는 state/events/ledger 3종뿐). 설계 트랙은 그 파일의 `design_allowed_prefixes` **allow-list** 로 판정하므로 빈 문자열 접두사 한 줄이면 모든 경로에 매치돼 **강제가 통째로 꺼졌다** | verified | measured | `core/src/hook.ts:180` | `evidence/round3-verify.log` §10 — 정책 파일(`config.yaml` + `.harness/profile/`)을 상태 파일과 같은 등급으로 올려 Write·셸 전 경로 deny. 재현 절차 재실행: 정책 쓰기 6종 전건 deny · **과차단 0**(design/packets/waves/docs/README 전건 allow) |
| SEC-70 | BLOCKER | 06 | 🔴 **구축·출하 트랙(P7~P12) 강제가 0** — 스펙 §4-2 매트릭스 2·3행 미구현. 정상 게이트 경로로 재측정: P8 에서 배포 명령·설계 문서 직접 수정 allow, P12 에서 신규 기능 코드 allow. 13페이즈 중 6페이즈에 강제 없음 | verified | measured | `core/src/hook.ts:724` | `evidence/round3-verify.log` §11 — P7~P9 배포 3종·설계 문서 수정 deny(backtrack 중이면 허용), P10~P12 신규 파일·미승인 배포 deny(기존 파일 수정·`.harness/` 산출물은 allow). **과차단 0**: 소스·테스트·npm test 는 구축 트랙에서 그대로 통과 |
| UX-71 | HIGH | 03 | **과차단이 광범위하고 사유 문구가 사실과 다르다** — 설계 트랙에서 `notes.txt` · `.gitignore` · `.env.example` · `assets/logo.svg` · `package.json` · `test/a.test.ts` 가 전부 deny 되고 사유는 "Source code cannot be written". 허용은 `.harness/` · `docs/` · 루트 `*.md` 뿐이라 **소스가 아닌 파일도 막힌다** — 사용자가 하네스를 끄게 만드는 방향 | verified | measured | `core/src/hook.ts:371` | allow-list 단일 규칙 → **deny-list 2단**(프로파일 `source_globs` → 소스 확장자 44종 바닥). 사유는 실제로 걸린 규칙을 말한다. **워크플로 산출은 그대로 머지하지 않았다** — 적대적 검증이 새 구멍 3건을 E2E 로 실증했고 메인에서 전부 닫은 뒤 들였다: ① **심링크 비대칭**(`config/settings.yaml -> src/app.ts` 로 실제로 소스를 덮어썼다 — 최종 판정이 `rel`·`realRel` 중 한쪽만 봤다) → 양쪽을 보고 걸린 경로를 사유에 표시. ② **테스트 디렉토리 예외가 은닉 통로**(`test/app.ts`·`tests/server.go`·`spec/engine.py` 전부 allow — 접두사 6글자로 강제가 꺼졌다) → 예외를 **파일 이름 규칙**으로 좁혔다(`*.test.*`·`*_test.*`·`test_*`·`conftest.py`·`FooTest.java`). ③ **안내가 새로 거짓**(「설정은 쓸 수 있다」인데 `next.config.js`·`vite.config.ts` 등 35종이 deny) → 설정 파일 예외(`*.config.*`·`*.conf.*`·`.*rc.js`·`gulpfile`)를 추가하고 SessionStart·deny 문구를 실제 규칙과 일치시켰다. 측정(P2): 구현 차단 **22/22** · 테스트 접두 구멍 **6/6 차단** · **과차단 0/44**(설정 16종·테스트 8종 포함) · 심링크 Write·Bash 양 표면 DENY(소스 아닌 대상은 ALLOW) · 706 → **791 tests green**. 대가로 이름 없는 보조 파일(`test/helpers/factory.ts`)은 막힌다 — **의도된 계약**으로 테스트에 고정했다. 전문 `evidence/round3-verify.log §16` |
| I18N-72 | HIGH | 04 | **i18n 이 절반이었다** — CLI·코어만 영문화했고 실제 배포되어 모델에게 지시를 내리는 계층(skills 11 · agents 5 · marketplace.json · plugin.json)에 **한글 15,569자**가 남아 있다. 비한국어 사용자에게 워크플로 지침 계층이 통째로 불가용 | verified | measured | `core/test/i18n-en-default.test.ts:199` | skills 11 · agents 5 · `.claude-plugin/*.json` 2 = **16개 파일 전량 영문화**(한글 15,569 → **0**, 남은 6자는 저자 이름 「장욱」 — 고유명사라 번역 대상이 아니다). 번역만 하지 않고 **낡은 서술을 실측으로 교정**했다: 「`harness` 엔 `--help` 가 없다」(UX-24 로 생겼다) · 「캔버스 URL 필드가 코어에 없다」(`harness design link` 가 있다) · 「게이트 피드백 수집 명령이 아직 없다」(FEAT-23 이 넣었다) · 설계 트랙 허용 목록 서술(UX-71 로 deny-list 2단이 됐다) · 옛 한국어 deny 문구 인용(현재 영문 문구로). **회귀 가드**를 `i18n-en-default.test.ts` 에 추가했다 — 목록이 아니라 `skills/`·`agents/`·`.claude-plugin/` **디렉토리 전체**를 훑으므로 새 스킬이 자동으로 사정권에 든다. 오염 주입(`agents/researcher.md` 에 한글 1줄)으로 **실제로 무는 것**을 확인했다(RED → 복구 후 GREEN). 796 tests green 전문 `evidence/round3-verify.log` §17 |
| FEAT-73 | MED | 01 | SessionStart 첫 지시가 **패키지에 없는 명령**을 가리킨다 — `remote_control` 기본 on 이라 매 세션 `/remote-control` 실행을 지시하는데 `commands/` 디렉토리 자체가 없다 | verified | measured | `core/src/hook.ts:503` | 번호 붙은 지시에서 빼고 **맨 뒤 조건부 안내 1줄**로 내렸다(「있으면 실행, 없으면 건너뛴다」 + 폴백 채널 + 끄는 법). 첫 지시는 활성 웨이브 이어받기. 기본값 on·요구 15 유지 — 훅은 슬래시 명령 존재를 알 수 없어 탐침은 거짓 음성을 낳는다. 스펙 `evidence/round3-verify.log` §13-6a 「첫 행동으로 지시」와 어긋나므로 스펙에 개정 블록 명시. 측정: en/ko 41/41 PASS, **과차단 0**(on→off 차이가 그 한 줄뿐 · 지시 번호 1부터 연속 · pre-tool deny 4/allow 6 불변 · `.harness/` 없으면 침묵), 회귀 테스트 7건 |
| OPS-74 | HIGH | 11 | **자기 채점의 구조적 한계가 실증됐다** — 라운드 3의 「우회 60/60 deny · 과차단 0」 보고는 *내가 막힐 거라 예상한 목록*만 담았다. 정책 파일 쓰기 · 설계 트랙 밖 페이즈 · 「막지 말아야 할 것을 막는지」가 목록에 없었다. 42 에이전트 독립 적대 감사가 BLOCKER 2건을 찾았다 | verified | measured | `agents/readiness-auditor.md:43` | 절차로 못 박았다 — 「더 열심히」로는 안 나오는 종류의 결함이라 **규칙**이 필요했다. ① 차단 측정은 **「막으면 안 되는 것」 목록과 짝**으로만 보고한다(한쪽만 있는 측정은 반쪽이다) ② 자기 목록이 구조적으로 빠뜨리는 **세 축을 강제**한다: 판정의 **입력**(정책·설정)을 쓰는 것 · 작업한 트랙 **밖**의 상태 · **반대 방향**(막지 말아야 할 것) ③ **구현자가 쓴 측정 목록은 근거가 아니다** — 검증자가 자기 목록을 쓴다 ④ 재현 실패가 곧 반증이 아니다(규칙 소스를 읽고 정확한 레버를 찾은 뒤 판정) ⑤ **중간 계층만 보고 판정 금지**(「훅이 allow」 ≠ 「잠금이 열렸다」 — 끝까지 실행해 상태 변화를 관측). 배포되는 `agents/readiness-auditor.md` 철칙 6~8 과 리포 자체 절차 `.claude/skills/verify/SKILL.md` 양쪽에 넣었다. **실효 실측(라운드 3-D)**: 워크플로 구현 4건에 이 절차를 적용해 **3건에서 구현자가 못 본 결함**을 잡았다 — UX-71 새 구멍 3건(심링크로 실제 소스 덮어쓰기 성공) · OPS-76 우회 2건(개행 1회·`node` 직접 실행으로 탐지 장치 정지) · SEC-75 과차단 2건. 나머지 1건(FEAT-73)은 「과장 없음」 확인. **검증 없이 머지했으면 넷 다 「닫힘」으로 대장에 올라갔을 것이다.** 전문 `evidence/round3-verify.log §18` |
| SEC-75 | HIGH | 06 | **게이트가 산출물의 내용·구별성을 전혀 검사하지 않는다** — 2바이트 파일 1장으로 `gate submit --evidence measured` + `approve` 가 rc=0 으로 통과하고, 같은 파일 하나로 12개 게이트를 전부 열 수 있다. 근거 등급 `measured` 도 자기 신고라 검증되지 않는다 | verified | measured | `core/src/gate.ts:162` | 제출 시점 검사 3종: **최소 실질성**(공백 제외 80자 — 이 리포 실산출물 최소 213자·정당한 한국어 3문장 91자 아래, 공격의 40배 위 / 바이트가 아니라 **문자**로, 파일별이 아니라 **집합 전체**로), **구별성**(경로가 아니라 **내용** 다이제스트 — `cp a.md b.md` 를 실측으로 잡아 경로 기반을 폐기), **페이즈 적합성**(레지스트리가 아는 경로는 제 페이즈에서만). **워크플로 산출을 그대로 받지 않았다** — 검증자가 잡은 과차단 2건(부분 등록 상태에서 미등록 P6 리포트+등록 P0~P5 동반이 차단)을 메인에서 재현해 `assertPhaseFit` 을 고쳤다: **미등록 경로가 하나라도 섞이면 면제**, 남는 차단은 「제출 전부가 등록됐고 이 페이즈가 하나도 없는 경우」뿐. 측정: 과차단 2건 → 전건 ALLOW · 잡으려던 케이스는 그대로 DENY · 2바이트 1장 DENY(전: 13/13 통과, `ship verdict` GO) · 뮤테이션 9/9 RED · 724 → **770 tests green**. **닫히지 않은 부류는 [SEC-79] 로 분리했다** — 「한 글자 덧붙이기」가 아니라 실 산출물 0장으로도 열린다는 것이 실측됐고, 코드 머리말의 경계 서술을 그 실측으로 정정했다. 전문 `evidence/round3-verify.log §15` |
| OPS-76 | HIGH | 11 | **정책 변경이 탐지되지 않는다** — [SEC-69] 로 에이전트 경로는 닫았지만, 사람이 터미널에서 `config.yaml` 을 직접 고치면(의도된 탈출구) `doctor` 는 여전히 exit 0 · issues 0 이다. 정책이 바뀐 사실이 어디에도 남지 않아, 리뷰어가 「왜 이 프로젝트는 설계 트랙에서 소스를 쓸 수 있나」를 알 방법이 없다 | verified | measured | `core/src/policy.ts:97` | 정책 파일 집합(`config.yaml` + `.harness/profile/**`)의 SHA-256 을 저널에 고정(`policy-pinned`)하고 doctor 가 대조한다 — 드리프트는 **warning**(issue 아님: 재생으로 못 고치고, 정당할 수 있다). 정당한 정산은 `doctor --accept-policy`. **워크플로 산출을 그대로 받지 않고 메인에서 다시 쟀다** — 검증자가 짚은 대로 그 잠금이 `HARNESS_CMD_RE`(자기호출 제외용)를 쓰고 있어 개행·`env`·`sudo`·`bash -c` 로 풀렸다 → SEC-78 의 **탐지 전용** 패턴 + `(?![\w-])` 경계로 교체. 훅을 타지 않는 `node core/dist/cli.js doctor --accept-policy` 도 E2E 로 통했으므로 **CLI 2차 게이트 `HARNESS_ACCEPT_POLICY=1` 을 추가**했다(SHIP-52 의 `HARNESS_ALLOW_FORCE` 와 같은 형태 — 한 겹만으로는 방어 심층이 아니다). 측정: 우회 12종 전건 deny · 과차단 0/8 · node 직접 실행 exit 1(경고 유지) · env 켜면 exit 0(경고 정산) · 732 tests green. **의도된 과차단**: 하네스 명령과 함께 인용부호 안에 그 명령을 적으면 막힌다(`--force` 잠금이 이미 치르는 값 — 좁히면 `bash -c` 가 열린다). 전문 `evidence/round3-verify.log §14` |
| SEC-77 | HIGH | 06 | 스펙 §4-2 **1행** 「P0~P6 빌드·배포 명령 차단」이 반쪽 — 배포 목록이 리터럴 5개뿐이라 `npm publish`·`terraform apply`·`wrangler deploy` 등 최빈 배포 명령이 통과했고, **빌드 명령 차단은 아예 없었다** | verified | measured | `core/src/config.ts:13` | `evidence/round3-verify.log` §11 — 배포 목록을 계열별 21종으로 넓히고(컨테이너·PaaS·레지스트리·인프라) 프로파일이 정의한 `build` 명령을 설계 트랙에서 차단. config·번들 프로파일·코드 내장 바닥값 **3곳 동기화**(기존 테스트가 불일치를 잡았다). 과차단 0 |
| SEC-78 | HIGH | 06 | **자기호출 인식 정규식이 개행·접두 명령에 뚫린다** — `cd /tmp` 뒤 개행, `env`/`sudo`/`nohup`/`time` 접두, 백틱·`$()`·`bash -c` 안에서 `phase set --force` 가 훅을 통과했다(9종 중 8종). 실제 페이즈 변경은 CLI 의 `HARNESS_ALLOW_FORCE` env 게이트가 2차로 막아 **열리지는 않았으나**, 방어 심층의 한 겹이 이미 뚫린 상태였다 | verified | measured | `core/src/hook.ts:113` | `evidence/round3-verify.log` §12 — 탐지 전용 패턴을 stop 가드용과 **분리**(두 용도는 틀려야 안전한 방향이 반대다). 11종 전건 deny · 과차단 0(`git commit -m "harness"` 등 6종 allow 유지). 워크플로 적대적 검증자가 발견 |
| SEC-79 | MED | 06 | **실 산출물 0장으로 13게이트가 열린다** — [SEC-75] 가 2바이트 1장을 닫았지만, 서로 다른 **필러 파일 13장**(각 80자, 총 1KB)이면 `gate submit` 13건 전건 통과 → 승인 시 `ship verdict` **GO** · `doctor` issues 0. 비용이 40배 올랐을 뿐 부류는 그대로다. 최저가 경로가 SEC-75 가 **과차단 가드로 고정한 조합의 상위집합**이라 「막으면 안 되는 것」과 우회로가 같은 메커니즘을 쓴다 | verified | measured | `core/src/gate.ts:374` | 세 갈래로 닫았다. **① 산문이 아닌 것 거부**(고유 글자·낱말이 **동시에** 바닥일 때만 — 하나만 보면 숫자 표나 띄어쓰기 없는 일본어·중국어가 걸린다). **② 새 텍스트의 절대량**: 이미 심사된 게이트들이 보지 않은 조각이 최소치(80자)만큼 있어야 한다. 처음엔 유사도 비율(양방향 0.9)로 막았는데 **희석 경로**가 남았다 — 진짜 문서 한 장에 35자짜리 얇은 파일을 하나씩 덧붙이자 게이트 5개가 열렸다. 임계가 있으면 그 아래로 지나가는 길도 있다. 절대량으로 바꾸자 상위집합 특례도 필요 없어졌다(동반 제출은 새 조각이 실제로 많아 통과한다). **③ 리뷰 패킷이 제출물을 보여 준다** — 급소는 검사가 약한 것이 아니라 **승인 직전 사람이 읽는 문서가 실제 제출물을 아예 안 실었다**는 것이었다(패킷은 레지스트리 등록 문서만 실었다). 이제 제출 경로별 실질 문자·고유 글자·낱말과 「최소치 근처」 알림, 읽을 수 없는 제출물 blocker 를 싣는다. 측정: 필러 13장 **13→0** · 한 글자 사본 13장 **13→1** · 희석 사다리 **13→2** · 정상 13페이즈 전 생애주기 **13/13 개통·승인 + GO + doctor 0**(과차단 0) · 회귀 21건. **남는 것**: 게이트마다 진짜로 새 80자를 쓰면 열린다 — 그 80자가 **좋은가**는 순수·로컬·결정적 코어 밖이고 사람 승인이 방어다(§4-3). 4개 언어 「알려진 한계」에 그렇게 적었다 |
| API-80 | LOW | 02 | **도움말이 광고하는 인자 형태가 먹지 않는다** — `harness doc url` 도움말은 `--url <주소>` 를 적는데 구현은 위치인자만 받아서, 도움말대로 친 사람이 「artifact URL 이 https 가 아니다: "--url"」로 exit 1 을 본다. API-30 과 같은 계열(CLI·MCP·도움말 간 인자 불일치) | verified | measured | `core/src/cli.ts:1124` | — I18N-72 중 P0 스킬 절차를 실측하다 발견. API-30 과 **같은 처방으로 둘 다 받게** 했다(도움말과 구현이 갈리면 고칠 곳은 「사람이 친 것이 먹게 만드는 것」이다) + 도움말을 실제 형태로 정정. 측정: 위치인자·`--url` 양쪽 exit 0 · URL 없으면 exit 1(침묵 성공 없음) · 회귀 4건 |
| USE-81 | MED | 04 | **공개 README 4개 언어 전부가 있는 기능을 「없다」고 적는다** — 「`--help` 는 없다; 이 표가 레퍼런스다」인데 [UX-24] 로 `--help` 가 생겼고 전 명령군에서 동작한다. [FEAT-23](README 가 없는 기능을 광고)의 **반대 방향**이며, 사용자는 있는 진입점을 못 찾는다 | verified | measured | `README.md:187` | — 재감정 감정자(효용성 축)가 `README.ja.md` 에서 처음 짚었고 4개 언어 전건 재현. 문장을 실제 동작(`harness --help` · `harness <군> --help`)으로 교체하고, `doctor` 행이 4개 언어 모두 `--accept-policy` 를 빠뜨린 것도 함께 채웠다(번역본만 낡는 것이 이 리포의 반복 실패 모드다). 측정: 문서(README 4 + skills 11 + agents 5)에서 `harness ...` 호출 **45조합 추출 → MISSING 0** |
| USE-82 | MED | 04 | **`backtrack` 이 사유 없이 성공한다** — `harness backtrack P0` 가 exit 0 으로 `reason: "(미기재)"` 를 상태·저널에 기록한다. 「침묵 성공 0」 위반이자 **영문 기본 출력에 한국어가 박히는 경로**(i18n 가드의 명령 목록에 없어서 안 잡혔다) | verified | measured | `core/src/cli.ts:1244` | — 독립 재감정(사용성 축) 발견, 메인에서 재현. `--reason` 을 필수로 하고 빈 문자열·공백도 거부. 역행은 승인된 설계로 되돌아가는 결정이라 「왜」가 없으면 나중에 재구성할 수 없다. 측정: 사유 없음 exit 1 + **상태 파일 바이트 불변** · 공백만 exit 1 · 사유 있으면 기록 · **영문 출력 한국어 0**. 회귀 3건(`cli-contract.test.ts`) |
| API-83 | LOW | 02 | `ship deploy` 도움말이 **필수 `--sha` 를 광고하지 않는다** — 광고된 형태 그대로 치면 exit 1. [API-80]과 같은 부류의 재발 | verified | measured | `core/src/help.ts:162` | — 도움말을 실제 필수 인자로 정정. 「도움말대로 쳤는데 실패한다」는 사용자가 자기 실수라고 오해하는 종류의 결함이다 |
| API-84 | MED | 02 | **같은 명령군 안에서 인자 형태가 갈린다** — `ship defect add` 는 `--id` 만, `update` 는 위치인자만 받는다(`update --id X` → 「No such defect id: --id」). [API-30]·[API-80]과 같은 뿌리 | verified | measured | `core/src/cli.ts:468` | — 양쪽 다 받게 했다(API-30 관례). 측정: add 위치인자·`--id` 각각 exit 0 · update 위치인자·`--id` 각각 exit 0 · 회귀 2건 |
| UX-85 | MED | 04 | **미초기화 가드가 명령마다 갈린다** — `status` 만 「run `harness init`」 을 안내하고 `wave create`·`gate status`·`phase set`·`node upsert`·`ship verdict`·`loop next` 는 **내부 tmp 경로가 박힌 raw ENOENT** 를 뱉는다. `loop next` 는 exit 0 으로 `wave create` 를 권하는데 그대로 치면 ENOENT — **안내가 실행 불가능** | verified | measured | `core/src/cli.ts:312` | — 가드를 **한 곳으로** 모았다(명령마다 복제하면 새 명령이 생길 때마다 빠진다 — SEC-50 이 그 사고였다). **아는 명령에만** 걸어 오타는 UX-24 계약대로 「알 수 없는 명령」이 먼저다. 측정: 10개 명령 전건 init 안내 · ENOENT 0 · **내부 경로 노출 0** · 미지 명령은 init 안내가 아님 · `--help`/`--version` 은 init 전에도 exit 0. 회귀 12건 |
| UX-86 | LOW | 04 | **필수 인자를 빼면 `undefined` 가 오류문에 박힌다** — `adr show` → 「No such ADR: undefined」, `wave activate` → 「No instruction sheet for wave undefined」. 사람이 「내가 등록을 안 했나」로 **오진**한다. 오타에 가까운 명령 제안도 없다 | verified | measured | `core/src/cli.ts:163` | — 값이 흘러들기 전에 잡는 `req()` 를 도입하고 6개 경로에 적용, 오타에는 편집거리 기반 제안을 붙였다(임계 = 이름 길이/3, 넘으면 제안 안 함 — **틀린 제안은 제안 없음보다 나쁘다**). 측정: 6종 전건 `undefined` 0 + 사용법 문자열 · `stauts`→`status`·`wav`→`wave` 제안 · `xyzzy` 무제안. 회귀 7건 |
| LOGIC-87 | LOW | 08 | **`node upsert --parent` 가 원장에 없는 부모를 무경고 수용**한다 — `wave create --refs` 는 검증하는데 여기만 안 해서 댕글링 부모가 조용히 생기고 RTM 사슬이 끊긴다 | verified | measured | `core/src/ledger.ts:44` | — `--refs` 와 같은 계약으로 검증하고 자기 자신을 부모로 두는 것도 막았다. 측정: 없는 부모 exit 1 + 등록 방법 안내 · 자기참조 exit 1 · 있는 부모/부모 없음 exit 0. 회귀 3건 |
| SPEC-88 | LOW | 09 | **기본 프로파일에서는 빌드 차단이 무효다** — `generic` 의 `commands.yaml` 이 `build: ''` 라 `commandFor` 가 undefined 를 주고 설계 트랙에서 `npm run build`·`make`·`tsc` 가 ALLOW. 배포 차단(코어 내장 22종)은 유효하나, [SEC-77] 문구를 읽은 사람이 「빌드도 막힌다」로 과대평가한다 | verified | measured | `profiles/generic/commands.yaml:10` | — **고치지 않고 문구를 정확히 했다.** 빈 값 = 「지어내지 말고 사람에게 물어라」는 의도된 설계이고(모르는 스택의 빌드 명령을 박으면 P7 「CI 그린」이 「실행된 적 없는 명령이 실패하지 않았다」로 통과한다), 설계 트랙에는 애초에 새 구현 코드가 없어 빌드 차단의 보안 가치가 낮은 반면 과차단 위험은 실재한다. 프로파일 주석에 강제 결과를 명시. 측정: deploy 계열 DENY · `npm run build`/`make`/`tsc` ALLOW(문서화된 한계) |
| OPS-89 | MED | 11 | **판정 블록과 대장 집계가 갈린다 — 세 번째 재발** — 대장 행을 고치고 `00-summary.md` 판정 블록을 안 고쳐 「verified 58 · open 6」 대 실제가 어긋났다. 두 번은 피어가, 세 번째는 독립 감정자가 잡았다. **사람이 매번 손으로 세는 구조**가 원인 | verified | measured | `core/test/ledger-summary-sync.test.ts:104` | — 손으로 고치지 않고 **테스트로 고정**했다([OPS-64]와 같은 처방). 대장 헤더 집계 대 실제 행 · 판정 블록의 verified/open 수 · open 항목 전건 나열 · **닫힌 항목이 open 표에 남아 있는지**를 검사한다. 커밋 해시는 검사하지 않는다 — 커밋마다 필연적으로 낡고 그 사실은 정보가 아니다. 도입 즉시 실제 드리프트(58 대 59)를 잡아 RED 로 시작했다. 회귀 4건 |
| SEC-90 | BLOCKER | 06 | 🔴 **접두 명령 하나로 쓰기 스캐너가 통째로 꺼진다** — `sudo tee src/app.ts` · `nohup cp /tmp/x .harness/state.json` · `time sed -i "" s/a/b/ .harness/config.yaml` 이 **전 페이즈에서 ALLOW**. `commandName` 이 벗기는 접두가 env 대입뿐이라 명령 이름이 `sudo`/`nohup`/`time`/`env`/`command`/`nice`/`exec`/`doas` 로 잡혀 `tee`·`cp`·`sed -i` 규칙을 아예 안 탔다. **소스만이 아니라 코어·정책 파일까지** 열려 SEC-49/50/69 가 닫은 표적이 접두어 한 조각으로 재개방됐다 | verified | measured | `core/src/bashwrite.ts:70` | — 독립 재감정(실효성 축) 발견, 메인 전건 재현. **에이전트 레인**이다(사용자가 터미널에서 치는 것은 의도된 탈출구라 결함이 아니다). 리다이렉트 형태는 원문 스캔이 따로 잡아 계속 DENY 였고, 그래서 **명령 계열에서만 조용히 성립**해 리다이렉트만 재는 목록으로는 안 보였다([OPS-74] 가 말한 축). 고침: `PREFIX_COMMANDS` 와 그 플래그·값(`sudo -u me`·`nice -n 10`·`timeout 5`)까지 벗기고 진짜 명령 이름을 잡는다. 측정: 접두 12종 × 표적 5종 **60/60 DENY** · **과차단 0/15**(`sudo ls`·`time npm test`·증적 쓰기 유지) · 회귀 2건 |
| SEC-91 | HIGH | 06 | **디렉토리를 대상으로 준 쓰기가 판정을 빠져나간다** — `mv /tmp/x src` · `cp -r /tmp/x .harness` · `tar -C src` · `unzip -d src` · `git clone <url> src` 가 ALLOW. 스캐너는 대상을 정확히 뽑아 왔는데 **판정이 파일 이름 일치만** 봐서 `.harness` ≠ `.harness/state.json`, `src` ∉ `src/**` 로 빠졌다. `find -exec sed -i` 도 안쪽 명령을 안 봤다. 생성물을 만들어 `mv` 로 제자리에 옮기는 것은 **모델이 자연히 가는 경로**다 | verified | measured | `core/src/profile.ts:484` | — 보호 경로가 대상 **아래에 있으면** 같은 판정을 주도록 `coversPath` 를 두고, 소스 글롭은 리터럴 접두로 `isSourceTree` 를 새로 만들었다. `find -exec` 는 xargs 와 같은 처방(안쪽 명령을 다시 판정). 측정: 우회 17종 **전건 DENY** · **과차단 0/11**(`cp -r /tmp/dir docs`·증적 디렉토리·읽기 전용 `find` 유지) · **페이즈 계약 불변**(P8 에서 `mv /tmp/x src` ALLOW, `.harness` 는 계속 DENY) · 회귀 3건 |
| SEC-92 | BLOCKER | 06 | 🔴 **에이전트가 쓴 스크립트를 실행하면 훅이 그 안을 못 본다** — 설계 트랙에서 `.sh` 는 정당하게 허용되므로(셋업 스크립트는 구현이 아니다) `run.sh` 를 쓰고 `sh run.sh` 로 실행하면 훅에는 한 줄만 보인다. **P0 → P7 이 실제로 열렸다**(`Phase → P7 (--force: gate check skipped)`) — 요구 2(승인 전 구현 금지)를 직접 깬다. `doctor --accept-policy`·소스 쓰기·디렉토리 이동도 같은 방식으로 전부 통과했다 | verified | measured | `core/src/hook.ts:638` | — 독립 재감정(실효성 축)이 짚고 메인에서 **끝까지 실행해** 확인(훅 allow 가 아니라 상태가 실제로 바뀌는 것을 봤다). 훅은 파일시스템을 볼 수 있으므로 **실행되는 스크립트를 읽어 같은 규칙 한 벌로** 판정한다. 경계를 좁게 잡았다: 루트 **안** 파일만 · 64KB 상한 · **깊이 1**(사슬을 끝까지 따라가면 순환·비용 문제가 생기고, 한 겹만 봐도 「쓰고 바로 실행」이라는 실제 경로는 닫힌다) · 읽기 실패는 무시. 측정: 래퍼 우회 **9/9 DENY**(`sh`·`bash`·`zsh`·`./`·`source`·`.`) · **과차단 0/8**(정상 스크립트·`npm run build` 스크립트·`cat`·**루트 밖 스크립트**·없는 파일 유지) · 비용 p50 63ms→63ms · p95 66→68ms(스크립트 읽어도 차이 없음) · 회귀 3건 |
| API-92 | MED | 02 | **웨이브 목표 필수가 CLI 에만 있었다** — MCP `harness_wave_create` 는 `goal ?? (미지정)` 으로 **목표 없는 빈 껍데기 웨이브**를 만든다. [API-29] 가 CLI 에서 막은 침묵 성공이 표면 하나로 부활 | verified | measured | `core/src/wave.ts:152` | — 독립 재감정(엔지니어링 축) 발견, 메인 재현(`callTool(root,'harness_wave_create',{})` → `wave-001` 생성). **규칙을 CLI 가 아니라 도메인(`createWave`)으로 내려** 두 표면이 함께 상속하게 했다. 측정: MCP 목표 없음 거부 + 파일 미생성 · 목표 있으면 양쪽 생성 · 회귀 2건 |
| LOGIC-93 | MED | 08 | **원장 부모 검증이 CLI 에만 있었다** — MCP `harness_node_upsert` 로 `parent: NOPE-9`·자기 자신 부모가 **원장에 그대로 기록**된다([LOGIC-87] 이 CLI 에서 막은 것). SEC-50(Write 만 막고 Bash 는 비어 있음)과 같은 **표면 비대칭** | verified | measured | `core/src/ledger.ts:44` | — 메인 재현: `ledger.yaml` 에 `parent: NOPE-9` 가 실제로 쓰였다. **검증을 도메인(`upsertNode`)으로 내렸다** — 표면이 몇 개든 함께 상속한다. 측정: MCP·CLI 양쪽 거부(같은 입력 같은 판정) · 있는 부모는 양쪽 통과(과차단 0) · 회귀 4건 |
| LOGIC-94 | MED | 08 | **접두 명령 목록이 두 벌이고 이미 갈려 있었다** — `bashwrite.ts` 16종 대 `hook.ts` 인라인 9종. `timeout 30 harness wave update "x"`·`stdbuf`·`setsid`·`ionice`·`unbuffer` 가 **자기호출로 인식되지 않아** 정산한 턴이 활동으로 집계되고 **stop 가드가 정산 직후 다시 차단**했다(로그 → 활동 → 또 로그 요구 루프) | verified | measured | `core/src/hook.ts:71` | — 목록을 `bashwrite.ts` 한 곳으로 모으고 정규식을 거기서 생성한다. 접두 명령의 플래그·값(`timeout 30`·`sudo -u me`)도 함께 건너뛴다. **넓히다 오판을 만들었다가 되돌린 기록**: 맨 단어까지 건너뛰게 했더니 `time make harness`·`sudo apt-get install harness` 가 자기호출로 잡혔다 — 이 패턴은 **좁게 틀려야 안전**하다(넓히면 진짜 작업 턴이 활동에서 빠져 정산 강제가 조용히 풀린다). 측정: 자기호출 인식 **15/15** · 작업 턴 오판 **0/12** · 회귀 3건 |
| LOGIC-95 | LOW | 08 | **설계 문서 보호가 두 벌이고 한 벌은 죽어 있었다** — 같은 조건을 `judgeWritePath` 와 `preTool` 이 각각 다른 문구로 검사하는데 앞쪽이 항상 먼저 반환해 뒤쪽은 **도달 불가**. 「고쳤는데 안 고쳐지는」 함정이다 | verified | measured | `core/src/hook.ts:1279` | — 죽은 두 번째 벌을 지우고 규칙을 `judgeWritePath` 한 곳에 뒀다. 측정: 제거 후에도 P8 에서 Write·Edit·MultiEdit·Bash **4표면 전건 DENY**(보호 불변) · 회귀 1건 |
| SEC-96 | HIGH | 06 | **잠금이 「이름」으로 판정해서 코어를 직접 부르면 뚫린다** — `node <경로>/core/dist/cli.js doctor --accept-policy` 가 훅을 통과해 **정책 드리프트 경고가 1 → 0 으로 사라졌다**(탐지 장치 정지, E2E). `--force` 쪽이 우연히 막힌 것은 그 가드에만 `HARNESS_ALLOW_FORCE` 리터럴 절이 있어서였지 형태를 인식해서가 아니다 | verified | measured | `core/src/hook.ts:125` | — 독립 재감정(효용성 축)이 짚고 메인에서 **끝까지 실행해** 확인(경고 1→0). `CORE_INVOKE_RE` 로 `node|npx|bun|deno … core/dist/{cli,mcp}.js` 를 harness 호출로 인식하고, accept-policy 가드에도 env 리터럴 절을 대칭으로 넣었다. **잠금은 「무엇을 실행하는가」로 판정해야 한다 — 어떤 이름으로 부르는지가 아니라.** 측정: 우회 9/9 DENY · **과차단 0/9**(`node cli.js status`·`doctor`·`--repair`·`node build.js` 유지) · 회귀 2건 |
| OPS-94 | MED | 11 | **`state.json` 이 사라지면 복구가 막다른 길이 된다** — 모든 명령이 「`harness init` 을 먼저 실행하라」고 하고 `init` 은 「이미 있다」로 거부한다. 저널은 멀쩡하고 `doctor --repair` 가 재생할 수 있는데 **그 명령까지 같은 가드에 막혔다**. 안내문이 실행 불가능해진다 | verified | measured | `core/src/state.ts:35` | — 독립 재감정(가성비 축) 발견. **내가 [UX-85] 로 넣은 중앙 가드가 원인**이다(`isInitialized` = state.json 존재를 「초기화됐는가」로 씀). 질문 둘을 분리했다: `hasHarness`(.harness/ 가 있는가 — 가드가 본다) 대 `isInitialized`(state.json 이 성한가 — 복구가 본다). 측정: 삭제 후 `doctor --repair` exit 0 + state.json 재생 · `status`·`wave list` 복귀 · `.harness/` 자체가 없으면 init 안내 그대로 · 회귀 3건 |
| SEC-97 | HIGH | 06 | **래퍼를 씌우면 열린다 — 세 갈래** ① `sh -c "cp /tmp/x src/app.ts"`(가장 기본 래퍼인데 안쪽을 안 봤다) ② `git checkout -- src`·`git restore`·`git stash pop`(「되돌리기」라 쓰기로 안 보이지만 결과는 덮어쓰기) ③ **스크립트 깊이 2**(`a.sh` → `b.sh` 로 잠금이 통째로 풀려 P0→P7 실증). 리다이렉트 형태(`bash -c "echo x > src/app.ts"`)만 우연히 막혀 있어 **명령 계열에서만 조용히 성립**했다 — [SEC-90]과 같은 모양 | verified | measured | `core/src/bashwrite.ts:302` | — 실효성 축 **재측정**(BLOCKER 3건을 닫은 뒤 다시 돌린 것)이 찾았다. 처방은 xargs·find 와 같다: 감싸인 것을 꺼내 **같은 스캐너로 다시 판정**한다. 스크립트는 깊이 **3** 까지 따라가고(순환은 seen, 비용은 64KB 상한), `npm run <script>` 는 `package.json` 정의를 읽어 판정한다. 측정: 우회 **15/15 DENY** · **과차단 0/14**(`bash -c "npm test"`·`git status/add/commit`·`git checkout -- docs`·`npm run test` 유지) · 회귀 5건. **남긴 한계**: `make <target>` 은 Makefile 문법 해석이 범위 밖이라 열려 있다 · 스크립트 깊이 4 이상도 열려 있다 — 둘 다 README 「알려진 한계」에 적었다 |
| PROD-A | MED | 07 | **README 4개 언어가 이미 출하된 기능을 「🔜 예정」으로 광고한다** — 승인 게이트·설계 트랙 스킬·디자인 서브시스템·구축/출하 트랙 4줄이 전부 실재하고, `phase set` 은 「v0 임시」라 적혔지만 실제로는 게이트 승인 없이 못 넘어간다. 「198 tests」(실측 850) · 「~0 토큰」(실측 ~240)도 낡음 | verified | measured | `README.md:119` | — 독립 재감정(상품성 축) 발견. [USE-81]과 **같은 부류의 확장**(있는 기능을 부정)이며 이번엔 로드맵 절 전체였다. 4개 언어를 **함께** 고쳤다(번역본만 낡는 것이 이 리포의 반복 실패 모드다): 🔜 4줄 → ✅ + 실제 명령 이름, `phase set` 서술 정정, 숫자 재측정 반영, **「알려진 한계」 절 신설**(readiness 스킬 미동봉 · 레이아웃 템플릿 미강제 · `/remote-control` 미제공 · P7~P9 스킬 부재 · 필러 문서 · 저널 손편집은 위협 모델 밖). 측정: 4개 언어 `🔜` 0건 · `198` 0건 |
| UTIL-A | MED | 01 | **문서가 시킨 대로 하면 프로파일 오버라이드가 안 먹는다** — `commands.yaml` 헤더는 「이 파일을 `.harness/profile/` 로 복사하라」고 하는데 `profile.yaml` 이 없으면 디렉토리 전체를 건너뛴다. 채운 값이 조용히 무시되고 `profile cmd deploy` 는 다시 「commands.yaml 을 채우라」고 답해 **순환**한다. P11 배포 스킬이 이 명령에 의존 | verified | measured | `core/src/profile.ts:254` | — 독립 재감정(효용성 축) 발견. 명령 매핑만 덮어쓰는 것은 정당한 사용법이므로 `commands.yaml` 만 있으면 최소 프로파일로 이어 간다(나머지 필드는 번들 프로파일 상속). 측정: 문서대로 복사 → `profile cmd deploy` = `vercel deploy --prod` · 둘 다 없으면 기존대로 generic 폴백(조용한 오작동 없음) · 회귀 2건 |
| UTIL-B | MED | 01 | **토큰 문서의 입력 스키마가 어디에도 없다** — P4 스킬은 `design-tokens.json` 이 단일 원천이라며 `tokens gen` 을 시키는데 형태(schemaVersion·color 의 light/dark·type/motion 하위 그룹)는 스킬·README·`--help` 어디에도 없었다. 코어는 기본값을 발명하지 않으므로(§7) **첫 시도는 반드시 실패**하고, 검증기가 한 번에 한 항목씩 알려 주므로 연쇄 오류로 더듬어 올라가야 했다 | verified | measured | `core/src/tokens.ts:111` | 골격을 **코드에 단일 원천으로** 두고 오류문·`tokens --help`·P4 스킬이 같은 하나를 가리키게 했다 — 문서에만 적으면 스키마가 바뀔 때 예시가 조용히 거짓이 된다. 측정: `tokens --help` 의 예시를 그대로 복사해 넣어 **첫 시도에 `tokens gen` exit 0**(산출물 3개). 회귀 5건(예시 자체를 `validateTokens` 로 검증 · 도움말·스킬 사본 일치) |
| UTIL-D | LOW | 02 | **미지 CLI 플래그가 조용히 무시된다** — `node upsert --id F-1 --title t --titel oops` 가 exit 0 으로 통과해 「요청과 다른 레코드」가 원장에 남고, 친 사람은 성공만 보므로 영영 모른다. 오타가 곧 잘못된 기록이 되는 경로 | verified | measured | `core/src/cli.ts:93` | 전체 합집합으로 판정한다(명령별 목록은 한 곳만 빠져도 곧 과차단이고 이 리포에서 과차단은 결함과 같은 무게다). 편집거리로 「그럼 무엇이었나」를 붙이고, 지원하지 않는 `--title=x` 형태도 함께 잡는다. 목록 드리프트는 **테스트가 cli.ts 를 파싱해** 막는다. 측정: 문서가 부르는 **241 호출 전건 과차단 0** · 오타·미지·`=` 전건 exit 1 · 값 자리의 `--force` 는 그대로 통과 |
| API-98 | MED | 02 | **MCP 도 같은 부류 — 스키마 밖 입력 키를 조용히 무시한다** — `harness_node_upsert({id,title,nosuchkey})` 가 ok=true 로 노드를 만든다. [UTIL-D] 의 대칭면이고 [LOGIC-93]·[API-92] 와 같은 뿌리(규칙이 표면마다 갈린다) | verified | measured | `core/src/mcp.ts:326` | 도구가 스스로 선언한 `inputSchema.properties` 로 판정 — 새 도구가 생겨도 목록이 갈리지 않는다. 측정: 미지 키·NO_ARGS 도구에 키 전건 ok=false + 원장 무변화 · 선언된 키만 쓰는 정상 호출 전건 통과 |
| OPS-99 | MED | 11 | **MCP 미초기화 안내가 `state.json` 기준이라 거짓 안내 + 막다른 길** — `.harness/` 가 있는데 `state.json` 만 지우면 「이 프로젝트는 하네스가 관리하지 않는다 → `harness init` 하라」고 답한다. 둘 다 거짓이다(디렉토리는 있고 `init` 은 거부한다). CLI 가 [OPS-94] 로 닫은 함정이 **이 표면에 그대로 남아 있었다** | verified | measured | `core/src/mcp.ts:283` | 열화와 미초기화를 갈라 각각 다른 안내를 주고, 열화 쪽은 복구 수단(`harness_doctor` repair)을 가리킨다. **가드를 넣었으면 복구 경로가 그 가드에 걸리는지까지 잰다** — doctor→status 가 되살아나는 것을 E2E 로 측정 |
| UTIL-E | LOW | 02 | **`doc url` 이 안내문과 달리 아무 호스트나 받는다** — 안내는 「claude.ai 아티팩트 주소」라면서 `https://example.com/x`·`https://localhost/x` 가 exit 0 으로 원장에 기록된다. 이 필드의 용도는 사람이 원격에서 여는 발행본이라, 열 수 없는 URL 이 승인 경로까지 실려 가면 게이트가 보는 것과 사람이 보는 것이 갈린다 | verified | measured | `core/src/registry.ts:279` | claude.ai 와 그 서브도메인만 받는다(스킬 11장이 전부 그 형태를 지시한다). **CLI 가 아니라 도메인(registry)에서** 고쳐 표면이 몇 개든 함께 상속한다. 측정: 정상 URL·서브도메인 통과 · example.com·localhost·`claude.ai.evil.test` 거부 + 원장 무변화 |
| PROD-D | LOW | 07 | **`gate feedback` 출력이 이중 불릿** — 저장 형식이 「한 코멘트 = 한 불릿」이라 `- ` 를 붙이는데, 리뷰 코멘트를 마크다운 목록째 붙여넣는 것이 가장 흔한 입력이라 `- - 코멘트` 가 기본값이었다. 사람이 읽는 개정 근거 문서라 표시가 곧 품질이다 | verified | measured | `core/src/gate.ts:515` | 입력쪽 불릿(`-`·`*`·`+`)을 벗겨 정규화. 도메인(gate)에서 고쳤다. 측정: 세 종류 불릿 전건 단일화 · 코멘트 수·내용 무손실 |
| PROD-B | LOW | 07 | dist-없음 폴백 메시지 2곳이 한국어 — 소스만 받은 사용자가 첫 화면으로 보는 문구이고 「영문 기본 출력」 원칙 위반 | verified | measured | `bin/harness:9` | `bin/harness`·`mcp/server.js` 영문화 |
| PROD-C | LOW | 07 | ⚠ **재현 실패 — 결함 아님.** 「`gate submit --paths` 에 없는 경로를 주면 「outside paths」(프로젝트 밖)로 오진한다」는 보고. 실측: 프로젝트 **안**의 없는 경로는 상대·절대 모두 정확한 문구(「Cannot read the artifact under review … check the path」)를 주고, 「outside paths」 는 **진짜 루트 밖**일 때만 나온다 | verified | measured | `core/src/gate.ts:345` | 재현 4형태(`docs/없음`·`/etc/없음`·`../없음`·디렉토리) 전건 확인. 감정 보고를 액면으로 받지 않는다는 절차가 값을 한 사례 |
| PERF-95 | LOW | 05 | **비간섭 프로젝트도 훅 기동비를 그대로 낸다** — `.harness/` 없는 프로젝트에서 내용·부작용·토큰은 0 이지만 Write/Bash 호출마다 node 가 두 번(pre+post) 떠 ~107ms 를 낸다(594KB 번들 require 포함) | verified | measured | `hooks/hooks.json:4` | 셔뱅 셸 래퍼 `bin/harness-hook` 이 판정을 sh 한 번으로 내린다. **hooks.json 문자열에 셸 가드를 박지 않았다** — 호스트가 훅 명령을 셸로 해석하는지에 방어 전체를 걸지 않기 위해서다. 조건은 코어의 루트 해석(`CLAUDE_PROJECT_DIR ?? cwd`, 부모 미탐색)과 **같은 판정**이고 그 동치를 테스트가 소스 대조로 고정한다. 측정: deny 동일 유지(env 없이 cwd 경로 포함) · 비간섭 stdout 0B·파일 0 · 지연 286.5 → 21.8 ms/회(**-92.4%**, n=40, 절대값은 부하·서브셸 포함이라 부풀려짐 — 의미 있는 것은 비율) |
| PERF-96 | LOW | 05 | **저널 압축·스냅샷 수단 부재** — 폴백 재생 비용이 저널 길이에 비례하는데 줄일 방법이 없다(+85B/턴로그) | deferred | measured | `README.md:242` | **의도적으로 만들지 않는다(영구 결정, 「알려진 한계」로 확정).** `events.jsonl` 은 감사 기록의 원천이라 그것을 다시 쓰는 명령은 **아무것도 지워서는 안 되는 자리의 삭제 원시연산**이 된다. 없어서 치르는 비용은 한계가 분명하다 — 10만 건(수십 년 분) 재생 p95 ≈ 101ms 이고 그나마 상태가 열화된 동안만이며 `doctor --repair` 로 끝난다. README 4개 언어 「알려진 한계」에 논거·실측과 함께 적었다 |
| SEC-A | BLOCKER | 06 | **`git apply` 로 저널을 위조해 사람 승인 없이 게이트가 열린다** — `echo >> .harness/events.jsonl` 은 막히는데 같은 일을 하는 패치 한 장은 통과했다(대상이 패치 **안**에 있어 경로 판정이 호출되지 않는다). 게다가 「패치를 적용한다」는 사실만으로 막는 규칙이 **설계 트랙에만** 걸려 있어 배포 게이트가 사는 출하 트랙이 비어 있었다. 실측: 위조 → `doctor --repair` → **P10 approved(위조 ts)** → `npm publish` **DENY→ALLOW** · `doctor` ok/0/0. [SEC-49] BLOCKER 가 다른 프리미티브로 부활한 것 | verified | measured | `core/src/hook.ts:714` | **감싸인 것을 꺼내 같은 스캐너로 다시** — 패치 파일 경로는 인자에 드러나 있으므로 읽어서 `+++/---` 대상을 꺼내고 **페이즈 무관**으로 같은 `judgeWritePath` 에 건다. 대상을 볼 수 없는 형태(stdin·파이프)는 거부하고 「패치를 파일로 넘겨라」를 안내. 리다이렉트 연산자를 패치 파일로 오인해 정당한 `git apply < ok.patch` 를 막던 과차단도 함께 고쳤다. 측정: 위조 6형태 전건 DENY · 전 페이즈(P0/P4/P7/P10/P12) DENY · 정상 패치는 구축 트랙 ALLOW 이고 **직접 쓰기와 문구까지 동일 판정** · `git status`·`git stash pop` 무영향 |
| UTIL-A1 | HIGH | 01 | **미래 게이트 선승인으로 설계 트랙 전체를 건너뛴다** — `canEnterPhase` 가 직전 하나만 봐서 P0 에서 `gate submit P6` → `gate approve P6` → `phase set P7` 세 명령이면 P1~P5 가 pending 인 채 지나간다. 제품이 `--force` 를 env 로 잠가 막으려던 일이 **그 잠금 아래로** 일어난다. 사고로도 발생(제출 실패한 페이즈를 건너뛴 채 출하) | verified | measured | `core/src/gate.ts:860` | 「그 페이즈 앞의 게이트가 전부 승인」으로 확대하고 빠진 게이트를 **전부 이름으로** 말한다. 사람의 탈출구(`--force`)는 그대로. 측정: 건너뛰기·중간 누락 차단 · **순차 13페이즈 생애주기 13/13 + GO**(과차단 0) |
| ENG-B | HIGH | 11 | **하네스 소유 정본 일부가 에이전트에게 열려 있다** — `ship/defects.yaml`·`ship/deployments.yaml`·`design/registry.yaml`·`.runtime/` 마커가 보호 밖. 결함 대장을 직접 고쳐 open blocker 를 `verified` 로 바꾸자 `ship verdict` 에서 사유가 사라지고 `doctor` 는 ok/0/0 — **저널엔 `status: open` 이 그대로인데 아무도 대조하지 않는다.** 사람이 승인 전에 보는 근거가 그 판정이므로 근거를 위조할 수 있으면 사람 게이트가 방어가 못 된다 | verified | measured | `core/src/hook.ts:151` | 보호 목록에 편입. 문서·증적(`design/*.md`·`evidence/`)은 **에이전트 산출물이라 그대로 허용**한다(막으면 설계 트랙이 안 돈다). 측정: 정본 6종 Write·Bash 전건 DENY · 산출물 4종 전건 ALLOW |
| ENG-A | MED | 03 | **`hooks.json` matcher 가 판정 대상 도구 집합의 고정되지 않은 두 번째 벌** — matcher 에서 `Bash` 를 지워도 918 tests green 이었다. 실배포에서는 그 순간 Bash 강제(리다이렉트·`--force`·배포 차단)가 통째로 조용히 꺼진다 | verified | measured | `core/test/surface-parity.test.ts:293` | `WRITE_TOOLS` 와 교차 고정. 오염 주입으로 무는 것 확인(matcher 에서 Bash 제거 → 2건 red) |
| COST-A | MED | 05 | **자기호출 판정 정규식이 ReDoS** — 접두 명령 연쇄가 길고 끝이 `harness` 가 아니면 backtracking 지수 폭발(래퍼 20개 253ms · 22개 1071ms · 25개 8270ms). 훅이 매 Bash 호출마다 돌리므로 **최악 입력의 상한이 사람이 기다리는 최대 지연** | verified | measured | `core/src/hook.ts:81` | 중첩 수량자를 버리고 선형 스캔으로. 측정: 래퍼 20·22·25·40개 전부 ~103ms(프로세스 기동비, 판정은 상수). **판정 내용 동일**이 진짜 조건이라 자기호출 9종·비자기호출 7종을 회귀로 고정(넓히면 정산 강제가 조용히 풀린다 — SEC-78) |
| UX-A1 | HIGH | 04 | **소환된 사람이 빠져나올 길이 없다 — 막다른 길 두 겹** — 안내 문구는 실재하지 않는 `harness loop clear` 를 가리키고(실제는 `loop critical clear`), 그 진짜 명령은 **어느 `--help` 에도 없다** | verified | measured | `core/src/help.ts:169` | 문구를 실재하는 명령으로 고치고 도움말에 해제 명령을 실었다 |
| UX-A2 | MED | 04 | **`ship defect add` 의 인자를 알아낼 방법이 없다** — 미지 플래그 오류가 `ship --help` 를 가리키는데 거기 플래그가 없다(같은 군의 `deploy` 는 이미 적고 있었다) | verified | measured | `core/src/help.ts:178` | add/update/list 를 인자까지 명기 · 「한 줄 요약이 비었다」 오류가 `--title` 을 말한다 |
| UX-A3 | MED | 04 | **`tokens lint` 가 읽지 못한 파일을 조용히 건너뛴다** — `tokens lint nofile.css` 가 「raw 값 없음」 exit 0 이라 **파일명 오타가 lint 통과로 위장**된다 | verified | measured | `core/src/cli.ts:989` | 읽지 못한 파일은 깨끗한 파일이 아니다 — 경로를 밝히며 exit 1 |
| UX-A4 | MED | 04 | **등록된 노드를 볼 방법이 없다** — `trace <미지 id>` 가 `report rtm` 을 가리키는데 rtm 은 F- 노드만 싣는다. UX-·FEAT- 노드는 어느 명령으로도 열람 불가(`node list` 부재) | verified | measured | `core/src/cli.ts:989` | `node list` 추가(doc·adr·wave·ship defect 는 이미 있었고 이 군만 없었다) + CLI·MCP 안내를 그쪽으로 |
| UTIL-A2 | MED | 04 | **`state.json` 만 없을 때 내부 절대경로가 박힌 raw ENOENT** — 이 상태는 미초기화가 아니라 **열화**이고 복구 수단이 실제로 있다 | verified | measured | `core/src/state.ts:48` | `readState` **한 곳**에서 고쳐 `doctor --repair` 를 가리키게 했다(명령마다 복제하면 새 명령이 생길 때 빠진다 — USE-93·OPS-94 가 그 사고). **그 복구 경로가 실제로 통하는지까지** 측정 |
| QUAL-A | MED | 10 | **출하 4.8 조건인 「대장 lint 통과」가 객관적으로 실패하고 있었다** — 심각도 어휘 `LOW-MED` 가 3행(직전 라운드에 추가된 행들) | verified | measured | `core/test/ledger-summary-sync.test.ts:156` | MED 로 정정하고, 더 중요하게 **어휘 검사를 리포 안으로 들였다** — 그 lint 는 리포 밖 스킬에 있고 리포 테스트는 어휘를 보지 않아 사전 밖 값이 918 green 을 통과했다. 밖의 검사에 기대는 조건은 안에서도 지켜야 한다. 오염 주입으로 무는 것 확인 |
| PROD-B1 | MED | 07 | **플러그인으로 깐 사람의 첫 명령이 실행되지 않는다** — 스킬·README·MCP 오류문구가 전부 맨 `harness init` 을 지시하는데 그 바이너리는 PATH 에 없고 위치 힌트가 어디에도 없다 | verified | measured | `skills/king-wjang-harness/SKILL.md:30` | 배선이 실제로 쓰는 경로(`${CLAUDE_PLUGIN_ROOT}/bin/harness`)와 PATH 추가 한 줄을 부트스트랩에 명기하고, 그 경로가 `hooks.json` 과 갈리지 않게 테스트로 고정 |
| PROD-B2 | MED | 07 | **이미 출하된 기능을 「로드맵」으로 광고** — design link/sync(diff→bump→STALE)·inventory·baseline·gate feedback 전부 실측 동작. [PROD-A] 와 같은 부류이고 방향도 같다(과소광고는 조용해서 오래 남는다) | verified | measured | `README.md:83` | 4개 언어에서 절 제목을 사실로 바꾸고, 진짜 미출하인 「캔버스 네트워크 pull」만 그 자리에 남겼다 |
| ENG-C | MED | 03 | 원장 상태 어휘 `LEDGER_STATUSES` 가 `cli.ts`·`mcp.ts` 두 벌 — 한쪽에 값을 더해도 다른 쪽은 모르고 무는 테스트가 없었다 | verified | measured | `core/src/types.ts:31` | `types.ts` 한 벌로. 어댑터가 목록을 다시 정의하지 않는 것을 소스 대조로 고정 |
| ENG-D | MED | 03 | 유령 참조 검증이 도메인이 아니라 **어댑터 두 벌** — 리포 자신의 원칙 위반이고 [LOGIC-93]·[API-92] 와 같은 형태(제3 호출면이 생기면 빠진다) | verified | measured | `core/src/wave.ts:138` | `createWave` 로 내리고 두 사본 삭제. 측정: 두 표면이 같은 문구로 거부 · 등록된 참조는 두 표면 통과(과차단 0) · 어댑터에 사본 부재를 소스 대조로 고정 |
| ENG-E | MED | 03 | `--parent ""` 를 **CLI 는 거부하고 MCP 는 받아** 원장에 빈 부모를 남긴다 — 원인은 CLI 에 부모 검증 두 번째 벌(USE-96)이 있던 것 | verified | measured | `core/src/ledger.ts:40` | 도메인 한 벌만 남기고 빈 값은 「부모 없음」으로 정규화해 **저장까지** 반영(검증만 정규화하면 원장에 그대로 남는다). 측정: 두 표면 판정·기록 동일 · 댕글링 부모는 둘 다 거부 |
| QUAL-B | LOW | 10 | 대장 lint(`ledger-lint.sh`)의 ID 패턴 `[A-Z]*-[0-9]*` 이 **글자 접미 ID 8행을 미검사** — 위반 1건이 그 사각에 숨어 있었다 | deferred | measured | `docs/release-readiness/2026-08-21/rubric.md:6` | **리포 밖**(사용자 스킬 `verifying-production-readiness/bin/ledger-lint.sh`)이라 여기서 고치지 않는다. 리포 안 어휘 가드(QUAL-A)가 같은 사각 없이 검사하므로 대장 자체는 방어된다 — 스킬 수정은 사용자 판단 |
| UTIL-A3 | LOW | 01 | `harness hook --help`·무인자 `harness hook` 이 무출력 exit 0 이고 `hook-errors.log` 에 unknown-hook-event 를 쌓아 `doctor` 경고로 누적된다 — README 의 「모든 군이 하위명령을 인쇄한다」와 어긋난다 | verified | measured | `core/src/cli.ts:157` | **사람의 질문과 배선 오타를 구분한다** — `harness hook` 무인자·`--help`·`-h`·`help` 는 이벤트 목록을 인쇄하고, 진짜 오타만 `hook-errors.log` 에 남는다. 도움말을 물어본 것이 `doctor` 경고가 되면 사람은 그 경고를 무시하기 시작한다 — `core/test/util-3i-args.test.ts:103` |
| UTIL-A4 | LOW | 01 | `loop critical raise` 가 성공 출력 뒤 **exit 2** — 비문서화라 `set -e` 스크립트·exit≠0 을 실패로 읽는 에이전트가 성공을 실패로 오독한다 | verified | measured | `core/src/loop.ts:392` | **exit 2 는 계약이라 값을 바꾸지 않고 출력에 적었다** — 「종료코드 2 는 실패가 아니라 사람을 소환했다는 뜻」과 해제 명령까지. 비문서화된 비영 종료코드는 `set -e` 스크립트와 「exit≠0 = 실패」로 읽는 에이전트에게 성공을 실패로 오독시킨다 — `core/test/util-3i-args.test.ts:127` |
| UTIL-A5 | LOW | 01 | 동봉 스펙 문서(`docs/superpowers/specs/…`)가 존재하지 않는 명령 표면(`/harness start`·`config`)을 광고 — 운영 표면(README·스킬)에는 없어 영향은 문서 안에 갇힌다 | verified | measured | `docs/superpowers/plans/2026-08-20-core-engine-v0.md:181` | 동봉 스펙 초안의 유령 명령(`/harness start`)을 실재하는 명령으로 바꾸고 그 사실을 문서에 남겼다 — 영향이 문서 안에 갇혀 있었지만, 스펙이 없는 표면을 광고하면 **다음 구현자가 그것을 계약으로 읽는다** — `docs/superpowers/specs/2026-08-20-king-harness-design.md:421` |
| UX-A6 | LOW | 04 | `tokens swap` 이 `--out` 없이 「1 token(s) changed」를 주장하나 아무것도 기록하지 않는다(드라이런 미표기) · `--out <dir>` 문서와 달리 단일 파일 생성 | verified | measured | `core/src/tokens.ts:111` | `--out` 없는 실행이 **드라이런임을 밝히고** 쓰는 법을 준다. `--out` 을 주면 실제로 그 파일이 생기는 것까지 확인한다 — 말과 결과가 같아야 한다 — `core/test/prod-3i-surfaces.test.ts:120` |
| UX-A7 | LOW | 04 | 인자 생략 시 `undefined` 누출 3곳 — `backtrack`·`gate submit`(Invalid phase: undefined)·`node bump`(Node undefined is not in the ledger). [UX-86] 이 6경로에서 닫은 부류의 잔재 | verified | measured | `core/src/cli.ts:244` | **같은 판정 아홉 벌을 한 벌(`requirePhase`)로 내렸다** — 목록이 아홉이면 언제나 하나를 빠뜨린다([UX-102] 와 같은 처방). 인자 생략 시 「어느 페이즈인가」로 묻고 유효 목록을 보여 준다. 페이즈를 받는 명령 **전부**를 전수 대조한다 — `core/test/util-3i-args.test.ts:50` |
| UX-A8 | LOW | 04 | 잡동사니 — 대소문자 비정규화(`p1`·`BLOCKER` 거부, 제안 없음) · `loop critical bogus` 가 「No pending escalation」으로 오도 | verified | measured | `core/src/cli.ts:244` | 대소문자를 정규화해 받는다(`p0`·`p7` 통과) — 대소문자는 사람의 실수지 다른 의도가 아니다. 진짜 틀린 값(`P99`)은 여전히 거부하고 유효 목록을 보여 준다 — `core/test/util-3i-args.test.ts:81` |
| QUAL-D | LOW | 10 | `gate feedback` 무인자 메시지가 「Invalid phase: undefined」 — UX-86 이 닫은 부류인데 이 경로가 목록에 없었다 | verified | measured | `core/src/cli.ts:244` | 같은 한 벌로 닫힌다 — `gate feedback` 도 이제 `undefined` 를 흘리지 않고 목록을 보여 준다. **이 경로가 [UX-86] 목록 밖이었다는 것이 요점**이라, 이름이 아니라 판정을 합쳤다 — `core/test/util-3i-args.test.ts:50` |
| QUAL-E | LOW | 10 | [UX-71] 의 「과차단 0/44」는 표본 내 결론 — `src/**` 아래 co-located 테스트(`src/foo.test.ts`)는 이름 예외가 먼저 걸려 표본에 없었다 | rejected | measured | `docs/release-readiness/2026-08-21/ledger.md:3` | **코드가 아니라 주장의 범위가 틀렸다.** 재 보니 `src/**` 아래 co-located 테스트는 실제로 막히고, 그것은 결함이 아니라 **의도된 절충**이다(이름 예외가 소스 트리 안에서도 살면 `src/app.test.ts` 한 이름으로 `src/**` 차단이 통째로 풀린다). 그 동작·거부문·소스 트리 밖 테스트 통과를 전부 못 박아 **다음 감정의 표본 안으로** 끌어들였다 — `core/test/cost-3i-residuals.test.ts:164` |
| PROD-B3 | LOW | 07 | `lang: ko`·`HARNESS_LANG` 런타임 언어 옵션이 README 4종(한국어판 포함)에 미기재 — 4개 언어 문서를 내는 제품이 그 옵션을 숨긴다 | verified | measured | `README.md:83` | [PROD-168] 과 **같은 것**이라 같은 처방으로 닫힌다 — 4개 언어 설정 절이 `lang` 키를 적고, `DEFAULT_CONFIG` 의 모든 키가 4개 언어에 있는지 전수 대조한다 — `core/test/doc-claims.test.ts:81` |
| PROD-B4 | LOW | 07 | MCP 서버(도구 16종)가 README 4종에 완전 미기재 — 숨은 가치다 | verified | measured | `README.md:83` | MCP 도구 절을 4개 언어에 신설했다 — 도구 이름과 함께 **의도적으로 못 하는 것 둘**(게이트 승인 불가 · 미승인 게이트를 지나 페이즈 이동 불가)을 적었다. 보증은 할 수 있는 것이 아니라 **못 하는 것**으로 이루어진다 — `core/test/prod-3i-surfaces.test.ts:56` |
| PROD-B5 | LOW | 07 | `--version` 이 「core v0」만 출력 — `plugin.json`·마켓플레이스의 0.0.1 과 표기가 어긋난다 | verified | measured | `bin/harness:9` | [PROD-126] 과 **같은 것**이라 같은 처방으로 닫힌다 — `--version` · `plugin.json` · CHANGELOG 세 표면의 버전이 이제 한 값이다 — `core/test/prod-3i-surfaces.test.ts:38` |
| PROD-B6 | LOW | 07 | 구판 감사 `docs/release-readiness/readiness.md`(판정 「출하 가능」·198 tests 시절)가 최신 「출하 불가」 감사와 나란히 남아 있다 | verified | measured | `docs/release-readiness/2026-08-21/00-summary.md:1` | 구판 감사를 **지우지 않고** 머리에 폐기 표시를 박았다 — 감사 기록은 지우는 것이 아니라 무엇이 언제 참이었는지를 남기는 것이다. 현재 판정의 위치(`00-summary.md`·출하 불가)를 함께 가리킨다. 배포본에는 export-ignore 로 애초에 안 실린다 — `core/test/prod-3i-surfaces.test.ts:68` |
| ENG-F | LOW | 03 | `mcp/server.js`(전송층 — 루트 해석·알림 억제·isError 매핑·stdout 위생)에 테스트 0건 — 이 파일의 어떤 뮤테이션도 생존한다 | verified | measured | `mcp/server.js:39` | [ENG-158] 과 **같은 것**이라 같은 처방으로 닫힌다 — `mcp/server.js` 를 실제 프로세스로 띄워 JSON-RPC 왕복(initialize · tools/list · 깨진 줄 뒤 정상 요청)을 잰다 — `core/test/eng-3i-residuals.test.ts:161` |
| COST-B | LOW | 05 | 열화(`state.json` 부재·손상) 상태가 자가회복하지 않아 매 훅 호출마다 저널 재생 비용을 무기한 재지불한다(100k 저널에서 +35ms/호출) | verified | measured | `core/src/hook.ts:151` | [COST-131] 과 같은 처방으로 닫힌다 — 이제 위반 없이 오래 작업해도 복구 권고가 턴마다 닿는다. **자동 복구는 여전히 하지 않는다**: 훅은 상태를 쓰지 않는다는 계약이 있고(`doctor --repair` 몫), 그 계약을 비용 때문에 깨면 훅이 판정기가 아니라 수정기가 된다 — `core/test/cost-3i-residuals.test.ts:73` |
| SEC-100 | BLOCKER | 06 | **`base64 -d ⏐ sh` 로 저널을 위조해 사람 승인 없이 배포 게이트가 열린다** — [SEC-A] 와 **결과가 동일**한데 포장만 다르다. 직접형 `echo '{...}' >> .harness/events.jsonl` 은 DENY 인데, 같은 문자열을 base64 로 감싼 `echo <b64> ⏐ base64 -d ⏐ sh` 는 훅이 **0바이트(=허용)** 로 통과시킨다. 실측 사슬: 위조(`phase-set P10` + `gate-approved P10`) → `doctor --repair`(issues 에 phase/gates mismatch 를 정직하게 보고하면서 **위조 쪽으로** 상태를 맞춘다) → `status` phase=**P10** → `npm publish` **DENY→ALLOW**. SEC-A 의 처방이 `git apply` 만 덮고 **파이프-투-셸을 안 덮었다** | verified | measured | `core/src/bashwrite.ts:172` | **포장이 아니라 부류를 막았다.** 해석기가 프로그램을 파이프·통째 명령치환·프로세스치환·stdin 에서 받으면 페이즈 무관 deny(`opaqueExec`). 리터럴 프로그램(`sh -c "npm test"`)과 스크립트 파일(`python3 x.py`)은 그대로 통과한다 — 전자는 재귀 스캔이, 후자는 본문 이어붙이기가 본다. **꺼낼 수 있는 것은 꺼내고 꺼낼 수 없는 것만 막는다.** 측정: 위조 6형태 전 페이즈(P0/P4/P7/P10/P12) DENY · 정상 25건 중 오차단 0 · 구축 트랙 9건 중 0 |
| SEC-101 | BLOCKER | 06 | **`mv .harness /tmp/x` 한 줄로 하네스 전체가 무음 해제된다** — 위조도 인코딩도 필요 없다. `rm -rf .harness` 는 **DENY** 인데 `mv .harness /tmp/gone`·`find .harness -delete` 는 **ALLOW(0바이트)** 다(상대경로일 때. 절대경로 `mv <abs>/.harness` 는 DENY — 규칙이 경로 형태에 걸려 있다). 끝단 관측: 해제 전 `Write src/app.ts` **deny** → `mv` 실행 → 같은 Write **0바이트 ALLOW**, `bin/harness-hook` sh 게이트도 침묵. `rm -rf` 를 막아 둔 것이 「에이전트는 하네스를 치우면 안 된다」는 제품 자신의 입장을 증명하는데 그 입장이 무효가 된다. [SEC-69](자기 무장해제)와 같은 부류 | verified | measured | `core/src/bashwrite.ts:363` | **보호 대상이 사라지는 것**을 본다 — `mv` 의 원본을 쓰기 대상으로 올리고(`cp` 와 갈리는 지점은 「원본이 사라지는가」), `find … -delete` 의 시작 경로와 `rmdir` 를 대상에 넣었다. 이름을 열거하는 대신 결과를 본다. `bashwrite.test.ts` 가 「mv 는 목적지만」으로 **옛 규칙을 고정하고 있었고 그 고정이 곧 구멍이었다** — 픽스처를 고쳤다. 측정: 제거 4형태 전 페이즈 DENY · 정상 이동·정리 과차단 0 |
| UX-102 | HIGH | 04 | **`loop next` 가 가장 막힌 순간에 실재하지 않는 `harness loop check` 를 유일한 다음 수로 안내한다** — fail×4 → `loop critical clear` → `loop next` → `"raise the critical event with \`harness loop check\`"` → 실행하면 `Unknown loop subcommand: check`(exit 1). 실재 명령은 `loop critical raise`. **이 출력을 읽는 것은 사람이 아니라 에이전트**(loop next 는 에이전트 루프 표면)라 막다른 길의 무게가 더 크다. [UX-A1](`loop clear` 유령)과 **동형 재발** | verified | measured | `core/src/loop.ts:453` | 문구를 실재하는 `loop critical raise` 로 고치고 streak 리셋 경로를 함께 적었다. 그리고 **이름 하나가 아니라 부류를 막았다**: `guidance-commands-exist.test.ts` 가 core/src 의 백틱 안 `harness …` 안내를 전수 뽑아 도움말 레지스트리와 대조하고(프로즈 오탐 0), cli-contract 가 **안내를 기계로 뽑아 실제로 실행**해 거부당하지 않는지 본다. 그 과정에서 `--reason` enum 오표기와 `--detail` 실질 필수도 드러나 함께 닫았다(파생 규칙은 `raiseCritical` 한 벌로 합침) |
| SEC-103 | MED | 06 | **`gate approve` 만 잠금이 0겹** — 훅이 `Bash("harness phase set P5 --force")` 는 **deny**(사유문에 "the user must run it themselves: `HARNESS_ALLOW_FORCE=1 …`")하면서 `Bash("harness gate approve P1")` 은 **0바이트 침묵 allow** 다. `--help` 가 스스로 "Humans only — never an agent" 라 적은 명령인데 훅 계층이 비어 있고, `--force`/`--accept-policy` 가 갖는 env 잠금도 없다. 기댈 것은 권한 다이얼로그 하나뿐이라 allowlist/bypassPermissions 환경에서 방어가 0(이 세션의 에이전트 실행에서 프롬프트 없이 `submit → approve` 성공) | verified | measured | `core/src/hook.ts:1578` | `--force`·`--accept-policy` 와 **같은 형태**로 훅에서 에이전트 경로를 닫는다. env 잠금은 두지 않았다 — approve 는 탈출구가 아니라 사람의 **정상 흐름**이고, env 를 요구하면 문서·패킷·도움말이 가리키는 길이 통째로 어긋난다. 훅은 에이전트 도구 호출에만 붙으므로 사람 경로에는 비용이 0 이다. 측정: 이름·경로·래퍼 4형태 DENY · submit/status/verify 과차단 0 |
| QUAL-104 | MED | 10 | **9바이트 텍스트 파일을 `mock.png` 로 두면 UX 증거 게이트가 통과한다** — 제품이 **이미 가진** PNG 헤더 검사를 완료 게이트가 부르지 않는다. `evidence check wave-001` 은 같은 파일에 "cannot read the PNG header — it may be a corrupt file that is only named .png" 를 내면서도 **`ok: true`** 를 반환하고, `wave complete` 는 "Wave completed". 증거가 아예 없을 때는 정상 거부된다. README 헤드라인 주장("can't ship a UX feature that was never actually drawn")의 자기해제 경로 | verified | measured | `core/src/evidence.ts:497` | `validateEvidence` 에 `usable` 을 두고 **`wave complete` 가 같은 값을 쓴다**(기준 한 벌). 「자기가 주장하는 것이 아닌」 파일(헤더 불가·형식 아님)만 빼고 크기 의심은 빼지 않는다 — 의심으로 막으면 정상 캡처가 걸린다. 거부 문구도 사실을 말한다(파일이 있는데 못 쓰면 「없다」가 아니라 문제를 그대로 싣는다). **픽스처 3곳이 `'fake'` 를 .png 로 쓰고 있었다 — 이 리포에서 세 번째 「픽스처가 규칙을 거짓말한다」** |
| UTIL-105 | MED | 01 | **STALE 웨이브를 재활성화하면 무경고로 완료된다** — `node bump` 은 활성 웨이브를 STALE 로 정산·비활성화한다(가드 있음). 그러나 그 웨이브를 `wave activate` 로 되살리면 무경고로 활성화되고 `wave complete` 가 "Wave completed" 를 낸다. 결과 frontmatter 는 `status: done` + `design_refs: F-1` — **v1 기준으로 지었다는 기록이 남지 않는다.** README "nothing silently builds on an outdated decision" 이 advisory 로만 성립 | verified | measured | `core/src/wave.ts:322` | 되살리는 **순간** 막고 `node bump` 이 이미 하던 안내(「현재 설계로 새 웨이브를 열어라」)로 보낸다. 끝단에서 막지 않는 이유는 그때는 이미 낡은 결정 위에 지은 뒤이기 때문이다. 그 과정에서 `bumpNode` 의 저널·STALE 전파가 CLI·MCP **두 벌**인 것이 드러나 `reviseNode` 한 벌로 합쳤다(도메인만 부르는 경로에서는 전파가 아예 안 됐다) |
| ENG-106 | MED | 03 | **판정 규칙 중복 — 노드 upsert 병합 의미론이 두 벌** (`core/src/cli.ts:1206` + `core/src/mcp.ts:430`). 뮤테이션 실측: 클론에서 `mcp.ts` 의 `version: prev?.version ?? 1` → `version: 1` 로 바꿔도 **982 전건 green(미검출)**, 같은 변경을 `cli.ts` 에 하면 검출. MCP 가 개정 카운터를 조용히 리셋해도 CI 가 못 잡는다. 엔지니어링 축의 **하드 조건 「중복 규칙 0」 위반** — [LOGIC-93]·[API-92]·[ENG-C/D/E] 에 이은 네 번째 벌 | verified | measured | `core/src/ledger.ts:176` | `mergeNode` 한 벌로 내리고 두 표면은 사용자가 준 것만 넘긴다. 어댑터가 `upsertNode` 를 직접 부르지 못하게 테스트로 고정했다. 측정: CLI·MCP 양쪽에서 bump 후 재등록 시 version 보존 · 미지정 필드 승계 확인 |
| ENG-107 | MED | 03 | **테스트가 규칙 3건의 뮤테이션을 못 잡는다**(15 표본 중 3 생존) — 특히 SEC-96 회귀 테스트가 **리포 이름의 우연**으로 통과해 node-invoke 가드를 제거해도 초록이 유지된다. 회귀가 초록 뒤에 숨는 구조 | verified | measured | `core/src/hook.ts:135` | **그 절만 발화하는 입력**으로 잰다 — 설계 트랙에서는 「구현 코드 금지」가 먼저 잡아 주는 탓에 가드를 지워도 초록이었다(감정이 말한 「우연히 통과」가 정확히 이것). 구축 트랙 + 이름·env 없는 형태로 재고, **뮤테이션 실증**: `CORE_INVOKE_RE` 절을 죽이면 이 테스트가 CAUGHT |
| EFF-108 | MED | 02 | **배포 명령 차단이 순수 조회를 과차단한다** — 설계 트랙에서 `grep "npm publish" README.md` 가 **deny** "Deploy-ish commands (npm publish) cannot run in the design track.". `echo npm publish is step 3 >> notes.md` 도 동일. CORE_FILES 방어망은 `scan.mutating &&` 가드로 `grep`/`cat` 를 통과시키는데 deploy 차단에는 그 가드가 없다(`cmd.includes(b)`). 리포 자신의 원칙(「과차단 = 결함과 같은 무게」)에 정면 위배 | verified | measured | `core/src/bashwrite.ts:475` | `commandLines`/`runsCommand` 로 **실행 위치**만 본다 — 접두 명령·패키지 러너(npx)를 벗기고 `sh -c`·`eval`·`xargs`·`find -exec` 안쪽을 꺼내 함께 본다(래퍼 한 겹으로 풀리지 않는다). config 목록과 프로파일 판정이 같은 함수를 쓴다. 측정: 조회 5형태 통과 · 실제 실행 3형태 DENY |
| EFF-109 | MED | 02 | **출하 트랙에서 `sed -i s/x/y/`·`perl -i -pe s/x/y/` 로 기존 파일을 고치면 거짓 "New files cannot be created" 로 거부된다** — sed 스크립트 `s/x/y/` 가 경로로 오파싱돼 출하 new-file 규칙이 오발한다. 다른 구분자(`s⏐x⏐y⏐`)나 Edit 도구로는 통과. 출하 트랙의 핵심 작업(기존 결함 수정)에 마찰 + 원인을 오도하는 메시지 | verified | measured | `core/src/bashwrite.ts:325` | 주 추출기(`scriptFiles` — 이들 도구의 실제 계약대로 `-e`/`-f` 가 있으면 프로그램은 그 플래그가 데려가고, 없으면 첫 피연산자가 프로그램)와 안전망(`pathLikeMentions`) **두 곳 다** 치환 스크립트를 안다. 한쪽만 고치면 다른 쪽이 남는다 |
| COST-110 | MED | 05 | **MCP 표면에 비간섭 게이트가 없다** — 훅은 sh 게이트로 3ms·0토큰까지 줄여 놨는데(PERF-95), MCP 서버는 `.harness/` 가 없는 프로젝트에서도 세션마다 상주한다. 실측: 빈 디렉토리에서 initialize+tools/list → 16개 도구 **5,824B**, RSS **41,536KB**. 스키마 주입형 호스트에서는 ≈1.4k 토큰 상시 지불 | verified | measured | `mcp/server.js:60` | 이 파일이 **이미 쓰던 규칙**(코어 번들이 없으면 살아 있되 도구 0)을 `.harness/` 부재에 그대로 적용했다. 실측 5,835B → **13B**. `harness init` 이 세션 도중 실행되면 `notifications/tools/list_changed` 로 알린다 — 비용을 줄이려다 기능을 없애지 않기 위해서다 |
| COST-111 | MED | 05 | **순수 조회만 한 턴도 stop 정산 block 을 유발한다** — 활성 웨이브에서 `ls` 한 번의 post-tool 이 `.runtime/last-activity` 를 기록해 stop 이 284B block 을 낸다. 탐색만 한 턴마다 왕복 1회(≈65tok + 응답)가 추가된다. pre-tool 이 이미 계산한 `scanBashWrites` 의 순수조회 판별을 post-tool 이 쓰지 않는다 | verified | measured | `core/src/hook.ts:1265` | **화이트리스트**로 간다. 처음에 `!scan.mutating` 으로 재려다 되돌렸다 — 그 판정은 「모르겠으면 참」이 안전한 방향이라 **부정으로 쓰면 편향이 뒤집힌다**(`git commit` 이 조회로 잡혔다). 아는 것만 조회로 인정하고 `!mutating` 과 AND 로 묶으므로 `echo hi` 는 조회, `echo hi > f` 는 활동이다. [SEC-78] 의 교훈(활동 집계에서 빠진 작업 턴은 정산 강제를 조용히 푼다)을 지킨다 |
| PROD-112 | MED | 07 | **`design sync` 가 내용이 바뀌어도 "unchanged (same hash)" 라고 거짓 보고한다**(draft 노드) — 실측: `design link` 후 `'AAA'` sync → `'BBB completely different…'` sync ⇒ **두 번 다** `UX-9 unchanged (same hash)`, exit 0(해시는 실제로 다르고 저널에는 canvas-synced 가 기록된다). approved 노드에서는 광고대로 동작. 「정직한 판정」이 정체성인 제품의 자기모순이고, 캔버스를 반복 수정 중인 사용자가 sync 가 고장났다고 오판한다 | verified | measured | `core/src/design.ts:68` | 결과가 셋(동일 · 바뀜/draft · 바뀜/개정)이라 **세 문장으로** 말한다. `contentChanged` 를 따로 두어 「개정했는가」와 「내용이 바뀌었는가」를 섞지 않는다. 같은 자리의 bump 사본도 `reviseNode` 로 합쳤다 |
| PROD-113 | MED | 07 | **배포본에 내부 작업물이 통째로 들어가고, 그중 하나가 README 와 반대되는 출하 판정을 말한다** — `git archive HEAD` = 201 파일에 `progress.md`(140K 한국어 내부 로그) · `docs/release-readiness/`(476K) · `docs/appraisal/`(48K) = **664K**. 그중 `docs/release-readiness/readiness.md:3` 은 "**판정 출하 가능** · open 전체 0" 인데 `README.md:201` 은 "still **not-ready**" — 설치자가 서로 모순되는 판정 문서를 동시에 받는다. [PROD-B6](LOW)이 파일 잔존만 다뤘고 **배포 동봉**은 새 면 | verified | measured | `.gitattributes:9` | `export-ignore` 로 `progress.md`·`docs/release-readiness`·`docs/appraisal`·`.codesight` 를 배포본에서 뺀다. 측정: `git archive` **207 → 151 파일**. 리포에는 그대로 남는다 — 빼는 것은 배포본에서다 |
| PROD-114 | MED | 07 | **지원 채널이 없다** — README 4종에서 github/issues/support/contact 실질 히트 **0**(유일 히트는 타 프로젝트 링크). `package.json` 의 `repository`·`bugs`·`homepage`·`license` **전부 undefined**(LICENSE 파일과 불일치). 공개 배포 제품이 고장났을 때 사용자가 갈 곳이 없다 | verified | measured | `package.json:5` | LICENSE 가 MIT 인데 비어 있던 `license` 필드를 채우고, 4개 언어 README 에 지원 안내를 넣었다(전부 로컬에서 도는 실재 명령 — `harness doctor`·`doctor --repair`·`hook-errors.log`·`--help`). **`repository`/`bugs` 는 채우지 않았다**: `git remote` 가 없어 공개 저장소 주소를 지어낼 수 없다 — 저장소 위치가 정해지면 채워야 한다(그때까지 남는 한계로 고지) |
| QUAL-115 | MED | 10 | **대장 lint 가 글자접미 ID 를 통째로 건너뛴다** — 외부 `ledger-lint.sh` 의 ID 패턴 `[A-Z]*-[0-9]*` 이 라운드 3-F 가 도입한 글자접미 ID(SEC-A·UX-A1·PROD-B1 등)를 못 본다. 라운드 3-G 착수 시점에 **122행 중 83행만** 검사됐고, [QUAL-B](deferred)는 이 사각을 「8행」이라 적어 둔 채 낡아 있었다(실제 41행). **실증**: 이번 라운드 신규 행을 글자접미로 등재하자 lint 가 「헤더 open BLOCKER(2) ≠ 표 실제(0)」라는 **거짓 실패**를 냈다 — 가장 중요한 숫자를 lint 가 못 보고 있었다는 뜻이다. lint 통과는 4.8 하드 조건인데 그 통과가 3분의 1을 안 본 통과였다 | verified | measured | `docs/release-readiness/2026-08-21/ledger.md:3` | 사각 자체는 리포 **밖**(스킬) 소관이라 없앨 수 없다. 대신 **조용히 자라는 것**을 막았다: 신규 35행을 숫자 ID(100–134)로 등재해 커버리지를 83/122 → **118/157** 로 올리고, lint 가 못 보는 행 수의 상한(41)을 테스트로 고정했다. 리포 안 검사(집계·인용·어휘)는 원래 전 157행을 덮는다 |
| UX-116 | MED | 04 | **리뷰 패킷의 Blockers 가 존재하지 않는 강제를 단언한다** — doc 등록 없이 `gate submit P0` 하면 패킷에 "no artifact registered for P0 — an empty packet cannot open a gate" 가 실리는데, 실제로는 `gate approve P0` 도 `phase set P1` 도 성공한다. 사람 리뷰어가 「어차피 시스템이 거부한다」고 믿게 만들고, 처방("register the artifacts")에 실제 명령(`harness doc upsert`)이 없다 | verified | measured | `core/src/report.ts:405` | 사실대로 적는다 — 「열 수 없다」가 아니라 「지금 승인하면 보지 않은 것을 승인하게 된다」. 처방에 실재하는 명령을 실었다(`harness doc upsert` → 발행 → `harness doc url`) |
| UX-117 | MED | 04 | **`state.json` 이 손상되면 `status` 가 원시 파스 오류만 뱉고 처방을 안 준다** — `echo 'garbage{' > .harness/state.json` → `status` → `Unexpected token 'g' … is not valid JSON`(exit 1). 혼란 시 가장 먼저 치는 명령이 스트레스 순간에 원인만 던지고 `harness doctor --repair`(실측 복구 성공)를 안내하지 않는다 | verified | measured | `core/src/state.ts:50` | 파일이 **없는** 경우는 이미 처방이 있었는데 **손상된** 경우만 비어 있었다 — 원인은 달라도 처방은 같다(저널이 진실이므로 재생). 안내한 명령이 실제로 복구하는 것까지 테스트로 고정했다 |
| UX-118 | MED | 04 | **`profile cmd` 가 프로파일 파스 오류를 삼키고 순환 처방을 낸다** — `commands.yaml` 에 중복 키가 있으면 `profile cmd build` 가 "Profile generic has no 'build' command — fill it in commands.yaml" 를 낸다(방금 채운 사용자에게 다시 채우라고 하고, 어느 파일인지 경로도 없다). 같은 상태에서 `profile show` 는 파스 오류를 줄·열까지 노출 — 표면 간 비대칭 | verified | measured | `core/src/cli.ts:668` | `inspectProfile` 로 파스 오류를 함께 싣고 파일 경로를 말한다. 같은 상태에서 `profile show` 는 줄·열까지 보여 줬다 — **같은 사실을 표면마다 다르게 말하면 사람은 덜 말하는 쪽을 믿는다** |
| UTIL-119 | LOW | 01 | README 명령표 4개 언어가 `wave create [--goal g]` 를 선택 인자로 오표기 — 실제 필수(`wave create --refs F-1` → "A wave needs a goal" exit 1) | verified | measured | `README.md:182` | README 4종을 `--goal <g>` **필수** 표기로 고치고, 실제로 필수인 것을 끝단으로 확인한다(무인자 `wave create` → exit 1) — `core/test/prod-3i-surfaces.test.ts:106` |
| UX-120 | LOW | 04 | 성공 출력이 bare id 뿐이라 무슨 일이 났는지 안 알려 준다 — `node upsert`→`N-1`(생성/갱신 구분 불가) · `wave create`→`wave-001` · `evidence spec`→경로만(프로젝트 트리에 파일을 만들었다는 사실 무언급) | verified | measured | `core/src/cli.ts` | `doc upsert`·`node upsert` 가 **생성인지 갱신인지**를 말하고, `doc upsert` 는 다음 수(발행 → `harness doc url <id> <url>`)까지 싣는다 — 같은 id 로 두 번 부른 사람이 자기가 무엇을 덮었는지 알 수 있다 — `core/test/ux-3i-next-move.test.ts:38` |
| UX-121 | LOW | 04 | `doctor --repair` 출력이 **수리 전** 이슈를 그대로 나열한다 — `repaired:true` + `ok:false` + "state.json is damaged" 가 동시에 찍혀, 청정 여부를 알려면 한 번 더 돌려야 한다 | verified | measured | `core/src/doctor.ts` | **판정(ok)을 수리 후 상태로** 낸다 — 수리했을 때만 한 번 재진단하고, 수리 전 issues 는 「무엇이 어긋나 있었나」로 그대로 남긴다(새 `remaining` 필드가 남은 문제를 말한다). 한 번 더 돌려야 알 수 있는 보고는 사람에게 같은 일을 두 번 시킨다 — `core/test/low-3i-guidance.test.ts:56` |
| UX-122 | LOW | 04 | `backtrack <P>` 성공 문구가 다음 수를 안 준다 — "Backtrack started → P0" 직후 `status` 는 여전히 P1(마커만 설정됨을 말하지 않는다). `gate submit` 성공 문구도 다음 수(사람 approve)를 안 준다 | verified | measured | `core/src/cli.ts` | `backtrack` 은 「마커만 섰고 페이즈는 그대로」와 다음 수(`phase set`)를, `gate submit` 은 다음 수(사람 승인)를 말한다 — `core/test/low-3i-guidance.test.ts:81` |
| UX-123 | LOW | 04 | MCP 오류의 처방이 CLI 명령이다 — `harness_wave_create` 의 고스트 ref 오류가 `harness node upsert` 를 처방한다(MCP 도구명 `harness_node_upsert` 미언급) | verified | measured | `core/src/mcp.ts` | 고스트 ref 처방이 **두 표면의 이름을 함께** 말한다(CLI `harness node upsert` · MCP `harness_node_upsert`). 도메인은 양쪽에서 불리는데 문구가 CLI 로 고정돼 있어 MCP 에이전트는 없는 도구를 찾았다 — `core/test/low-3i-guidance.test.ts:98` |
| UX-124 | LOW | 04 | `doc submit` 거부문에 정확한 처방 명령(`harness doc url <id> <url>`)이 없다 — "Publish it as a claude.ai artifact first and register the URL" 까지만 | verified | measured | `core/src/cli.ts` | 처방에 **칠 수 있는 명령**을 실었다(`harness doc url <id> <url>`). 한국어 쪽은 내부 함수명 `setDocArtifactUrl` 을 사용자에게 노출하고 있었다 — 그것까지 함께 없앴다 — `core/test/ux-3i-next-move.test.ts:69` |
| PROD-125 | LOW | 07 | README 4개 언어의 테스트 계량이 낡았다 — "976 passing (33 files)" vs 실제 **982 passed (33 files)** | verified | measured | `README.md:187` | README 4개 언어의 테스트 계량을 실제와 맞췄다 — **1047 passing (41 files)**. `doc-claims.test.ts` 가 파일 수까지 대조하므로 낡으면 빨강이 된다 — `core/test/doc-claims.test.ts:1` |
| PROD-126 | LOW | 07 | 버전 표면이 약하다 — `--version` 이 "king-wjang-harness core v0"(0.0.1 미표기) · CHANGELOG 없음 · git tag 0개. 버그 리포트에 정확한 버전을 적을 수 없다 | verified | measured | `bin/harness:9` | `--version` 이 `package.json` 을 읽어 실제 버전을 낸다(`king-wjang-harness v0.0.1`) — 상수로 박으면 릴리스마다 두 곳이 갈린다. `plugin.json` 과의 일치, CHANGELOG 존재까지 테스트로 고정했다. CHANGELOG.md 신규 — `core/test/prod-3i-surfaces.test.ts:38` |
| PROD-127 | LOW | 07 | `tokens gen` 이 기본으로 프로젝트 루트에 `tokens.css`·`tokens.ts`·`tailwind.tokens.js` 를 뿌리는데 기본 위치가 문서화돼 있지 않다(`--help` 는 `[--out <dir>]` 만) | verified | measured | `core/src/tokens.ts` | `tokens gen` 도움말이 기본 출력 위치를 말한다(프로젝트 루트의 `tokens.css`·`tokens.ts`·`tailwind.tokens.js`) — `core/test/prod-3i-surfaces.test.ts:94` |
| PROD-128 | LOW | 07 | 출하 스크립트·코어 주석이 한국어 전용(`bin/harness`·`bin/harness-hook`·`mcp/server.js`) — 4개 언어로 배포하는 제품인데 비한국어 사용자가 안전 관련 주석(무해 계약 등)을 읽을 수 없다 | verified | measured | `bin/harness-hook` | 배포되는 스크립트·전송층의 주석을 전부 영문화했다(`bin/harness`·`bin/harness-hook`·`mcp/server.js` — 한국어 줄 **0**). 4개 언어로 배포하는 제품에서 비한국어 사용자가 **안전 관련 계약**(무해 계약·stdout 위생·판정 동일성)을 못 읽는 상태였다 — `bin/harness-hook:1` |
| COST-129 | LOW | 05 | 훅 1회당 `config.yaml` 을 3번 읽고 3번 YAML 파싱한다(`handleHook`/`loadProfile`/`tr`) — 51B 파일이라 실측 영향은 무시 수준, 대형 config 에서만 유의 | verified | measured | `core/src/config.ts:38` | **「대형에서만 유의」는 「무시해도 된다」가 아니었다.** 실측: 51B 는 0.06ms×3 이지만 66KB config 는 **37.5ms×3 = 112ms/호출**로 훅의 실제 일(인프로세스 p50 0.9ms)을 압도했다. 나노초 mtime+크기 키로 캐시해 **112ms → 0.011ms**. 밀리초 해상도로는 같은 밀리초 안의 변경을 놓치므로 나노초를 쓴다 — 성능을 위해 정확성을 내주면 최적화가 아니라 버그다 — `core/test/cost-3i-residuals.test.ts:98` |
| COST-130 | LOW | 05 | `.harness` 가 디렉토리가 아니라 **파일**이면 sh 게이트와 코어 판정이 갈린다 — sh 게이트는 0B(전부 허용), `bin/harness hook` 은 deny. 「판정 기준이 코어와 글자 그대로 같다」는 주석 계약이 깨진 상태 | verified | measured | `bin/harness-hook:12` | sh 게이트를 `-d` 에서 **`-e`(존재)** 로 바꿔 코어와 같은 판정을 쓴다 — `.harness` 가 일반 파일이면 그 프로젝트는 미사용이 아니라 **잘못 설정된** 것이므로 통과가 아니라 판정으로 보낸다(fail-closed). 코어 주석의 「같은 정의」 계약도 사실과 맞췄다 — `core/test/cost-3i-residuals.test.ts:130` |
| COST-131 | LOW | 05 | 열화(저널 재생) 상태의 비용이 **허용** 경로에서는 고지되지 않는다 — `degradedNote` 가 deny·session-start 에만 붙어, 위반 없이 오래 작업하면 복구 권고를 못 보고 호출당 +30ms·20MB 읽기를 계속 낸다 | verified | measured | `core/src/hook.ts` | 열화 고지를 **턴 끝(stop)** 에도 붙였다. allow 경로에 붙이지 않은 이유는 **비간섭 계약**(위반이 없으면 0바이트) 때문이고, 턴 끝은 그 계약을 안 깨면서 사람에게 닿는 유일한 지점이며 턴당 한 번이라 잡음이 되지 않는다. **차단이 있으면 사유를 대신하지 않고 덧붙인다** — `core/test/cost-3i-residuals.test.ts:73` |
| EFF-132 | LOW | 02 | `git push` 가 generic 프로파일의 deploy 목록에 없어 출하 트랙에서 허용된다 — 프로파일 완전성 문제 | rejected | measured | `profiles/generic/profile.yaml:25` | **결함이 아니라 이미 내린 반대 결정이다.** 목록에 넣어 봤더니 `hook-pre-tool.test.ts:217` 이 「브랜치 push 는 조회·개발 명령이라 막으면 과차단」이라고 못 박아 두고 있었고, `profile.test.ts` 는 번들 프로파일과 `DEFAULT_CONFIG` 목록이 **바이트 동일**이어야 한다고 강제한다. 설계 트랙에서 WIP 브랜치를 못 올리면 정상 작업이 막힌다 — 이 리포는 과차단을 미차단과 같은 무게로 센다. 되돌렸다 |
| QUAL-133 | LOW | 10 | `gate status` 가 저널 재생으로 살아난 게이트를 빈 배열 `[]` 로 출력하는데 강제 경로는 그 재생 상태를 따른다 — 표면 간 불일치([SEC-100] 재현 중 관측) | verified | measured | `core/src/gate.ts` | 상태 해석을 **한 벌(`resolveState`)로** 내려 조회와 강제가 같은 답을 낸다. 열화라는 사실도 함께 표시한다 — 조용히 재생 결과만 보여 주면 이번엔 열화가 숨는다. 정상 상태에서는 표시가 붙지 않는다(과보고 0) — `core/test/cost-3i-residuals.test.ts:45` |
| VAL-134 | MED | 10 | 프로젝트 **안**의 없는 파일을 `gate submit --paths` 로 주면 "Cannot read the artifact" 가 아니라 **"outside paths"(프로젝트 밖)로 오진**한다. 원인은 파일 없음인데 위치 문제로 안내해 첫 사용자가 탈출하지 못한다. **심링크 루트 한정이 아니었다** — 라운드 3-I 사용성 감정자가 평범한 루트에서 재현했고, 그래서 LOW 에서 MED 로 올린다. [PROD-C] 가 「재현 실패 = 결함 아님」으로 닫은 결론은 두 번 다 틀렸다 | verified | measured | `core/src/gate.ts:330` | **존재하는 가장 가까운 조상까지 realpath 로 풀고 나머지 구간을 다시 붙인다** — 예전에는 해석 실패 시 리터럴 경로를 써서, 심링크 루트(맥OS `mktemp`)에서 루트만 풀리고 대상은 안 풀려 안의 파일이 밖으로 판정됐다. 없는 파일은 이제 「읽을 수 없음」으로, 진짜 루트 밖은 그대로 거부(과소차단 0), 심링크 루트의 정상 제출 통과(과차단 0) — `core/test/med-3i-messages.test.ts:38` |
| SEC-135 | BLOCKER | 06 | 🔴 **열거 밖 쓰기 프리미티브가 모든 쓰기 차단을 비껴간다** — `scanBashWrites` 가 쓰기 도구를 **이름으로 열거**해서, 목록 밖 도구가 위치 인자로 파일을 쓰면 대상 추출이 안 되고 `mutating=false` 로 ALLOW 됐다: `xxd -r -p a.hex src/app.ts` · `openssl enc -out src/b.ts` · `csplit -f src/c …` · `split -l1 in src/d`. 리다이렉트형은 막히는데 **위치인자형만** 뚫렸다. [SEC-50]·[SEC-92](둘 다 BLOCKER)와 같은 부류의 재발 — 라운드 3-I 가치 축 감정자가 찾고 메인 세션이 끝단까지 재현 | verified | measured | `core/src/bashwrite.ts:434` | **기본값을 뒤집었다** — 조회라고 아는 것(`READ_ONLY_HEADS`)만 빼고 나머지는 변형으로 본다. 대상을 직접 올리지는 않는다(`node build.js` 오인) — 기존 안전망이 발화해 진짜 경로만 판정으로 간다. 발견 원문 `docs/release-readiness/2026-08-21/round3i/appraisal2-4-value.md:55` · 재현 `docs/release-readiness/2026-08-21/round3i/repro-3i.md:11` · `fixes-round3i.md` · 커밋 `d065cf1` |
| SEC-136 | BLOCKER | 06 | 🔴 **[SEC-69] 자기 무장해제 재발** — [SEC-135] 의 구멍으로 `openssl enc -out .harness/config.yaml` 을 쓰면 `design_allowed_prefixes` 가 덮여 **강제 자체가 풀린다**. 실측 사슬: 막혀 있던 `Write src/app.ts` 가 config 덮기 한 번으로 ALLOW 로 바뀌었고, `doctor` 는 정책 드리프트를 **warning 으로만** 알릴 뿐 강제를 복원하지 않았다 | verified | measured | `core/src/hook.ts:901` | [SEC-135] 처방으로 닫힌다. 더해 **가장 무거운 사유를 먼저 말하도록** 코어·정책 대상을 먼저 판정한다 — 안 그러면 입력 파일이 「새 파일 금지」로 거부돼 진짜 사유가 안 보인다. 발견 `docs/release-readiness/2026-08-21/round3i/appraisal2-4-value.md:76` · 재현 `docs/release-readiness/2026-08-21/round3i/repro-3i.md:23` · `fixes-round3i.md` · 커밋 `d065cf1` |
| SEC-137 | BLOCKER | 06 | 🔴 **비용 캡이 방어를 되돌린다 — 64KB 넘는 스크립트는 본문을 아예 안 읽고 통과** — 같은 첫 줄(`echo "{}" >> .harness/events.jsonl`)의 35B 스크립트는 DENY, 70KB 패딩본은 **ALLOW**. [SEC-49]·[SEC-A]·[SEC-100] 세 커밋이 막아 온 저널 위조 → `doctor --repair` → **사람 승인 없는 게이트 개통** 사슬이 크기 패딩만으로 다시 열렸다. README 「알려진 한계」에도 미기재였다 | verified | measured | `core/src/hook.ts:680` | 캡은 지연을 지키려 남기고 **못 읽은 것을 사실로 올려 거부**한다(`opaqueExec` 와 같은 태도). 발견 `docs/release-readiness/2026-08-21/round3i/appraisal2-5-cost.md:81` · 재현 `docs/release-readiness/2026-08-21/round3i/repro-3i.md:31` · `fixes-round3i.md` · 커밋 `d065cf1` |
| SEC-138 | MED | 06 | **`gate approve` 훅 백스톱이 사소한 난독화로 열린다** — `node -e "require('…'+'cli.js')…"` · `harness gate 'appr''ove' P0` · `bin/hh gate approve P0` 로 **실측 개통**. 같은 파일의 `--force`·`--accept-policy` 는 **훅+CLI env 두 겹**이라 같은 난독화를 버틴다 — **이 비대칭이 결함이다.** [SEC-103] 이 이 백스톱을 만든 이유가 「allowlist·bypassPermissions 환경엔 승인 다이얼로그가 없어 방어가 0」이었는데, 바로 그 환경에서 한 겹이 뚫린다 | verified | measured | `core/src/hook.ts:1115` | **두 번째 겹을 TTY 로 뒀다** — env 가 아니라 TTY 인 이유는 `gate approve` 가 탈출구가 아니라 정상 흐름이라서다(사람에게 비용 0). 훅에는 `--force` 와 같은 두 절(탈출구 env 리터럴 언급 + 실행 형태)을 뒀다. 난독화 3형태 전부 승인 불가·정상 사람 경로 통과·`gate status`·`verify`·`submit` 과차단 0 — `core/test/sec-138-approve-tty.test.ts:50` |
| PERF-139 | MED | 05 | **광고한 지연이 어느 표면에서도 재현되지 않는다 — 그리고 G9 는 표면 선택만으로 통과·불통과가 갈린다** — 같은 코드·같은 저널(100,002줄·15MB·열화)을 두 표면에서 쟀다: **인프로세스 p95 18.9ms** vs **프로세스 wall-time p95 162ms**. README 의 「실측 62ms · 폴백 102ms」는 **둘 어느 쪽과도 안 맞는다**(표면 미기재라 재현 불가). G9(「폴백 p95 < 150ms」)는 wall-time 으로 읽으면 미충족, 인프로세스로 읽으면 8배 여유로 충족이다 — 라운드 3-G(103.5ms)와 3-I(202.5ms) 감정자가 어긋난 이유가 이것이다(**둘 다 정직했고 표면이 달랐다**). wall-time 절대값의 **99ms 는 Node 기동**이라 그 문턱은 제품이 아니라 측정 머신을 잰다 | verified | measured | `docs/release-readiness/2026-08-21/evidence/perf-139-latency.log:20` | README 4개 언어의 지연 주장을 **표면·실측값과 함께** 재현 가능하게 고쳤다(인프로세스 2.6/18.9ms · wall 133/162ms · 그중 Node 기동 99ms). G9 문턱은 **사용자 결정**으로 「폴백이 더하는 p95 비용 < 50ms」로 옮겼다 — 실측 wall +29ms · 인프로세스 +16.3ms 로 충족. 절대 wall-time 은 머신의 Node 기동에 지배돼 제품이 아니라 측정 머신을 잰다. **기준을 바꾼 것이므로 감정자 재검증 전까지 이 근거로 판정을 올리지 않는다**([OPS-74]) — 재정의 사유는 `docs/release-readiness/2026-08-21/gates.md:25` |
| QUAL-140 | MED | 10 | **1×1 PNG 가 UX 증거 게이트를 통과한다** — 유효한 70B·1×1 PNG 를 `.harness/evidence/<wave>/` 에 두면 `wave complete` 가 성공한다. 같은 파일에 `evidence check` 를 돌리면 "70 bytes (1x1) is too small — most likely a blank screen or a failed capture" 가 나온다 — **검출 로직은 이미 제품 안에 있는데 완료 판정이 그것을 소비하지 않는다.** [QUAL-104] 는 0바이트·비PNG 만 막았다(바이트가 아니라 **치수**로 재야 한다). `.harness/evidence/` 쓰기는 훅이 막지 않으므로 에이전트가 스스로 만들 수 있다 | verified | measured | `core/src/wave.ts:274` | **의심을 게이트 기준으로 올리고, 바이트가 아니라 치수로 잰다.** 크기 문턱은 `tEXt` 청크 패딩으로 넘을 수 있어(SEC-137 이 실증한 수법) 각 변 200px 문턱을 더했다 — 가장 좁은 뷰포트도 375px 라 실주행 캡처는 반드시 넘는다. `wave complete`(usable)와 `hasMeasuredEvidence`(출하)가 **같은 세 조건**을 쓴다. 1×1·패딩 1×1 모두 거부 · 400×300 실캡처 통과(과차단 0) — `core/test/qual-140-ux-dimension-gate.test.ts:80` |
| PROD-141 | MED | 07 | **배포 아카이브에서 자기 테스트가 안 돈다** — `.gitattributes` 가 `docs/release-readiness` 를 export-ignore([PROD-113])하는데, 함께 배포되는 `ledger-summary-sync.test.ts` 는 그 디렉토리를 **절대경로로 읽는다.** `git archive HEAD`(마켓플레이스·태그 배포 경로) 산출물에서 `npm test` = **1 파일 수집 실패(ENOENT)·12 테스트 미실행**. [PROD-113] 을 고정한 테스트는 「export-ignore 가 있는가」만 보고 「아카이브가 여전히 자급하는가」를 안 본다 | verified | measured | `core/test/ledger-summary-sync.test.ts:16` | `describe.skipIf` 만으로는 부족했다 — **vitest 는 건너뛸 describe 의 본문도 수집 시점에 실행**해서 아카이브에서 여전히 죽었다(실측). 읽기 자체를 안전하게 바꾸고, 리포 안에서 조용히 건너뛰면 빨강이 되는 sanity 검사를 함께 뒀다. **실측**: `git archive HEAD` 산출물에서 `npm test` → 수정 전 `Test Files 1 failed`·1059 passed, 수정 후 **42 passed | 1 skipped · 실패 0** — `core/test/ledger-summary-sync.test.ts:20` |
| ENG-142 | MED | 03 | **결함 대장 쓰기 차단이 무테스트** — `STATE_FILES` 의 `defects.yaml`·`deployments.yaml` 보호 절을 지워도 아무 테스트가 안 잡는다(뮤테이션 생존). 이 절은 「대장을 위조해 사람 게이트의 근거를 속인다」([ENG-B] 실사고)의 **유일한 대책**이고 doctor 의 대조 범위 밖이라 회귀해도 침묵한다 | verified | measured | `core/src/hook.ts:151` | **이름 두 개가 아니라 부류를 막았다** — 보호 목록을 `hook.ts` 소스에서 직접 읽어(사본이 낡지 않게) Write·Edit·Bash 리다이렉트·Bash 위치인자 4표면 × 설계·구축·출하 3트랙 전수 대조한다. 목록에 파일을 추가하면 보호가 붙기 전까지 빨강이다. **뮤테이션**: 감정자가 죽인 그 두 줄을 지우면 2건 CAUGHT · 비보호 `.harness` 산출물 과차단 0 — `core/test/eng-142-core-file-guards.test.ts:46` |
| ENG-143 | MED | 03 | **출하 measured-only 규칙이 두 벌** — `gate.ts` 의 게이트 승인 검사와 `ship.ts` 의 verdict 가 「증거 등급이 measured 여야 한다」를 각각 구현한다. 발산해도 강제는 fail-safe 지만 문언이 갈리면 **verdict 와 approve 가 서로 다른 말을 한다.** `ship.ts` 헤더가 「다시 구현하지 않는다」고 선언해 둔 것과 코드가 어긋나는 것 자체가 이 리포가 반복해 무는 「같은 규칙 두 벌」 패턴 | verified | measured | `core/src/gate.ts:667` | 규칙과 **문언을 한 벌로** 내렸다(`measuredOnlyViolation`) — 위반이면 사유 문자열, 아니면 null 을 주고 던질지 모을지는 부르는 쪽이 정한다. `ship.ts` 머리말의 「다시 구현하지 않는다」 선언과 코드를 다시 일치시켰다. 승인 거부문과 verdict 사유가 **같은 절**을 쓰는 것을 테스트로 고정 — `core/src/gate.ts:667` |
| UX-144 | MED | 04 | **usage 티어 하향이 기록되지 않아 stale 지시가 무기한 주입된다** — `if (inject) recordTier(...)` 라서 **상승할 때만** 기록한다. 한 번 90% 를 찍으면 이후 사용량이 10% 여도 모든 새 세션 SessionStart 에 "[harness] usage at 90% — split waves smaller" 가 계속 주입되고, **해제 명령이 없다**(`usage` 는 tier·status 뿐). 유일한 탈출은 미문서화된 `.harness/.runtime/usage-tier` 손편집이다. `usage.ts` 모듈 주석(「하강(리셋)은 조용히 기록만 한다」)과 코드가 정반대 | verified | measured | `core/src/cli.ts:542` | **기록은 항상, 주입은 상승에만.** `usage.ts` 모듈 주석이 「하강(리셋)은 조용히 기록만 한다」고 적어 둔 계약이 코드에만 없었다. 하향 기록 → SessionStart 주입 소멸을 **끝단(훅 출력)으로** 확인했고, 같은 티어 반복 무주입·재상승 시 재주입도 함께 고정했다 — `core/test/med-3i-messages.test.ts:70` |
| UX-145 | MED | 04 | **`evidence packet --ux <UX-x>` 가 사유 없는 bare usage 로 실패한다** — help 는 `[--wave]` 를 선택으로 적어 두었는데 활성 웨이브가 없으면 원인 설명 없이 Usage 한 줄만 내고 exit 1. **형제 명령 `evidence spec` 은 같은 상황에서** "no active wave — activate ... or pass waveId" 로 설명한다 — 같은 사실을 표면마다 다르게 말한다 | verified | measured | `core/src/cli.ts:664` | 원인 둘을 갈랐다 — `--ux` 누락만 usage 이고, 활성 웨이브 부재는 형제 명령 `evidence spec` 과 **같은 문장·같은 무게**로 설명하며 실제 플래그명 `--wave` 를 말한다 — `core/test/med-3i-messages.test.ts:99` |
| UX-146 | MED | 04 | **훅 차단을 조정하는 정책 config 키 4종이 어디에도 문서화돼 있지 않다** — `design_allowed_prefixes`·`design_blocked_bash`·`design_system_frozen_roots`·`block_raw_values` 가 README 4개 언어·스킬·agents 어디에도 없고 내부 감정 문서에만 존재한다. **차단의 입력을 사용자가 조정할 통로가 발견 불가능하다.** 프로파일 쪽이 `profile show`·problems·doctor drift 로 잘 노출되는 것과 대조적 | verified | measured | `core/src/config.ts:12` | **이름이 아니라 부류를 막았다** — `DEFAULT_CONFIG` 의 모든 키가 README 4개 언어에 있는지 전수 대조하는 테스트를 뒀다(키가 늘면 문서화 전까지 빨강). 함께 설정 절을 4개 언어에 신설해 8개 키와 `.harness/config.yaml` 경로·정책 변경 수용 절차를 적었다 — `core/test/doc-claims.test.ts:81` |
| UX-147 | MED | 04 | **`profile cmd` 실패 처방이 번들 플러그인 디렉토리를 가리킨다** — "set it in `<plugin>/profiles/generic/commands.yaml`" 라고 안내하는데, 실측상 프로젝트 로컬 `.harness/profile/commands.yaml` 이 **항상 우선**한다. 플러그인 설치본을 고치면 업데이트에 유실되고 **다른 모든 프로젝트에 영향**을 준다. [UX-118] 이 파스 오류 노출은 고쳤지만 경로 안내는 그대로 남았다 | verified | measured | `core/src/profile.ts:328` | 처방이 **고쳐도 되는 곳**을 가리킨다 — 프로젝트 로컬 `.harness/profile/commands.yaml` 이 항상 우선하므로 그것을 먼저 안내하고, 로컬 프로파일을 실제로 쓰는 중이면 그 파일을 그대로 가리킨다. 플러그인 설치본 경로는 더 이상 안내하지 않는다 — `core/test/med-3i-messages.test.ts:121` |
| QUAL-148 | MED | 10 | **판정 근거표(`00-summary.md` 게이트 실측)가 낡아 자기 문서 안에서 모순된다** — G1 이 "**626 passed ×3, 27 파일**"(실제 1031/39) · G9 가 "폴백 재측정 대기 ⚠" · G10 이 "gitleaks 71 커밋"(감사 후반 82). 같은 문서 헤더와 대장 [PERF-26](verified)과 어긋나고, **"G1–G13 전부 ✅" 를 자기 근거표가 보여 주지 못한다.** 판정을 읽는 사람이 무엇을 믿어야 할지 알 수 없다 | verified | measured | `docs/release-readiness/2026-08-21/00-summary.md:332` | 세 행을 전건 **재측정해서** 맞췄다 — G1 `626 passed·27파일` → **1085 passed ×3 · 44파일**, G10 `gitleaks 71 커밋` → **148 커밋 no leaks**, G9 는 통과라고 적는 대신 **「표면 미기재로 판정 불가」**로 정직하게 바꿨다([PERF-139] 이 그 이유를 실측으로 들고 있다). 낡은 숫자를 지우는 것이 아니라 다시 재는 것이 처방이다 — `docs/release-readiness/2026-08-21/evidence/perf-139-latency.log:20` |
| UTIL-149 | LOW | 01 | P11 트랙 deny 문구가 실동작을 과대 서술 — 신규 `src/app.ts` Write 는 "this track only changes what the defect ledger lists" 로 거부되지만 **기존** `src/app.ts` Edit 는 결함 원장이 비어 있어도 무출력 허용된다. 실제 강제는 「신규 파일 생성 금지」뿐인데 원장 스코프 강제가 있는 것처럼 읽힌다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-1-utility.md:143` | **강제하지 않는 것을 강제한다고 말하지 않는다.** 실제 강제는 「신규 파일 생성 금지」 하나뿐임을 deny 문구에 명시하고, 기존 파일 편집이 실제로 통과하는 것을 끝단으로 확인했다 — `core/test/low-3i-guidance.test.ts:140` |
| UX-150 | LOW | 04 | `doc --help` 의 upsert 표기에 `--refs` 가 없다 — 스킬(`skills/phase-p10-harden/SKILL.md:73` 등)은 그것을 쓰고 CLI 도 정상 수용한다. help 만 본 사용자는 RTM 추적성 연결을 누락하게 된다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-1-utility.md:147` | `doc upsert` 도움말에 `--refs`(와 `--url`)를 실었다 — 스킬이 쓰고 CLI 가 이미 받던 인자다. help 만 본 사용자는 RTM 추적성 연결을 누락했다 — `core/test/prod-3i-surfaces.test.ts:94` |
| UX-151 | LOW | 04 | 깨진 `config.yaml` 이 무음으로 기본 프로파일에 폴백한다 — 사용자가 설정했다고 믿는 정책이 실제로는 안 걸린 상태로 조용히 진행된다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-1-utility.md:151` | 판정 경로(훅)의 무해 계약은 그대로 두고, **진단 경로(doctor)** 가 `inspectConfig` 로 사실을 본다 — 프로파일이 `inspectProfile` 로 이미 쓰는 형태다. 강제가 사라진 것보다 **사라진 줄 모르는 것**이 나쁘다 — `core/test/low-3i-guidance.test.ts:109` |
| SEC-152 | LOW | 06 | `NotebookEdit` 의 `notebook_path` 가 구축·출하 트랙에서 미판정 — 다른 쓰기 도구와 달리 대상 경로가 판정에 올라가지 않는다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-2-effectiveness.md:66` | 대상 추출을 `file_path ?? notebook_path` 로 넓혔다 — **도구를 목록에 넣는 것과 그 도구의 대상을 아는 것은 다른 일이다**([SEC-135] 와 같은 부류). 3트랙 × 보호파일·소스 노트북 거부 · 정상 노트북 과차단 0 — `core/test/sec-3i-residuals.test.ts:32` |
| SEC-153 | LOW | 06 | dangling symlink 는 `realpath` 가 해석되지 않아 판정 사각이 남는다 — 링크 대상이 아직 없으면 보호 경로 대조를 비껴간다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-2-effectiveness.md:66` | 끊긴 심링크에서 `readlinkSync` 로 **링크 대상까지 풀어** 판정에 올린다. 대상이 없다고 안전한 게 아니다 — 그 링크로 쓰면 **파일이 생긴다**(정책 파일이 아직 없는 새 프로젝트가 정확히 그 경우다). 무해한 곳을 가리키는 링크는 그대로 통과 — `core/test/sec-3i-residuals.test.ts:53` |
| SEC-154 | LOW | 06 | 번들 프로파일 디렉토리(루트 밖)로의 쓰기가 판정되지 않는다 — 플러그인 설치본을 고치면 전 프로젝트에 영향인데 훅이 보지 않는다. [UX-147] 과 같은 뿌리 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-2-effectiveness.md:66` | 번들 프로파일을 **정책과 같은 등급**으로 올렸다(페이즈 무관 deny) — 소스 글롭·배포 명령을 정의하니 훅 판정의 **입력**이고([SEC-69] 의 논리), 루트 밖이라는 이유로 Bash 경로에서 통과하고 있었다. 그 편집은 이 머신의 **모든 프로젝트**에 영향을 주고 플러그인 업데이트에 사라진다. 거부문은 고쳐도 되는 곳(`.harness/profile/`)을 가리킨다. 플러그인 밖 루트 밖 쓰기는 예전대로 통과(과차단 0) — `core/test/sec-3i-residuals.test.ts:76` |
| ENG-155 | LOW | 03 | 재생 evidence 폴드([LOGIC-21])가 무테스트 — `doctor --repair` 후 증거 등급이 사라지는 **과거 실사고**의 재발을 아무 테스트도 못 잡는다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-3-engineering.md:70` | [LOGIC-21]·[LOGIC-56] 의 재발을 무는 테스트를 뒀다 — `state.json` 삭제 후 `doctor --repair` 로 재생해도 `evidence`·`submittedAt` 이 살아남고, 무효화된 게이트가 승인으로 되살아나지 않는다 — `core/test/eng-3i-residuals.test.ts:40` |
| ENG-156 | LOW | 03 | `.harness/` 무조건 허용 가드가 무테스트 — 과차단 방향이라 위협모델 밖이지만, 주석이 「자물쇠가 된다」고 경고해 둔 바로 그 절이 무가드다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-3-engineering.md:71` | `.harness/` 산출물 쓰기가 4트랙 전부에서 열려 있는지 전수 대조 + **의도된 경계**(설계 문서는 역행 없이 못 고친다)를 함께 고정했다 — 경계를 안 적어 두면 다음 사람이 「.harness 는 다 열려 있다」로 읽고 그 절을 지운다 — `core/test/eng-3i-residuals.test.ts:71` |
| ENG-157 | LOW | 03 | **낡은 헤더 주석 2곳이 현재 사실과 정반대다** — `gate.ts:41-43` 은 「replayState 가 evidence·submittedAt·invalidated 를 폴드하지 않는다」, `ship.ts:39-41` 은 「defect-added 등 미등록」이라 적어 두었는데 `events.ts` 에 전부 구현·등록돼 있다. 계약 문서가 거짓이면 다음 수리자가 이미 있는 것을 다시 만든다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-3-engineering.md:72` | 머리말 두 곳을 현재 사실로 고쳤다. **이름이 아니라 부류를 막았다** — 코드가 실제로 발행하는 이벤트가 전부 `EVENT_TYPES` 에 있는지 전수 대조하고, 머리말이 배선 상태를 **긍정으로** 적는지 본다(부정 패턴으로 잡으면 수정 이력 설명까지 걸린다) — `core/test/eng-3i-residuals.test.ts:113` |
| ENG-158 | LOW | 03 | `mcp/server.js`(JSON-RPC 전송 ~180줄)를 참조하는 테스트가 하나도 없다 — 얇지만 알림·버퍼링·감시 로직이 있다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-3-engineering.md:73` | `mcp/server.js` 를 실제 프로세스로 띄워 JSON-RPC 왕복을 잰다 — initialize 응답 · tools/list 에 도구 적재 · **깨진 줄 뒤의 정상 요청도 응답**(세션 불파괴 계약) — `core/test/eng-3i-residuals.test.ts:161` |
| COST-159 | LOW | 05 | [PERF-26] 이 넣은 fast-path 가 all-replay 저널에서는 naive 보다 **느리다**(역행) — 최적화가 최악 입력에서 비용을 더 쓴다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-5-cost.md:93` | 정규식을 `indexOf` 우선 탐색으로 바꿔 최악 입력 비용을 줄였다(역행 +20% → +15%). **남은 역행은 없앤 척하지 않는다** — 사전 필터는 걸러 낼 것이 없는 입력에서 반드시 손해이고, 그것이 본질이다. 실측표(현실 분포 15.9ms vs 51.5ms · 최악 54.9ms vs 47.6ms)를 코드 계약으로 박아, 최악 분포가 흔해지면 판단을 뒤집도록 했다 — `core/src/events.ts:98` |
| UX-160 | LOW | 04 | UX `wave complete` 거부 문구가 0바이트 증거가 있을 때도 "there is no visual evidence" 로 동일하다 — 정확한 원인을 주는 `evidence check <wave>` 를 가리키지 않는다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:49` | 거부 사유 판별을 `files` 가 아니라 새 `entries`(본 항목 수)로 바꿨다 — `files` 는 0바이트·끊긴 심링크·서브디렉토리를 담기 전에 거르므로 **뭔가 넣은 사람에게도 「증적이 없다」**가 나갔다. 정말 빈 디렉토리에만 그 문구가 나가는 것까지 함께 고정했다 — `core/src/evidence.ts:74` |
| UX-161 | LOW | 04 | 1×1·70바이트 캡처의 경고가 `wave complete` 시점에 안 나오고 P12 `ship verdict` 에서야 NO-GO 로 나온다 — 정산 시점에 말해야 할 것이 출하 시점으로 밀린다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:50` | 같은 처방으로 닫힌다 — 1×1 캡처가 이제 `wave complete` 에서 사유와 함께 거부되므로 경고가 P12 `ship verdict` 로 밀리지 않는다 — `core/src/evidence.ts:129` |
| UX-162 | LOW | 04 | `design sync --from <없는 파일>` 이 가공 없는 `ENOENT: no such file or directory` 원시 에러를 낸다 — 다른 명령의 세공된 에러와 대조적 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:51` | 없는 파일에 대해 세공된 오류와 처방을 낸다 — 어느 파일인지(절대경로) · 왜 파일로 받는지(코어는 네트워크를 안 쓴다) · 어떻게 고치는지(`--from <파일>`) — `core/test/ux-3i-next-move.test.ts:81` |
| UX-163 | LOW | 04 | doctor 경고 "1 hook decision failure(s) recorded — find out why" 가 로그 위치(`.harness/.runtime/hook-errors.log`)를 안 알려 준다 — README 지원 표에는 있는 경로다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:52` | 경고에 `hook-errors.log` 절대경로를 실었다 — 경로는 README 지원 표에만 있었고 doctor 를 돌린 사람은 그 표를 안 보고 있다. **처방은 손이 닿는 곳에 있어야 한다** — `core/test/ux-3i-next-move.test.ts:93` |
| UX-164 | LOW | 04 | 배포성 명령 deny 한 줄에 탈출 경로가 없다 — 어느 페이즈·게이트가 열어 주는지 무언급이라 다른 deny 의 풍부함과 비대칭 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:53` | 설계·구축 트랙 deny 에도 탈출 경로를 실었다(「출하 트랙 P10 이후, 게이트 승인 시 열린다」 + `harness status`). 탈출 경로 없는 deny 는 강제를 끄고 싶게 만든다 — `core/test/low-3i-guidance.test.ts:52` |
| UX-165 | LOW | 04 | 플래그 명칭 불일치 — help 는 `--acceptance`, 문서 전체는 `--accept`(둘 다 동작하나 별칭 미문서). `evidence spec` 에러는 "pass waveId explicitly" 라고만 하고 실제 플래그명 `--wave` 를 안 쓴다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:54` | `evidence spec` 오류가 실제 플래그명 `--wave` 를 쓰고(영/한 양쪽), help 가 `--acceptance|--accept` 로 별칭을 밝힌다 — `core/test/low-3i-guidance.test.ts:172` |
| UX-166 | LOW | 04 | 이중 트랙 혼란 — 코어 플로우 안내대로 `gate submit --paths` 만 쓰면 리뷰 패킷 머리에 "No artifact is registered … this packet is not grounds for approval" 이 **항상** 박힌다. doc 레지스트리(`doc upsert`+`doc url`) 병행이 전제인데 최상위 help·코어 플로우 어디에도 그 연결 안내가 없고, 그 상태로 `gate approve` 는 성공한다 — 패킷과 게이트가 서로 모순된 신호를 준다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-6-usability.md:56` | **제출 시점에 두 표면을 잇는다** — 등록 문서가 없으면 「패킷이 승인 근거가 아니라고 적을 것」과 잇는 방법(`doc upsert` → 발행 → `doc url`)을 그 자리에서 말한다. 판정은 패킷과 **같은 한 벌**(`docsForPhase`)을 쓴다. 강제를 새로 만들지는 않았다 — 그건 별개 결정이다 — `core/test/low-3i-guidance.test.ts:155` |
| PROD-167 | LOW | 07 | 배포본 위생 자기 기준([PROD-113])이 미완 — 한국어 내부 스펙·플랜(`docs/superpowers`)과 한국어 개발 레시피(`.claude/skills/verify`)가 export-ignore 누락으로 배포본에 잔존한다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-7-product.md:78` | `docs/superpowers` 와 `.claude` 를 export-ignore 에 추가했다 — [PROD-113] 이 세운 기준에 이 둘이 빠져 있었다. **실측**: `git archive HEAD` 산출물에서 두 경로 파일 수 **0** — `.gitattributes:7` |
| PROD-168 | LOW | 07 | `lang: ko` 옵션이 README 4종(한국어판 포함) 어디에도 없다 — `--help` 푸터에만 존재해 문서만 읽는 사용자는 존재를 모른다 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-7-product.md:81` | 같은 설정 절이 `lang` 키를 4개 언어 전부에 적는다(`lang: ko` 포함) — 이제 `--help` 푸터에만 있지 않다 — `README.md:201` |
| PROD-169 | LOW | 07 | `agents/readiness-auditor.md` 가 `verifying-production-readiness` 스킬을 두고 "It is already installed on this machine" 이라고 **단정**한다 — 신선한 설치에서는 거짓이고 README 「알려진 한계」의 정직 공지와 자기모순이다. ※ 스킬 자체의 동봉은 사용자 결정(출하 직전 동봉 예정)이므로 결함은 이 단정문에 한정 | verified | measured | `docs/release-readiness/2026-08-21/round3i/appraisal2-7-product.md:77` | 「It is already installed on this machine」 단정을 지우고 **동봉되지 않는다는 사실**을 적었다. 더해 로드 실패 시 **대체 절차를 지어내지 말고 멈추라**는 지시를 넣었다 — 기억으로 재구성한 rubric 의 판정은 없느니만 못하다(판정처럼 읽히기 때문이다) — `agents/readiness-auditor.md:19` |
| SEC-170 | BLOCKER | 06 | 🔴 **`cd` 한 줄로 설계 게이트·저널·정책 파일 보호가 전부 풀린다** — `tee .harness/events.jsonl` 은 DENY 인데 `cd .harness && tee events.jsonl` 은 **ALLOW**. 같은 파일인데 보호 목록을 **리터럴 상대경로**로 대조해서 생긴 구멍이다. 열거 밖 도구(`xxd`·`openssl`)는 슬래시 있는 토큰만 보는 안전망에 기대는데 `cd` 가 그 안전망을 통째로 끈다. 제품 표면 실측: `cd src && xxd -r -p p.hex app.ts`·`cd .harness && xxd … state.json`·`… config.yaml`·`cd .harness && tee events.jsonl` **전건 ALLOW**. [SEC-135]·[SEC-136]([SEC-69]) 재개방 | verified | measured | `core/src/bashwrite.ts:64` | **지난 네 번은 열거의 실패였고 이번은 정규화의 실패다** — 도구를 더 열거해도 안 닫힌다. 세그먼트를 걸으며 가상 cwd 를 추적해 대상을 판정 **전에** 정규화한다. `cd` 대상을 정적으로 못 읽으면(`cd $D`) 버리지 않고 `unresolvedTargets` 로 올려 **하네스 소유 파일 이름**만 막는다(과차단 최소화). 실측: 9벡터 전건 DENY · 동적 `cd` 3형태 DENY · 정상 작업 9건 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-4-value.md:104` |
| PROD-171 | MED | 07 | **실재하지 않는 MCP 도구 2종을 4개 언어가 광고한다** — `harness_gate_verify`·`harness_doc_upsert`. MCP `tools/list` 실측 16종에 없다(같은 기능은 CLI 에만 있다). **축 1·6·7 세 감정자가 각자 찾았다** — 사람이 읽고 「있다」로 적는 방식이 세 번 실패했다는 뜻이다 | verified | measured | `core/test/doc-claims.test.ts:77` | 이름을 고치는 대신 **부류를 막는다** — README 의 `harness_*` 이름을 기계로 뽑아 서버 도구 목록과 전수 대조하고, 개수 표기도 함께 고정한다. 도구가 늘거나 이름이 바뀌면 문서가 먼저 깨진다. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:110` |
| ENG-172 | MED | 08 | **「이 명령이 실행되는가」 판정이 두 벌이고 이미 갈렸다** — 설계 트랙 빌드 차단만 `cmd.includes` 였다(다른 곳은 `runsCommand`). 그래서 언급만 해도 막히고, 공백을 두 번 주면 안 막혔다(`npm  run  build`). 엔지니어링 축의 **하드 조건「중복 규칙 0」 위반**이라 잔여가 아니라 조건 미충족이다. [EFF-108] 이 배포 명령에서 고친 것과 같은 부류인데 빌드 쪽만 남아 있었다 | verified | measured | `core/src/hook.ts:1321` | `runsCommand` 한 벌로 합쳤다. 실측: 언급 통과 · 이중 공백 차단 · 설계 트랙 차단 유지 · 구축 트랙 통과. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-3-engineering.md:1` |
| EFF-173 | MED | 02 | **게이트 승인 뒤에도 정상 배포가 「신규 파일」로 오차단** — `docker push registry.io/app:v1` 의 이미지 참조가 경로 안전망에 잡혀 출하 트랙의 신규 파일 금지가 발화했다. 존재하지 않는 「파일」을 사유로 드는 거부라 사람을 엉뚱한 곳으로 보낸다. 같은 부류로 `@scope/pkg`·URL 도 잡혔다. **과차단은 이 제품에서 결함과 같은 무게다** | verified | measured | `core/src/bashwrite.ts:810` | 정규식이 `:`·`@` 에서 끊겨 토큰만으로는 못 가리므로 **앞뒤 한 글자**를 본다. 실측: 이미지 참조·URL·스코프 패키지 통과 · 진짜 경로(`src/app.ts`·`.harness/events.jsonl`) 그대로 차단 · P12 승인 후 배포 통과. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-2-effectiveness.md:65` |
| PROD-174 | LOW | 07 | **`[Unreleased]` 변경분이 `0.0.1` 이름표로 출하된다** — CHANGELOG 는 「`--version` 을 버그 리포트에 붙여라」라고 하는데, 라운드 3-I 수정 전·후 두 배포본이 같은 `v0.0.1` 을 말한다. 버그 리포트의 버전 식별력이 없다 | verified | measured | `CHANGELOG.md:9` | **사용자가 `0.1.0` 으로 확정했다**(2026-08-23). `package.json`·`plugin.json`·`marketplace.json` 을 맞추고 CHANGELOG 의 `[Unreleased]` 를 `[0.1.0]` 으로 끊었다. `repository`·`bugs` 도 함께 채웠다 — **주소를 지어내지 않고 사용자가 만든 비공개 저장소를 적었고, 그 저장소가 비공개라는 사실을 README 4종에 명시했다.** 실측: `harness --version` → `king-wjang-harness v0.1.0`. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:117` |
| SEC-175 | MED | 06 | **깊이 캡은 fail-open, 형제인 크기 캡은 fail-closed — 같은 버그를 하나만 고쳤다** — [SEC-137] 로 64KB 캡은 「못 읽으면 거부」가 됐는데 깊이 캡(`depth>=3`)은 조용히 빈 결과를 반환했다. 실측: `a.sh→b.sh→c.sh→d.sh` 의 `d` 가 저널을 써도 **ALLOW**, 70KB@깊이4 도 ALLOW(깊이 컷이 `statSync` 전에 리턴해 `unread` 에도 안 실린다). 파일을 한 겹 더 겹치는 것만으로 [SEC-137] 이 되살아난다 | verified | measured | `core/src/hook.ts:684` | 형제 캡에 **같은 처방**을 적용한다 — 상한을 넘으면 「거기에 스크립트가 있는데 안 읽었다」를 사실로 올리고 거부한다. 사유 문구는 크기와 나눈다(사람이 고쳐야 할 것이 다르다). 실측: 4겹 저널쓰기 DENY · 3겹은 본문 판정 그대로 · README 「알려진 한계」 4개 언어 정정. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-5-cost.md:88` |
| UTIL-176 | MED | 01 | **승인된 설계의 무감지 개정** — `phase set` 은 앞으로 갈 때만 게이트를 검사한다. 뒤로 갈 때는 검사할 것이 없어 조용히 통과했고, 승인된 게이트는 그대로 남으므로 **P7 → P3 에서 설계를 고치고 아무 기록 없이 되돌아오는** 경로가 자연 명령 두 줄로 성립했다. 「승인된 설계 위에서만 빌드된다」는 중심 보증이 빈다 | verified | measured | `core/src/cli.ts:568` | 새 강제를 만들지 않는다 — 역행용 명령은 **이미 있다**(`harness backtrack`: 사유를 받고 저널에 남기고 STALE 전파). 후진 `phase set` 을 그 문으로 보낸다. 실측: P7→P3 거부·상태 불변 · 거부문이 실재 명령을 가리킴 · `backtrack` 정상 동작 · 같은 페이즈는 역행 아님. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-1-utility.md:107` |
| COST-177 | MED | 05 | **폴백이 가장 필요한 순간에 가장 느리다 — 손상 저널에서 문턱의 4~20배** — 사전 필터([COST-159])는 타입 문자열을 먼저 훑지만 **손상 줄은 언제나 프리필터를 빠져나가 `JSON.parse` 로 내려가 예외를 낸다**. 예외 비용이 줄 수에 비례해 쌓여 100k줄 전부 손상이면 인프로세스 p95 **573ms**(같은 머신 실측). 폴백은 바로 그 손상 상태를 살아남으려고 존재하는데, **G9 은 프리필터가 전부 건너뛰는 가장 친화적인 저널만 쟀다** | verified | measured | `core/src/events.ts:168` | 타입을 못 찾은 줄은 파싱하지 않는다 — `HarnessEvent` 는 `type` 을 반드시 가지므로 결과는 같고 예외만 없앴다. 같은 프로세스 전/후 실측: 50퍼센트 손상 **269.2 → 8.0ms** · 100퍼센트 손상 **573.1 → 11.6ms** · 현실 분포 14.2 → 14.6ms(무변). 게이트도 함께 고쳤다 — **G9 이 저널 부류마다** 재도록 `gates.md` 개정. 실측 `evidence/cost-177-journal-shapes.log` · 소스 `evidence/cost-177-bench.ts` · 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-5-cost.md:105` |
| COST-178 | LOW | 05 | **100퍼센트 상태전이 저널에서 폴백 p95 49.4ms — 문턱 50ms 코앞** — 사전 필터의 문제가 아니라 10만 건 **파싱 자체의 비용**이라 느린 머신에서는 넘는다. 줄이려면 저널 압축·스냅샷이 필요하다 | deferred | measured | `docs/release-readiness/2026-08-21/gates.md:84` | **도달 가능성으로 문턱을 나눴다** — 저널은 하네스 명령으로만 늘어나므로 10만 번의 상태 전이는 실제 프로젝트에서 도달할 수 없다. 그래서 문턱을 걸지 않고 **숫자를 기록**한다(`gates.md` G9 절). 저널 압축은 이 라운드 범위 밖. **「도달 불가라 문턱을 안 건다」는 우리가 하는 말이므로 다음 감정에서 이 구분 자체를 재검증받는다**([OPS-74]) |
| QUAL-179 | MED | 10 | 감정자 주장: 「외부 대장 lint 가 글자접미 ID 39행을 조용히 건너뛰면서 완전한 척 보고한다」 — **재현되지 않았다**(아래 참조) | rejected | measured | `docs/release-readiness/2026-08-21/round3j/appraisal3-4-value.md:106` | **재현 실패 — 결함이 아니다.** 감정자는 lint 의 행 매칭을 `[A-Z]*-[0-9]*` 로 가정하고 에뮬레이션했으나, 실제 스크립트는 `[A-Z]*-[A-Za-z0-9]*` 라 **글자접미 ID 도 이미 검사한다**(`ledger-lint.sh:287`). 실측: 대장 데이터 행 **211개 중 lint 검사 211개**(글자접미 21개 포함) — **사각 0**. 메인 세션이 감정 보고를 **재현 없이 등재한 것이 잘못**이었고([OPS-74] 의 반대 방향 — 감정자 보고도 재현해서 받아야 한다), 이 행이 그 기록이다 |
| PROD-180 | MED | 07 | **「Measured」 표의 수치를 배포본만으로 재현할 수 없다** — 벤치 스크립트·저널 픽스처·방법론(n·백분위·구성)이 출하되지 않고 근거 로그는 `export-ignore` 다. 인프로세스 수치는 계측 지점 자체가 노출되지 않는다. 수치의 정직성과는 별개로 **제3자 재현 불가** | verified | measured | `README.md:119` | **받는 사람이 돌릴 수 있는 벤치를 패키지에 실었다** — `npm run bench:hook`(`scripts/bench-hook-latency.mjs`, 빌드·의존성 추가 없음). 저널을 세 부류(현실·손상·적대)로 합성해 **실제 훅 프로세스**를 재고 이 머신의 `node` 기동 바닥값을 함께 찍는다. 실측(`evidence/prod-180-hook-latency.log`): 현실 **+25.1ms** · 손상 **+12.0ms**(문턱 50ms 안) · 적대 +110.4ms(기록만). README 4종에 재현 절차를 적고, **문서가 가리키는 npm 스크립트가 실재하는지**를 테스트로 고정했다. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:111` |
| PROD-181 | MED | 07 | **대표 마케팅 실험(두 에이전트 A/B)에 기록이 없다** — README 헤드라인 절의 근거인데 방법론(모델·과제·시행 수)이 미기재이고, 배포본·리포 evidence 트리·문서 어디에서도 측정 기록을 찾지 못했다 | verified | measured | `README.md:41` | **주장을 지우지 않고 그 주장이 무엇인지 정확히 적었다** — 「각 조건 1회, 방법론 미기록, 측정이 아니라 일화」. 없는 기록을 지어내지 않고, 벤치마크처럼 읽히던 것을 멈췄다(4개 언어). 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:112` |
| UX-182 | LOW | 03 | **게이트 거부문의 괄호가 처방과 다른 게이트를 말한다** — 「가장 앞의 것부터: `gate submit P0`」이라고 하면서 상태는 바로 앞 게이트 것을 보여 준다(`P0, P1 (P1 is currently: pending)`). 막힌 사람이 두 이름 사이에서 헤맨다 | verified | measured | `core/src/gate.ts:884` | 한 문장 안의 두 이름을 같게 맞췄다 — 처방이 가리키는 게이트의 상태를 보여 준다. 실측: `P0 is currently` 로 바뀌고 `P1 is currently` 는 사라짐. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-6-usability.md:60` |
| UX-183 | LOW | 03 | **`ship defect add --evidence` 가 존재하지 않는 경로도 조용히 받는다** — 대장은 「measured 근거」를 세는데 가리키는 곳이 없는 경로가 그 자리에 들어가면 **집계는 정직해 보이고 내용은 비어 있다** | verified | measured | `core/src/cli.ts:243` | 거부하지 않고 **사실만 말한다** — 근거가 다른 저장소·로그·URL 을 가리키는 정당한 경우가 있고, 등재 자체를 막으면 사람이 대장을 안 쓴다(그러면 대장이 0 이 된다). 실측: 없는 경로 경고 · 있는 경로·URL·산문에는 침묵. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-6-usability.md:60` |
| PROD-184 | LOW | 07 | **같은 README 안에서 토큰 수치가 어긋난다** — 헤드라인 「~0 context tokens」(2곳) vs measured 표 「~240 tokens」. 둘 다 참인 주장이지만(규칙이 프로세스 밖이라 늘지 않는다 vs 세션당 주입량) 읽는 사람에게는 모순이고 **광고 쪽이 더 눈에 띈다** | verified | measured | `README.md:10` | 헤드라인을 실측값으로 바꾸고 「늘지 않는다」는 참인 부분만 남겼다(4개 언어). **부류를 막는 검사**도 넣었다 — 한 문서 안의 토큰 수치가 전부 같아야 한다. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:115` |
| PROD-185 | LOW | 07 | **설정 표가 강제를 과대 광고한다** — `design_allowed_prefixes` 행이 「이 접두사 밖은 전부 거부」라지만 실측은 설계 트랙에서 `tests/login.test.ts`·`tailwind.config.js` 를 허용한다(막는 것은 **구현 소스**다). `lang` 행은 「MCP 포함 모든 문구」라는데 MCP 거부문은 영어로 남는다. 「Triple enforcement」의 훅 계층은 **기본 off** 인데 무조건처럼 읽힌다 | verified | measured | `README.md:225` | 세 곳 다 실측대로 고쳤다(4개 언어) — 훅 자신의 거부문·주입 문구는 원래 규칙을 옳게 설명하고 있었고 **README 표만 어긋나 있었다**. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-7-product.md:114` |
| ENG-186 | LOW | 08 | **해시 규율이 두 벌이고 정책 쪽은 무테스트** — `computePolicyHash` 와 `computeArtifactHash` 에 경로·길이·내용 구분자 규율이 복제돼 있고 공유 헬퍼가 없다. 정책 쪽 길이 구분자를 제거한 뮤테이션이 **전 스위트 초록으로 생존**했다 — 한쪽만 고쳐지면 조용히 갈린다 | verified | measured | `core/src/policy.ts:97` | 규율을 `core/src/hash.ts` **한 벌**로 모으고 두 호출자가 같은 함수를 쓴다. 그 한 벌에 테스트를 걸었다 — 경계 없는 이어붙임·길이 구분자 제거·「못 읽음 vs 비어 있음」·결정성 4건. 감정자의 생존 뮤테이션이 겨눈 자리가 이제 테스트에 걸린다. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-3-engineering.md:1` |
| ENG-187 | LOW | 08 | **G9 게이트가 실행 가능한 형태로 없다** — 측정 스크립트·저널 생성기가 커밋돼 있지 않고 저널 규격이 과소결정(100k줄 ≠ 15MB)이며 차이-of-p95 의 노이즈 규칙이 없다. **누가 다시 재도 같은 답이 나오는지** 보장되지 않는다 | verified | measured | `docs/release-readiness/2026-08-21/evidence/cost-177-bench.ts:1` | 부분 처치: [COST-177] 벤치 소스를 커밋하고(`evidence/cost-177-bench.ts`) 저널 부류·표본 수·워밍업·측정 표면을 로그 머리에 적었다. **CI 자동 검사로 만드는 것은 별개 결정**이라 `gates.md` 에 남긴다. 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-3-engineering.md:1` |
| VAL-188 | LOW | 10 | **요약과 대장이 어긋난다** — `00-summary.md` 는 [QUAL-E] 를 rejected 라 하는데 대장 상태 칸은 verified 였고, deferred 집계도 낡아 있었다. G10 근거는 148 커밋(실제 171) | verified | measured | `docs/release-readiness/2026-08-21/ledger.md:140` | 대장 상태를 rejected 로 맞추고 요약의 deferred 서술을 5건으로 갱신했다. G1·G10 근거표는 **지우지 말고 다시 재서** 맞췄다(1223×3·53파일 · gitleaks 171커밋 5.06MB). 발견 `docs/release-readiness/2026-08-21/round3j/appraisal3-4-value.md:108` |
| SEC-194 | BLOCKER | 06 | 🔴 **글롭 경로가 코어·정책 파일 보호를 통째로 회피한다** — `printf x >> .harness/events.jsonl` 은 DENY 인데 `printf x >> .harness/e*.jsonl` 은 **ALLOW**. `cp /tmp/badcfg .har*/config.yaml` 로 정책을 갈아 끼우면 **강제 자체가 풀린다**. 사슬: 저널 위조 → `doctor --repair` → **사람·제출 없이 게이트 approved** → `npm publish` DENY→ALLOW. **뿌리는 [SEC-170] 과 같다(정규화 실패)** — 구현자가 `cd` 별칭만 닫고 글롭 별칭을 남겼다. **다섯 라운드 연속 목록 밖 BLOCKER** | verified | measured | `core/src/hook.ts:901` | **표기를 하나씩 세는 대신 질문을 바꿨다** — 「이 대상이 보호 파일을 **어떤 표기로든** 지목하는가」. 글롭은 정적으로 못 펴지만 **역방향은 된다**: 대상을 패턴으로 보고 **보호 목록을 그 패턴에 대조**한다. 과차단은 구조적으로 없다(보호 파일에 안 맞는 글롭은 그대로 통과). 판정 함수 한 벌에 둬서 Write·리다이렉트·명령 대상·안전망이 전부 같은 규칙을 탄다. 실측: 6벡터 전건 DENY(`cd`+글롭 조합 포함) · 정상 글롭 3건 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-2-effectiveness.md:1` |
| SEC-195 | BLOCKER | 06 | 🔴 **`gate approve` 사람 잠금이 바이너리 리네임 + PTY 로 뚫린다** — 훅은 하네스 호출을 **이름으로** 알아보고([SEC-138] 1겹), CLI 는 TTY 를 요구한다(2겹). 실측: `cp <plugin>/core/dist/cli.js /tmp/x.js`(ALLOW) → `python3 -c 'import pty; pty.spawn(["node","/tmp/x.js","gate","approve","P0"])'`(ALLOW) → **승인**. **복사본은 다른 이름이고, PTY 는 TTY 다** — 두 겹이 각각 한 줄에 무너진다 | verified | measured | `core/src/hook.ts:884` | 이름을 더 세는 대신 **사본을 만드는 행위를 막는다** — 판정기의 프로그램은 피판정자의 복제 영역 밖에 있어야 한다([SEC-154] 와 같은 논리). 복사 부류 명령에서만 발화해 **정상 직접 호출**(`node cli.js status`, [SEC-96] 이 연 조회 경로)과 읽기는 그대로 둔다. 실측: 복사 3형태 DENY · 직접 호출·읽기·무관 복사 과차단 0. **남는 한계는 [SEC-196] 으로 분리해 적었다**. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-2-effectiveness.md:1` |
| SEC-196 | MED | 06 | **패키지를 새로 설치해 그 사본을 돌리는 길은 훅이 막지 못한다** — [SEC-195] 는 설치본 복사를 막지만, `npm i` 로 받은 다른 사본은 이름도 경로도 다르다. 이름 기반 인식의 근본 한계다 | deferred | code | `core/src/hook.ts:1037` | **막을 수 있는 척하지 않는다.** 이 지점의 최종 방어는 **호스트의 권한 다이얼로그**이고, 그것이 「최종 클릭은 항상 사람」의 실제 근거다 — 제품은 **가장 싼 우회를 없애는 것**까지 한다. 훅 주석과 README 「알려진 한계」에 그대로 적었다. 이 리포의 강제는 **에이전트 레인의 규율이지 보안 경계가 아니다** |
| UTIL-189 | HIGH | 01 | **공식 역행 흐름이 순환 막다른 길** — `backtrack` 은 마커를 세우고 「`phase set` 을 실행하라」고 안내하는데, [UTIL-176] 이 넣은 후진 거부가 **마커를 조회하지 않아** 다시 「`backtrack` 을 쓰라」고 답한다. 몇 번을 반복해도 페이즈가 안 움직인다. 훅 거부문·SKILL·README 가 전부 이 흐름을 권하므로 **에이전트는 지시를 따를수록 루프를 돈다**. **구현자가 만든 회귀다** | verified | measured | `core/src/cli.ts:568` | 마커가 그 페이즈를 가리키면 **그것이 곧 「사유가 기록된 역행」**이므로 통과시킨다. 마커는 도착해도 **안 지운다** — 동결된 디자인 시스템을 여는 것도 이 마커이고, 역행의 목적은 도착이 아니라 개정이다. **교훈: 「A 를 막고 B 로 보낸다」는 처방은 B 의 왕복을 테스트로 고정해야 완성이다.** 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-6-usability.md:1` |
| PROD-190 | MED | 07 | **광고된 재현 도구 `npm run bench:hook` 의 출력이 한국어 고정** — 영문 README 4종이 이 명령을 광고하는데, 수치 해석에 필요한 문맥(측정 표면·게이트·적대 부류 제외 사유)이 전부 비영어다. 제품 기본 출력은 영어라는 조건 위반 | verified | measured | `scripts/bench-hook-latency.mjs:76` | 영문 기본 + `HARNESS_LANG=ko` 로 제품 규칙과 같게 맞췄다. **광고된 재현 절차가 읽히지 않으면 재현 절차가 아니다.** 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-7-product.md:1` |
| PROD-191 | MED | 07 | **벤치가 문턱만 말하고 판정을 안 해 부하 머신에서 「가짜 낙제」를 만든다** — 감정자 실측: 부하 중 델타 **+162.0ms**(문턱의 3.2배로 읽힘) vs 유휴 **+30.8ms**. `loadavg` 를 찍어 놓고 해석하지 않는다. 첫 사용자는 「이 제품은 자기 게이트를 3배 초과로 낙제한다」고 결론 내린다 | verified | measured | `scripts/bench-hook-latency.mjs:84` | PASS/FAIL 판정 + **부하 경고**(코어의 절반을 넘는 load 면 「유휴에서 다시 돌려라」)를 넣었다. 부하 중 초과는 `FAIL` 이 아니라 `over — machine busy` 로 찍는다. **재현 도구는 오독도 막아야 한다.** 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-6-usability.md:1` |
| PROD-192 | LOW | 07 | 음수 델타가 `+-2.0ms` 로 인쇄된다 | verified | measured | `scripts/bench-hook-latency.mjs:122` | 부호를 값에서 가져온다. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-7-product.md:1` |
| UX-193 | LOW | 03 | **역행을 끝내는 문이 안 보인다** — `harness backtrack` 을 인자 없이 부르면 「어느 페이즈인가?」만 답하고, 마커를 닫는 `backtrack clear` 는 어느 메시지에도 없다 | verified | measured | `core/src/cli.ts:1066` | 인자 없는 호출의 안내에 `harness backtrack clear` 를 넣었다 — 시작하는 문만 있고 끝내는 문이 안 보이면 사람이 갇힌다. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-6-usability.md:1` |
| QUAL-197 | MED | 10 | **라운드 3-K 잔여 — 대부분 닫았고 남은 목록을 여기 적는다.** 등재·봉인한 것: BLOCKER 3([SEC-194]·[SEC-195]·[SEC-198]) · HIGH 1([UTIL-189]) · MED 6 · LOW 7. **아직 안 본 것**: 축1 LOW2(설계 트랙에서 프로젝트 내 스크립트 실행의 거부 **사유**가 오도) · LOW3(테스트 파일 deny 문구 자기모순) · LOW4(증거 거부 사유가 실패 기준을 반만 말함) · LOW6/축3 LOW7(벤치가 G9 두 표면 중 하나만 잰다) · 축3 MED5(델타 추정기 노이즈 지배) · 축6 D5~D9 · 축7 K3(벤치 픽스처 크기가 README 「15MB」와 다름)·K4(미출하 스펙 절 번호 인용)·K8. **그리고 가성비 축(5)은 3-K 에서 안 돌았다** | open | measured | `docs/release-readiness/2026-08-21/round3k/appraisal4-1-utility.md:1` | **대장이 완전한 척하지 않게 이 행을 연 채로 둔다.** 「등재된 것이 전부」로 읽히면 그것이 지난 다섯 라운드가 반복한 실패다. 남은 것은 전부 LOW 이거나 측정 방법 개선이고 **강제·판정에 닿지 않는다** — 그래서 3-L 재감정을 막지 않는다. 3-L 이 이 목록을 다시 짚으면 그때 함께 닫는다 |
| SEC-198 | BLOCKER | 06 | 🔴 **대상 경로 자체의 변수가 코어·정책·저널 쓰기를 통째로 우회한다** — `D=.harness; echo '<phase-set P7>' >> $D/events.jsonl` → ALLOW → `doctor --repair` → **phase P7 · 에이전트 자기승격** → 소스 쓰기 개통. [SEC-170] 의 동적 가드는 **`cd` 대상에만** 걸려 있었고 대상 자체의 변수는 리터럴 취급됐다. **같은 부류의 세 번째 표기**(리터럴 → `cd` → 글롭 → 변수) | verified | measured | `core/src/bashwrite.ts:121` | 동적 값이 섞인 대상은 **어디에 떨어지는지 알 수 없다**는 사실을 그대로 올린다 — `resolveIn` 이 `$`·백틱을 만나면 미해결로 돌려 [SEC-170] 의 이름 기반 net 에 태운다. 실측: 변수 4형태 DENY · `build/$NAME.log`·`$HOME/notes.md` 과차단 0(하네스 소유 이름이 아니다). 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-4-value.md:1` |
| ENG-199 | MED | 08 | **셸 unwrap 목록이 두 벌이고 이미 갈렸다 — 하드 조건 「중복 규칙 0」 위반** — `INTERPRETERS`(볼 수 없는 실행)와 `commandLines` 안의 하드코딩 배열(감싼 명령 꺼내기)이 서로 다른 셸 집합을 봤다. 실측: `fish -c 'npm publish'`·`ash -c …`·`busybox sh -c …` 가 **설계 트랙 배포 차단을 우회**했다 — 꺼내는 쪽이 모르는 셸은 안쪽을 아예 안 본다 | verified | measured | `core/src/bashwrite.ts:306` | `SHELLS_TAKING_C` 한 벌로 모으고 양쪽이 그것을 쓴다. **드리프트를 구조로 막는 테스트**도 걸었다 — 목록의 모든 셸에 대해 `commandLines` 가 안쪽을 꺼내는지 전수 확인. 실측: 4형태 DENY · `sh -c 'npm test'` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-3-engineering.md:1` |
| QUAL-200 | MED | 10 | **시각 증거 게이트가 시각 산출물을 요구하지 않았다** — 검사는 `.png` 에만 걸려 있었고 그 밖의 확장자는 **무검증으로 `usable`** 이었다. 실측: `echo 'not an image' > notes.txt` 로 `wave complete` 성공, `evidence check` 는 그 txt 를 `usable` 로 분류. 거부문은 「스크린샷」, README 는 「그리지 않은 UX 는 출하할 수 없다」 — **세 표면이 서로 다른 말을 했다** | verified | measured | `core/src/evidence.ts:473` | 게이트를 여는 자격을 **제품이 이름 붙인 산출물**로 좁혔다 — 이미지(png·jpg·webp)와 내보낸 HTML(Claude Design 흐름이 말하는 둘). 다른 파일은 함께 있어도 되지만 캡처를 대신하지 못한다. 실측: txt 단독 `usable []`·`ok false` · 400×300 PNG 통과 · HTML 통과. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-6-usability.md:1` |
| ENG-201 | MED | 08 | **G9 게이트가 자동 검사 불가능한 형태** — 동봉 벤치가 항상 exit 0 이라 사람이 표를 읽고 직접 채점해야 했다. 게이트를 「문서에만 있는 기준」으로 두면 회귀는 사람이 안 볼 때 지나간다 | verified | measured | `scripts/bench-hook-latency.mjs:147` | 문턱을 넘으면 **exit 1**. 부하 중 초과는 실패로 세지 않는다(그때는 측정 머신을 재는 것이므로) — 대신 그 사실을 출력에서 말한다([PROD-191]). 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-3-engineering.md:1` |
| VAL-202 | LOW | 10 | **요약·대장 서술이 낡았다** — 대장 헤더 괄호가 「라운드 3-I 완료」인데 3-J·3-K 행이 뒤에 붙었고, 요약표 G10 커밋 수도 밀렸다(결과 0 leaks 동일) | verified | measured | `docs/release-readiness/2026-08-21/ledger.md:3` | 헤더 서술과 G10 커밋 수를 현재로 다시 쟀다. **숫자는 지우지 말고 다시 재서 맞춘다.** 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-4-value.md:1` |
| UTIL-203 | LOW | 01 | **G9 의 record-only 사유가 과장돼 있다** — 「10만 번의 상태 전이는 실제 프로젝트에서 **도달할 수 없다**」는 기계적 보증이 아니다. 저널에 상한도 속도 제한도 없고, `node bump` 한 번이 N 개 웨이브를 STALE 로 만들 듯 **한 명령이 여러 이벤트를 쓰는 경로**도 있다 | verified | measured | `docs/release-readiness/2026-08-21/gates.md:84` | 「불가능」을 「현실적으로 도달하기 어렵다 — 상한이 있는 것은 아니다」로 다시 적었다(gates.md·벤치 출력 양쪽). **문턱을 안 거는 근거가 과장이면 그 게이트의 판정도 과장이다.** 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-1-utility.md:1` |
| SEC-204 | LOW | 06 | **탈출구 env 검사가 부분 문자열로 매치된다** — `/HARNESS_ALLOW_FORCE/` 가 `HARNESS_ALLOW_FORCED_MIGRATION` 같은 무관한 이름까지 막는다(실측). 그리고 그 백스톱 절 자체가 **뮤테이션에서 생존**했다 — 다른 규칙이 먼저 잡아 주고 있어 그 절만 발화하는 입력이 테스트에 없었다 | verified | measured | `core/src/hook.ts:1206` | 이름 **전체**로 매치하게 좁히고, **그 절만 발화하는 입력**으로 테스트를 걸었다(`HARNESS_ALLOW_FORCE=1 ./somescript.sh` 등 3형태 DENY · 유사 이름 과차단 0). [ENG-107] 과 같은 교훈 — 픽스처가 그 절을 실제로 발화시키는지 확인해야 한다. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-3-engineering.md:1` |
| PROD-205 | LOW | 07 | **`HARNESS_LANG` 이 `--help` 와 코드에는 있는데 README 4종에 없다** — 있는 기능을 부정하는 방향의 어긋남 | verified | measured | `README.md:221` | 설정표 `lang` 행에 「한 번만 바꾸려면 `HARNESS_LANG=ko`」를 4개 언어에 넣었다. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-7-product.md:1` |
| ENG-206 | LOW | 08 | **낡은 주석: 재생 대상 타입 「8개뿐」** — 실제 `REPLAY_TYPES` 는 9개다. 두 벌 목록에 붙은 숫자는 언제나 낡는다 | verified | measured | `core/src/events.ts:138` | **개수를 주석에 박지 않는다** — 목록 이름을 가리키게 바꿨다. 발견 `docs/release-readiness/2026-08-21/round3k/appraisal4-3-engineering.md:1` |
| SEC-207 | BLOCKER | 06 | 🔴 **명령치환·중괄호 표기로 저널·정책·상태 위조 → 게이트·락 자기해제(E2E)** — `echo '{…phase-set P7…}' >> $(echo .harness)/events.jsonl` → ALLOW → `doctor --repair` → **phase P7**, 이어서 `src/app.ts` 쓰기 ALLOW. 배포판은 `tee -a .harness/{events,_x}.jsonl` 로 P10 gate-approved 를 위조해 `npm publish` DENY→ALLOW. **여섯 라운드, 여섯 번째 표기다** — 앞의 다섯(리터럴·`cd`·글롭·변수·복사)과 달리 이 표기는 **대상 토큰 자체를 부숴서** 판정에 아예 도달하지 않는다 | verified | measured | `core/src/hook.ts:1007` | **표기를 세는 것을 그만두고 「추출의 실패」를 감지한다** — 변형 명령의 텍스트에 하네스 소유 파일 이름이 보이는데 뽑아낸 대상 중 그 이름으로 끝나는 것이 하나도 없으면, **그 이름이 어디로 갔는지 모른다**. 모르는 것은 통과가 아니다. 과차단은 둘로 좁혔다 — **정적 확장 문자가 있을 때만** 발화하고(깨끗한 경로는 정상 판정으로), **중괄호는 펴서** 본다. 실측: 5벡터 DENY · `build/{a,b}.log`·`app/config.yaml` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-2-effectiveness.md:1` |
| SEC-208 | HIGH | 06 | **[SEC-195] 의 복사 차단이 도구 블랙리스트라 인터프리터 한 줄로 우회된다** — `python3 -c "open('/tmp/x','w').write(open(CLI).read())"` · `ruby -e 'IO.write(…)'`. **같은 파일에서 같은 실수(열거)를 두 번 했다** | verified | measured | `core/src/hook.ts:924` | 열거를 지우고 규칙을 뒤집었다 — **변형 명령이 하네스 프로그램을 언급하면 거부하고, 예외는 「인터프리터의 실행 대상으로 오는 한 형태」뿐**(`node …/cli.js status`, [SEC-96] 이 연 경로). 「목록에 있으면 막는다」가 아니라 「정당한 한 형태만 통과」라서 새 도구가 생겨도 기본값이 안전하다. 언급은 **원문 전체**에서 뽑는다 — 괄호 안 경로는 세그먼트 분해가 토막 내기 때문이고, 그것이 이 우회의 실제 원리였다. 실측: 3형태 DENY · 직접 호출·읽기·무관 명령 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-2-effectiveness.md:1` |
| EFF-209 | MED | 02 | **설계 트랙에서 읽기 전용 `sed -n`·`awk`·`perl` 의 소스 조회가 과차단된다** — 조회인데 변형으로 분류된다. 과차단은 이 제품에서 결함과 같은 무게다(사람이 질려서 하네스를 끄면 방어는 그 순간 0) | verified | measured | `core/test/med-3j-residuals.test.ts:420` | **처방은 이미 나와 있었다 — [SEC-221] 이다.** 「모든 형태에서 조회인 도구」와 「플래그로 갈리는 도구」를 나눈 순간 `sed -n`·`awk`·`perl` 의 조회 형태가 조회로 돌아왔다. 제품 표면에서 13벡터 재측정: 조회 7건 전부 통과 · 변형 6건 전부 차단(`.ts`·`.sql` 양쪽, 설계 트랙 P0). **고침이 아니라 못을 박았다** — 과차단을 막으려 목록을 다시 손대면([EFF-214] 가 만든 사고가 그것이다) 이 테스트가 먼저 깨진다 |
| QUAL-210 | MED | 10 | **라운드 3-L 이 한도로 4축에서 중단됐다** — 완료: 축2 실효성 **2.5** · 축7 상품성 **4.8**(처음으로 기준선 도달). 중단: 축1·3·4·6. **가성비 축(5)은 착수도 못 했다**. 이 대장에 등재된 3-L 결함은 축2·축7 것뿐이다 | open | measured | `docs/release-readiness/2026-08-21/round3l/appraisal5-7-product.md:1` | **대장이 완전한 척하지 않게 이 행을 연 채로 둔다.** 한도가 풀리면 중단된 4축 + 가성비 축을 다시 돌린다. 축2·축7 이 낸 것은 위/아래 행에 등재했다 |
| PROD-211 | LOW | 07 | **동봉 벤치로 재현 불가한 광고 수치가 남아 있다** — README 「Measured」의 **인프로세스** p95(2.6/18.9ms)는 벤치가 wall-time 한 표면만 재서 대조할 수 없다. 저널 「(15 MB)」 크기도 출력만으로 대조 불가. 벤치 스크립트·dist 번들에 한국어 주석 잔존 | verified | measured | `scripts/bench-hook-latency.mjs:77` | **동봉 벤치가 게이트에 적힌 대로 두 표면을 다 재게 했다.** G9 은 처음부터 「인프로세스·프로세스 wall-time **두 표면 모두**」인데 실려 나간 벤치는 wall-time 만 쟀다 — 그래서 README 의 인프로세스 수치를 **받는 사람이 다시 잴 수 없었다**. 자식 프로세스에서 번들의 `run(["hook","pre-tool"], root)` 를 직접 호출해 잰다(이 프로세스가 번들을 `require` 하면 벤치가 자기 자신을 오염시킨다). 유휴 머신 실측(load 3.32 · node 바닥 39.8ms): 인프로세스 정상 0.9ms · 폴백 18.6ms(**+17.7ms**) · wall 77.0 → 101.7ms(**+24.7ms**) — 둘 다 문턱 50ms 충족. README 4개 언어의 수치를 **이 실행이 찍은 값으로** 바꿨다 |
| PROD-212 | LOW | 07 | **벤치가 부하 중에도 PASS 를 그대로 찍는다** — 초과는 「machine busy」로 눌러 주면서 통과는 부하 여부와 무관하게 `PASS`. 부하 중 PASS 는 근거가 약한데 같은 무게로 읽힌다 | verified | measured | `scripts/bench-hook-latency.mjs:197` | **부하 표시를 양쪽에 똑같이 붙였다.** 초과만 「머신 부하」로 유보하고 충족은 그냥 `PASS` 로 찍으면, 바쁜 머신에서 돌린 사람이 **통과만 확정으로 읽는다**. 부하는 델타를 부풀리기도 하고 줄이기도 한다 — 신뢰할 수 없는 것은 판정 자체다. load 24.66/10코어에서 실측: 세 부류 모두 「pass — machine busy」로 표기됐고, 머리말에도 「충족도 포함해 전부 부하 표기」를 적었다 |
| SEC-213 | BLOCKER | 06 | 🔴 **이름을 조립하면 리터럴이 사라진다 — 일곱 번째 표기** — `a=events; b=.jsonl; echo FORGED >> .harness/$a$b` 가 ALLOW. 소유 파일 이름이 텍스트 어디에도 없어 [SEC-170]·[SEC-207] 의 이름 기반 안전망이 전부 조용했다. `config`·`state`·`src` 도 동일하고, `sed -i .harness/$f` 로 정책 파일 제자리 편집까지 성공 | verified | measured | `core/src/hook.ts:192` | **리터럴은 사라져도 디렉토리는 남는다.** 미해결 대상의 **정적 접두**를 잘라 그 자리의 이름 모를 파일을 **같은 판정 함수**로 물어본다. 거기에 더해 보호 파일이 사는 디렉토리(`OWNED_DIRS`, `CORE_FILES` 에서 파생)에서는 「모르는 이름」을 거부한다. 실측: 5벡터 DENY · `.harness/evidence/$w/shot.png`·`build/$NAME.log`·`docs/$TOPIC.md` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-1-utility.md:3` |
| SEC-216 | BLOCKER | 06 | 🔴 **경로를 인코딩해 숨긴 쓰기 — 여덟 번째 표기** — `p=$(base64 -d <<< <base64>); printf … >> $p` 가 ALLOW. 정적 성분이 **하나도** 없어 리터럴 이름도 디렉토리 접두도 남지 않는다. 위조 → `doctor --repair` → P10 gate approved → `npm publish` DENY→ALLOW 까지 E2E 재현 | verified | measured | `core/src/bashwrite.ts:468` | 두 가지를 한다. ① **볼 수 있는 대입은 편다** — 같은 명령 안 리터럴 대입과 훅이 실제로 아는 환경변수(`$HOME` 등)를 치환해 **정상 판정**으로 보낸다(과차단을 줄이는 쪽이 먼저다). ② **끝까지 안 펴지는 쓰기는 거부한다** — 「볼 수 없는 쓰기」는 이 리포가 `opaqueExec`(볼 수 없는 실행)에 이미 적용한 태도와 같은 부류다. 실측 13/13: 8표기 전건 DENY · `LOG=build/out.log; … $LOG`·`$HOME/notes.md`·조회·백업 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-4-value.md:3` |
| EFF-214 | MED | 02 | **저널을 읽는 것까지 막혔다** — `sed -n '1,5p' .harness/config.yaml`·`awk 'NR<3' …`·`perl -ne print …`·`cp .harness/events.jsonl /tmp/backup.jsonl` 전부 DENY(같은 파일 `cat` 은 ALLOW). 이름만으로 `mutating` 을 세운 탓이고, 백업 거부문은 「can only be **changed**」라 사실까지 오도했다. **「디버깅으로 저널을 읽는 것은 정당하다」는 이 파일 자신의 원칙과 정면으로 어긋났다** | verified | measured | `core/src/bashwrite.ts:30` | `sed`·`awk`·`perl` 을 이름 목록에서 빼고 **제자리 편집(`-i`)일 때만** 변형으로 본다. 그리고 이름 기반 안전망 둘 다 **대상이 하나도 안 뽑혔을 때만** 발화하게 했다 — 대상이 뽑혔다면 스캐너가 그 명령의 모양을 이해했다는 뜻이고 남은 언급은 대개 읽는 쪽이다. 실측: 조회·백업 5건 통과 · `sed -i`·프로그램 내부 `>`·`find -exec sed -i` 그대로 차단. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-1-utility.md:3` |
| EFF-215 | MED | 02 | **`find -exec` 의 「안쪽이 변형인가」를 이름 목록으로 물었다** — [EFF-214] 로 `sed` 가 목록에서 빠지자 이 절이 조용해졌고, 그동안 `find . -exec sed -i … {} +` 를 막아 온 것은 **`s/a/b/` 를 가짜 경로로 오인한 우연**이었다(테스트가 그 우연을 고정하고 있었다) | verified | measured | `core/src/bashwrite.ts:468` | 안쪽 명령을 **같은 스캐너에 통째로** 넘긴다 — 이름으로 다시 묻지 않는다. 가짜 경로 대상도 사라졌다. **초록이 규칙이 옳다는 증거가 아닌 예**로 남긴다 |
| ENG-217 | MED | 08 | **셸 `-c` 해제 규칙의 세 번째·네 번째 사본** — [ENG-199] 가 두 벌을 `SHELLS_TAKING_C` 로 모았는데 `SCRIPT_RUNNERS`(스크립트 본문 검사)와 그 **정규식 안 리터럴 alternation** 이 더 있었고 갈려 있었다. `fish`·`ash`·`busybox` 가 빠져 그 셸들로 스크립트를 돌리면 본문 검사가 발화하지 않았다. **「모았다」는 주장은 모은 것만 말한다** | verified | measured | `core/src/hook.ts:733` | 셋 다 정본에서 **파생**시킨다 — 목록도 정규식도 손으로 다시 적지 않는다. 접두 명령(`sudo`·`busybox`)도 정본(`PREFIX_COMMANDS`)에서 생성해 붙인다. 실측: `busybox sh f.sh`·`sudo sh f.sh`·`fish f.sh` 전건 DENY · `cat f.sh` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-3-engineering.md:3` |
| ENG-218 | LOW | 08 | **해시 길이 구분자 뮤테이션이 여전히 생존** — [ENG-186] 이 건 가드 테스트가 **`\0` 구분자만으로도 갈리는 조합**이라, 길이를 지워도 초록이었다. 불변식을 재는 것이 아니라 우연히 통과하는 것을 재고 있었다 | verified | measured | `core/test/med-3j-residuals.test.ts:217` | **길이가 유일한 차이인 조합**으로 바꿨다 — 길이를 빼면 두 입력이 같은 바이트열이 되는 쌍. **가드 테스트도 「그 절만 발화하는 입력」이어야 한다**([ENG-107] 과 같은 교훈). 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-3-engineering.md:3` |
| SEC-219 | HIGH | 06 | **루트 밖 실행 스크립트를 안 읽고 fail-open** — 에이전트는 프로젝트 밖(`/tmp` 등)에 파일을 쓸 수 있고(그건 하네스 소관이 아니다), 거기 쓴 스크립트를 실행하면 본문 검사가 **조용히 건너뛰어졌다**. 실측: `echo '… >> .harness/events.jsonl' > /tmp/x.sh; sh /tmp/x.sh` → ALLOW. 「못 봤으면 통과」 부류의 마지막 남은 자리 | verified | measured | `core/src/hook.ts:723` | **건너뛴 이유는 타당했다** — 루트 밖 시스템 스크립트를 전 규칙으로 판정하면 과차단이 폭발한다. 그래서 규칙을 없애는 대신 **판정 범위를 좁혔다**: 읽기는 하되 **하네스 소유 파일을 건드리는지 하나만** 본다. 실측: 저널 쓰는 밖 스크립트 2형태 DENY · `echo hello` 밖 스크립트·없는 파일 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-5-cost.md:1` |
| COST-220 | LOW | 05 | **「비간섭 비용 0」 표현이 엄밀하지 않다** — 실제로는 호출당 ~7ms 프로세스 wall(sh 게이트 기동)이 든다. 출력 0바이트·파일 생성 0·토큰 0 은 사실이지만 **시간은 0 이 아니다** | verified | measured | `README.md:120` | 「침묵을 반환한다」 뒤에 **드는 것은 시간**임을 적었다 — 출력·파일·컨텍스트 토큰은 0 이고, 셸 게이트가 Node 기동 전에 **p95 ~4ms**(실측 p50 3.7 · p95 4.7)로 빠져나간다. 4개 언어 반영. 발견 `docs/release-readiness/2026-08-21/round3l/appraisal5-5-cost.md:1` |
| SEC-221 | BLOCKER | 06 | 🔴 **「읽기로 분류된 쓰기 도구」로 코어·정책·상태 파일 도달 — 아홉 번째 부류** — `READ_ONLY_HEADS` 에 `yq`·`sort`·`awk` 가 있었는데 셋 다 **쓰기 플래그가 있다**: `yq -i '.x=1' .harness/config.yaml` 한 줄로 정책 파일이 열렸다(끝단에서 트랙 해제·페이즈 위조 실증). `awk` 는 [EFF-214] 에서 **구현자가 직접 넣은 것**이다 — 과차단을 고치다 구멍을 만들었다 | verified | measured | `core/src/bashwrite.ts:437` | **목록의 의미를 바꿨다** — `READ_ONLY_HEADS` 에는 **모든 형태에서 조회인 것만** 둔다. 쓰기 형태가 하나라도 있는 도구는 `CONDITIONAL_WRITERS` 에 **그 형태와 함께** 적고 그때만 변형으로 본다. 플래그 의미는 도구마다 달라(`grep -o` 는 출력 파일이 아니다) 일반 규칙으로 뭉갤 수 없고, 모르는 도구는 여전히 기본값이 「쓸 수 있다」다([SEC-B1]). 실측 11/11: 쓰기 형태 4건 DENY · 같은 도구 조회 형태 4건 통과 · `grep -o` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-2-effectiveness.md:1` |
| PROD-222 | MED | 07 | **MCP `serverInfo` 버전이 `0.0.1` 로 남았다** — 릴리스를 `0.1.0` 으로 끊으면서 이 한 곳이 낡았다. 버전을 **두 벌로 손 관리**한 결과다 | verified | measured | `mcp/server.js:17` | `package.json` 을 정본으로 읽는다 — 읽지 못하면 `unknown` 을 내고 **거짓 버전을 지어내지 않는다**. 실측: MCP `initialize` → `{name: king-wjang-harness, version: 0.1.0}`. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-7-product.md:1` |
| PROD-223 | LOW | 07 | **CHANGELOG `[0.1.0]` 안에 `### Added` 절이 둘** — 라운드 3-I 항목이 같은 릴리스에 합쳐지면서 중복처럼 읽힌다 | verified | measured | `CHANGELOG.md:114` | 뒤쪽 절을 「earlier in the same release (round 3-I)」로 이름 붙여 **어느 시점의 것인지** 드러냈다. 항목을 지우지 않는다 — 그것도 이 릴리스에 들어간 것이다 |
| PROD-224 | MED | 07 | **v0.1.0 이 고정되지 않았다** — 태그 0개이고 `main` 에는 제품이 없다. 버전만 올렸을 뿐 **릴리스가 존재하지 않는 상태**다 | open | measured | `CHANGELOG.md:13` | **푸시·태그·머지는 사용자 결정이다.** 원격 저장소는 만들어 뒀고(`https://github.com/wook8170/king-wjang-harness`, PRIVATE) `git remote` 등록·푸시·태그는 3-M 결과 확인 후 한 번에 한다. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-7-product.md:1` |
| PROD-225 | LOW | 07 | **README 4종의 「MCP 는 영어 유지·ko 문자열 미탑재」 주장이 실측과 어긋난다** — 번들에 ko 문자열이 일부 있거나 일부 표면이 번역된다 | verified | measured | `README.md:221` | 실측했다 — **주장의 본체는 참**이다: `lang: ko`·`HARNESS_LANG=ko` 양쪽에서 MCP 도구 설명·거절문이 **영어로 남는다**. 틀린 것은 괄호 안 부연(「ko 문자열이 번들에 없다」)이었다 — 번들에 한글 토큰 102개가 있다(소스 주석). **부연을 지우고 실측 사실로 바꿨다**(4개 언어). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-7-product.md:1` |
| ENG-226 | HIGH | 08 | **따옴표 안의 `&&`·`;` 가 분해 기준이 돼 래퍼 안쪽이 안 열렸다** — `sh -c 'cd src && echo x > app.ts'` 가 두 세그먼트로 갈려 **`cd` 추적도 대상 정규화도 일어나지 않았고** 설계 트랙 소스 쓰기가 통과했다. 감정자는 이것을 「셸 목록의 다섯 번째 사본(`case` 라벨)」으로 짚었는데, **재현해 보니 목록이 아니라 분해 자체가 원인**이었다 — `sh` 도 똑같이 뚫렸다 | verified | measured | `core/src/bashwrite.ts:136` | **토크나이저는 이미 따옴표를 존중한다 — 분해도 같은 규칙을 쓴다.** 라벨은 정본에서 생성할 수 없으므로 **드리프트를 테스트로 못 박았다**(정본의 모든 셸에 대해 안쪽이 열리는지 전수 검사). 실측: 4셸 전건 DENY · `echo 'a && b; c'` 과차단 0. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-3-engineering.md:1` |
| EFF-227 | MED | 02 | **`tmpfile=$(mktemp); echo x > $tmpfile` 이 전 페이즈에서 막혔다** — 셸에서 가장 흔한 관용구인데 [SEC-216] 의 「볼 수 없는 쓰기」 규칙에 걸렸다. **이 정도로 흔한 것이 막히면 사람이 하네스를 끈다** | verified | measured | `core/src/bashwrite.ts:522` | 값은 못 보지만 **어디에 떨어지는지는 안다** — `mktemp` 은 정의상 `TMPDIR` 아래다. 프로젝트 밖이므로 판정에서 허용으로 떨어진다. **모르는 것을 아는 척하지 않고, 아는 것을 모르는 척하지도 않는다.** 실측: 관용구 2형태 통과 · `$(base64 -d …)`·`$UNKNOWN` 은 그대로 DENY. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-1-utility.md:1` |
| COST-228 | MED | 05 | **명령 파싱 뜨거운 경로에 상한 없는 O(n²) — 타임아웃이 fail-open 이라 안전망이 통째로 꺼진다** — `pathLikeMentions` 의 탐욕 접두(`[A-Za-z0-9_.-]*\/…`)가 슬래시 없는 긴 낱말에서 매 위치마다 되짚는다. 실측(인프로세스): 2KB 2.6ms · 10KB **138ms** · 50KB **3495ms**. 커밋 dist e2e 로 30KB 명령에서 훅 **1223ms**, 외삽 100KB 에서 10초 타임아웃 초과 → **통과**. 이번 라운드가 넓힌 바로 그 경로이고, **동봉 벤치가 이 경로를 안 잰다** | verified | measured | `core/src/bashwrite.ts:963` | **슬래시를 먼저 찾고 거기서 뒤로 걸어** 접두를 모은다 — 각 문자를 상수 번만 본다. 실측 2KB 1.4ms · 10KB 2.8ms · 50KB **6.6ms**(530배) · 200KB 19.5ms **선형**. 추출 결과는 동일(판정 무변경). 「길이의 제곱」이라는 **부류**를 테스트로 고정했다. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-5-cost.md:1` |
| QUAL-229 | MED | 08 | **회귀 테스트가 규칙이 아니라 다른 절을 고정한다** — 감정자가 `OWNED_DIRS` 절을 `false` 로 꺼도 전 스위트가 초록임을 실증했다([SEC-213] 커버리지가 illusory). 구현자가 **그 절만 발화하는 입력을 찾으려 했으나 실패**했다 — 명령치환은 `opaqueExec` 가, 미정의 변수 형태는 경로 안전망이 **먼저** 잡는다(뮤테이션 전/후 판정 동일 실측) | verified | measured | `core/test/blocker-3j.test.ts:356` | **답을 냈다: 중복이었다.** 만들 수 있는 8개 벡터(정적 조립·명령치환·미정의 변수·하위 디렉토리·`.runtime`)가 **전부 다른 절에 먼저 걸렸다** — 절을 꺼도 16/16 동일. 그래서 **지웠다**. 발화하지 않는 방어는 유지 비용만 남기고 「그 절이 지킨다」는 착각을 만든다. 되살릴 근거는 하나뿐이다 — **그 절만 막는 입력을 실제로 보이는 것**(코드 주석에 그렇게 적어 뒀다). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-3-engineering.md:1` |
| ENG-230 | LOW | 08 | **셸 목록의 여섯 번째 사본** — `opaqueExecOf` 의 프로세스치환 감지 정규식에 `ash`·`busybox` 가 빠져 있었다. `ash <(echo x)` 로 「볼 수 없는 실행」 차단이 비껴갔다 | verified | measured | `core/src/bashwrite.ts:321` | 정본(`SHELLS_TAKING_C`)에서 정규식을 만든다 — 목록을 손으로 또 적지 않는다. **여섯 번째다** — 「모았다」고 적을 때마다 하나가 더 있었다. 실측: 정본 전 셸에서 `<(…)` DENY(테스트로 전수 고정) |
| EFF-231 | LOW | 02 | **`npm publish --dry-run` 이 배포로 막힌다** — 아무것도 게시하지 않고 **출하 전에 확인하려고** 쓰는 명령인데, 막으면 사람을 확인 없이 진짜 배포로 밀어 넣는다 | verified | measured | `core/src/hook.ts:1663` | dry-run 을 배포에서 뺀다. **판정은 줄 단위다** — 명령 전체에 한 번 걸면 `npm publish --dry-run; npm publish` 로 차단이 통째로 꺼진다(플래그 하나가 다른 줄을 사면하면 안 된다). 실측: dry-run 2형태 통과 · 사면 시도 DENY · 래퍼 배포 DENY · 조회 과차단 0 |
| SEC-232 | BLOCKER | 06 | **목적지-우선 플래그(`-t`/`--target-directory`)로 쓰기 대상 추출이 붕괴한다 — 열한 번째 부류.** `cp`/`install`/`ln`/`mv` 의 목적지를 「마지막 피연산자」로 고정해 뒀는데 GNU 의 `-t DIR` 은 목적지를 **앞에** 둔다. `cp -t .harness /tmp/config.yaml` 한 줄로 정책 파일이 덮이고, 끝단에서 설계트랙 해제·페이즈/게이트 위조까지 실증됐다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-2-effectiveness.md:1` · `round3n/appraisal7-4-value.md:89` — **최초 발견은 3-M 가치 축**이었고 그것이 등재되지 않았다([QUAL-234]) | verified | measured | `core/src/bashwrite.ts:SEC-232` | **위치가 아니라 플래그가 목적지를 정하게 했다.** `-t DIR`·`--target-directory=DIR`·`-tDIR` 을 해석해 **DIR 과 그 아래 생길 각 basename** 을 대상으로 올린다(디렉토리만 올리면 파일 단위 보호가 발화하지 않는다). `mv` 는 원본도 함께([SEC-101]). **`rsync`·`scp` 에는 적용하지 않는다 — 거기서 `-t` 는 `--times` 다**([SEC-221] 이 정리한 「플래그 의미는 도구마다 다르다」와 같은 선). 실측 13/13: 차단 8 · 과차단 0(정상 백업 `cp -t /tmp/bak src/app.ts` 통과) |
| SEC-233 | BLOCKER | 06 | **64KB 초과 페이로드가 훅을 통째로 fail-open 시킨다 — 표기가 아니라 판정 자체가 꺼진다.** `process.stdin.isTTY` 를 읽는 순간 node 가 fd 0 을 O_NONBLOCK 으로 바꾸고, 파이프 버퍼를 넘는 페이로드에서 `readFileSync(0)` 이 EAGAIN 을 던졌다. 그 예외를 바깥 catch 가 「stdin 없음」으로 삼켜 **빈 입력 = 무판정 = 통과**. 명령 뒤에 주석으로 64KB 를 붙이면 Bash·Write·Edit·`--force` 가 한꺼번에 열린다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-2-effectiveness.md:1` | verified | measured | `core/src/cli.ts:SEC-233` | 두 갈래로 고쳤다. ① `tty.isatty(0)` 로 묻는다 — fd 를 그대로 두고 묻기만 하므로 비블로킹 전환이 없다. ② stdin 을 **드레인**한다(EAGAIN 은 「아직 안 왔다」이므로 `Atomics.wait` 로 재시도, 상한 2초). ③ **못 읽었으면 거부한다** — 읽지 못한 호출은 통과시킬 수 있는 호출이 아니다([SEC-175] 가 스크립트 캡에 내린 것과 같은 결론). 범위 최소화: `.harness/` 가 있는 프로젝트의 `pre-tool` 에서만 거부하고 나머지는 기록만(비간섭·무해 불변식 유지). 실측: 66KB·200KB 코어파일/`--force` DENY · 300KB·400KB 정상 작업 ALLOW · 하네스 없는 프로젝트 침묵 |
| ENG-235 | HIGH | 03 | **셸 목록의 일곱 번째 사본 — 직접 실행 스크립트의 확장자 판정이 정본에서 갈렸다.** 확장자 네 개(sh·bash·zsh·ksh)가 손으로 적혀 `dash`·`fish`·`ash`·`busybox` 가 빠졌고, 확장자는 **이름일 뿐**이라 `#!/bin/sh` 본문을 `x.fish` 로 이름 붙이는 것만으로 [SEC-219] 의 본문 검사가 꺼졌다(fish 설치조차 불필요). 끝단: 저널 위조 후 하네스가 **스스로 권하는** `doctor --repair` 로 P0→P7 전진 실증. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-3-engineering.md:1` | verified | measured | `core/src/hook.ts:ENG-235` | `SHELLS_TAKING_C` 에서 파생한 `DIRECT_SCRIPT_EXT` 로 바꿨다 — 러너 형태는 이미 정본 파생이었는데 직접실행만 손 사본으로 남아 있었다. 정본 8셸 전수 테스트를 붙여 드리프트를 못 박았다(실측 8/8 DENY · 무해 스크립트 ALLOW) |
| ENG-236 | MED | 03 | **`--dry-run` 예외 규칙이 두 벌로 갈렸다.** [EFF-231] 이 훅에는 **줄 단위**로, 프로파일 경로에는 **명령 전체**로 구현해 `prisma migrate deploy --dry-run && prisma migrate deploy` 가 통과했다 — 훅 쪽 주석이 정확히 그 형태를 「차단이 통째로 꺼진다」고 경고하는데 형제 구현에 그 처방이 안 갔다. 표적이 운영 DB 마이그레이션이라 무게가 있다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-3-engineering.md:1` | verified | measured | `core/src/profile.ts:ENG-236` | `isDryRun` 을 `bashwrite` 정본으로 올리고 두 호출부가 같은 것을 쓰게 했다. 프로파일 판정도 `commandLines` 로 줄을 갈라 줄 단위로 본다. 실측: 세 구분자(`&&` · 세미콜론 · 논리합) 전부 DENY · 순수 `--dry-run` 은 ALLOW 유지 |
| QUAL-234 | MED | 10 | ★ **감정 보고서 세 장이 트리아지에서 통째로 빠졌고, 그 상태로 대장이 「open BLOCKER 0」을 말했다.** `round3l/appraisal5-6-usability` · `round3m/appraisal6-4-value` · `round3m/appraisal6-6-usability` 가 대장 인용 **0건** — 그중 3-M 가치 축이 낸 BLOCKER(`cp -t`)가 등재·기각·유예 어느 쪽도 아닌 채 **떨어졌다**. 대장이 거짓을 말하면 그것이 요약·README·커밋 메시지로 번진다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-4-value.md:1` · `round3n/appraisal7-6-usability.md:1` — 빠진 세 장은 `docs/release-readiness/2026-08-21/round3l/appraisal5-6-usability.md:1` · `docs/release-readiness/2026-08-21/round3m/appraisal6-4-value.md:1` · `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | verified | measured | `core/test/ledger-summary-sync.test.ts:QUAL-234` | 빠진 세 장의 결함을 **전건 등재**했고(아래 행들), 재발은 **기계로** 막는다: 모든 라운드 보고서가 대장에 최소 한 번 인용되는지 검사한다. 결함 하나하나를 세지 않는 이유는 그것이 사람의 판단이기 때문이다 — 기계가 확실히 셀 수 있는 것은 **보고서 한 장이 통째로 사라지는 것**이고, 실제로 일어난 실패가 정확히 그 부류였다 |
| ENG-237 | LOW | 03 | **`INTERPRETER_HEADS` 는 여덟 번째 셸 목록 사본이다** — 정본에서 파생되지 않는다. 지금은 실측 우회가 없으나 [ENG-235] 와 같은 부류이고, 같은 부류가 일곱 번 재발했다는 것이 이 행을 여는 이유다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-3-engineering.md:169` | open | code | `core/src/hook.ts:943` | 미착수 — 정본 파생으로 바꾸거나, 파생할 수 없는 이유를 코드에 적는다 |
| UTIL-238 | MED | 01 | **설계 트랙에서 `src/app.test.ts` 를 막으면서 거부문이 「테스트 이름 파일은 쓸 수 있다」고 광고한다** — 거부된 파일 자체가 그 규칙에 맞아 에이전트가 이름만 바꿔 재시도하는 헛수고 경로가 열린다. README 4개 언어도 「tests 는 안 막는다」를 조건 없이 말한다(실제로는 프로파일 소스 경로가 우선). 차단 자체는 옳다(`test/app.test.ts` 는 통과). 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-1-utility.md:1` · `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/hook.ts:1217` | 미착수 — 대상이 테스트 이름 규칙에 맞을 때는 거부문에 **우선순위 단서**를 붙이고, README 문구를 실제 규칙으로 좁힌다 |
| UTIL-239 | LOW | 01 | **`${TMPDIR:-/tmp}/x` 형태의 브레이스 기본값 쓰기가 전 페이즈에서 deny** — [EFF-227] 이 닫은 `mktemp` 관용구의 마지막 잔존형이다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-1-utility.md:1` | open | code | `core/src/bashwrite.ts:525` | 미착수 — `${VAR:-기본값}` 의 기본값을 정적 성분으로 인정한다(값을 몰라도 **어디에 떨어지는지는 안다**는 [EFF-227] 의 논리 그대로) |
| UTIL-240 | LOW | 01 | **`gate feedback --from` 이 상대경로를 cwd 기준으로 열고, 실패가 가공 없는 ENOENT 로 새어 나온다** — 다른 명령군은 프로젝트 루트 기준이라 같은 인자가 자리에 따라 다르게 해석된다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-1-utility.md:1` | open | code | `core/src/cli.ts:708` | 미착수 — 루트 기준으로 통일하고 실패를 사람 문장으로 바꾼다 |
| USE-241 | MED | 04 | **교차 그룹 플래그가 무음으로 삼켜진다 — 파서가 자기 문구를 위반한다.** `unknownFlags` 가 그룹별이 아니라 **전역** 어휘로 판정해 `harness wave create --goal g --reason r` 이 `--reason` 을 조용히 버리고 exit 0 으로 성공한다. 가드 자신이 「An unknown flag is never applied」라고 인쇄하는데 정확히 그 일이 일어나고, exit 0 이라 **알 길이 없다**. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/cli.ts:142` | 미착수 — `unknownFlags`/`nearestFlag` 에 **해당 명령군의 어휘**를 넘긴다(전역 어휘는 제안용 폴백으로만) |
| USE-242 | LOW | 04 | **`ship defect update --status` 의 광고 어휘와 파서 어휘가 다르다** — 도움말은 6종을 광고하고 파서는 4종만 받는다. 도움말을 믿고 친 첫 시도는 반드시 실패한다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/help.ts:182` | 미착수 — 도움말을 `DEFECT_STATUSES` 에서 **파생**한다(이 리포가 셸 목록에서 일곱 번 배운 것과 같은 교훈: 손 사본은 낡는다) |
| USE-243 | LOW | 04 | **`report packet` 의 무인자 usage 가 다른 명령을 가리킨다** — `harness evidence packet <phase>` 를 안내하는데 그 명령은 서식이 달라 그대로 치면 또 실패한다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/cli.ts:1052` | 미착수 — 명령 이름을 `harness report packet` 으로 고친다 |
| USE-244 | LOW | 04 | **필수 인자 누락을 값 오류로 오진하며 `undefined` 를 노출하는 명령 3개**(`node bump`·`doc url`·`design baseline`) — 형제 명령들은 「Missing argument — usage: …」로 정확한데 이 셋만 남았다. [USE-94] 가 세운 규칙의 잔여. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/cli.ts:428` | 미착수 — 인자 부재를 usage 분기로 선행 처리 |
| USE-245 | LOW | 04 | ★ **`harness hook <미지 이벤트>` 손 실행이 완전 침묵 exit 0** — 틀린 이벤트 이름을 친 그 자리에서 피드백이 0 이고 로그로만 남는다. **구현자 자신이 이번 라운드에 이 함정으로 「차단이 전부 풀렸다」는 잘못된 결론을 냈다** — 관측된 실제 비용이 있다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/cli.ts:329` | 미착수 — 「항상 exit 0」 계약은 플랫폼 호출용이고 플랫폼은 미지 이벤트를 보내지 않는다. **미지 이벤트일 때만** stderr 한 줄로 실제 이벤트 이름 넷을 안내한다(stdout 은 계속 침묵) |
| USE-246 | MED | 04 | **하네스 해제 시도를 오진하고, 실재하지 않는 처방을 가리키며, 진짜 문은 어디에도 안 적혀 있다.** `rm -rf .harness` 가 「state.json 을 손으로 고치지 마라」로 거부된다 — (a) 행위는 편집이 아니라 해제 (b) 해제하는 harness 명령은 없음 (c) 루브릭 개정 1 이 **의도된 사람 탈출구**로 남긴 것이 README 4개 언어·SKILL.md 어디에도 없음. **3-M 사용성 보고서가 이미 냈으나 등재되지 않았다**([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` · `docs/release-readiness/2026-08-21/round3n/appraisal7-6-usability.md:84` | open | code | `core/src/hook.ts:1078` | 미착수 — 거부문을 「해제」로 정확히 진단하고, 의도된 탈출구를 문서에 적는다 |
| USE-247 | LOW | 04 | **명령치환 거부문의 대상 표시가 한 글자로 뭉개진다** — `echo F >> $(echo .harness)/events.jsonl` 의 거부 사유가 무엇을 막았는지 보여주지 못한다. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `core/src/hook.ts:25` | 미착수 — 해석 불가 대상은 원문 조각을 그대로 인용해 보여준다 |
| USE-248 | LOW | 04 | **증거 0건 거부문이 스크린샷만 광고한다** — html 목업도 게이트를 여는데 첫 거부문은 절반만 말한다. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `core/src/wave.ts:299` | 미착수 — 게이트를 여는 증거 형태를 전부 적는다 |
| USE-249 | LOW | 04 | **SKILL.md 의 UX 게이트 설명이 구판 계약을 말한다**(「크기 0 초과 파일」 — 실측상 txt 는 못 연다). [QUAL-200] 이 규칙을 바꿨는데 문서가 안 따라왔다. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `skills/king-wjang-harness/SKILL.md:98` | 미착수 — 실제 계약(이미지 치수 또는 내보낸 HTML)으로 갱신 |
| USE-250 | LOW | 04 | **SessionStart 「다음 수」가 트랙과 무관하게 고정 문구다** — 출하 트랙에서도 설계 트랙 안내가 나오고, 빌드·출하 트랙은 브리핑 자체가 없어 그 트랙의 실재 규칙을 **첫 거부로만** 배운다. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `core/src/hook.ts:534` | 미착수 — 트랙별 브리핑·다음 수를 낸다 |
| USE-251 | LOW | 04 | **증거 잣대가 두 벌인데 예고가 없다** — `wave complete` 는 html 목업을 인정하고 `ship verdict` 는 같은 웨이브에 실행 캡처를 다시 요구한다. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `core/src/ship.ts:605` | 미착수 — 두 잣대를 한 벌로 만들거나, 다른 이유를 앞에서 예고한다 |
| USE-252 | LOW | 04 | **`design inventory --from` 이 마커 없는 HTML 에 무설명 빈 결과 exit 0** — 「침묵 성공」 부류의 잔여. 3-M 이월([QUAL-234]). 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-6-usability.md:1` | open | code | `core/src/cli.ts:1160` | 미착수 — 마커가 없으면 그 사실을 말한다 |
| PROD-253 | MED | 07 | **배포본의 `package-lock.json` 이 아직 0.0.1 이다** — 일곱 번째 버전 표면이 릴리스와 다르고, 소스 설치 첫 걸음(`npm install`)에서 트리가 더럽혀진다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-7-product.md:1` | open | code | `package-lock.json:3` | 미착수 — 잠금 파일 버전을 0.1.0 으로 맞춘다(설치 첫 명령이 워킹트리를 바꾸지 않게) |
| PROD-254 | LOW | 07 | **「MCP 도구 설명·거절문은 영어로 남는다」의 「거절문」이 실측보다 넓은 읽기를 허용한다** — [PROD-225] 가 좁힌 문구의 잔여. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-7-product.md:1` | open | code | `README.md:221` | 미착수 — 실측한 표면만 말하도록 한 번 더 좁힌다 |
| PROD-255 | LOW | 07 | **같은 릴리스의 두 표면이 다른 인프로세스 수치를 현재형으로 말한다** — README 는 새 실측을, CHANGELOG 는 옛 실측을 각각 현재형으로 적는다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-7-product.md:1` | open | code | `CHANGELOG.md:102` | 미착수 — CHANGELOG 는 그 시점 기록임을 분명히 하거나 같은 값으로 맞춘다 |
| PROD-256 | LOW | 07 | **문서가 인용하는 대상 문서가 배포본에 없다**(§4-3 인용) — `export-ignore` 로 빠지는 내부 문서를 배포되는 문서가 가리킨다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-7-product.md:1` | open | code | `docs/release-readiness/2026-08-21/gates.md:25` | 미착수 — 배포본에 남는 문서만 인용하거나 인용을 내재화한다 |
| PROD-257 | LOW | 07 | **배포된 메인 스킬이 배포본에 없는 `verify` 스킬을 가리킨다** — 3-M 이월·무변경. 발견 `docs/release-readiness/2026-08-21/round3m/appraisal6-7-product.md:1` · `docs/release-readiness/2026-08-21/round3n/appraisal7-7-product.md:1` | open | code | `skills/king-wjang-harness/SKILL.md:26` | 미착수 — 참조를 지우거나 그 스킬을 함께 싣는다 |
| QUAL-258 | LOW | 10 | **요약(`00-summary.md`)이 대장보다 낡는다** — 3-M 이 지적했으나 미수정. 동기화 스크립트가 있는데도 갱신 시점이 사람 손에 달려 있다. 발견 `docs/release-readiness/2026-08-21/round3n/appraisal7-4-value.md:139` | open | code | `docs/release-readiness/2026-08-21/00-summary.md:1` | 미착수 — 요약 동기화를 검증 한 벌에 넣거나 테스트로 고정 |
