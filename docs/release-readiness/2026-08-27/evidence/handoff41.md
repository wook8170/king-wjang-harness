
## 2026-08-28 (41) — ★ 수정 라운드 1·2 완료 · 차단 3건 닫음 · 판정 **출하 불가 → 조건부 출하 가능**

**정본.** `main` 로컬 작업 중(커밋 안 함) · `origin/main` = `bacb4bc` · **1465/1465 green · tsc 0** ·
로컬 태그 `v0.1.1`·`v0.1.2` 신규(**push 안 함**).
**아티팩트(원격에서 볼 것)**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### 판정 이동

**출하 불가(NO-GO) → 조건부 출하 가능(CONDITIONAL GO).** open BLOCKER **3 → 0**.
대장 159행(open 45 · verified 111+ · fixed 1 · deferred 1) · `vpr lint` R1–R13 통과.

### 라운드 1 — 차단 3건 (전부 verified · 변이 검증 완료)

| ID | 무엇이었나 | 어떻게 닫았나 |
|---|---|---|
| **API-01** | `2>&1` 하나로 **읽기 명령이 소스 쓰기로 오판** | `maskNonCommandText` — fd 복제·따옴표 heredoc 본문을 **길이 보존 마스킹** · `cd`·`pushd` 를 READ_ONLY_HEADS 에 · 자기해제 가드를 **줄 기반**으로. 과차단 **7/12 → 0/12**, 관통 0/8 유지 |
| **OPS-01** | 저널 손상이 UI 어디에도 안 보임 | 세션 시작에서만 저널 건강 확인(매 훅은 비용 때문에 안 함) · `stateDamaged` 로 두 문구 분리 |
| **OPS-02** | 번들 부재 시 **강제가 조용히 꺼짐**, `doctor` 는 ok:true | `bin/harness` catch 가 `hook-errors.log` 에 한 줄 — `doctor` 가 **이미** 그걸 센다(새 통로 안 만듦) |

**고치다 한 번 열었고 기존 테스트가 잡았다**: `` echo `harness … --force` `` (백틱 명령치환)이 통과했다.
`hook-pre-tool.test.ts` 「백틱 안 형태를 막는다」가 red 를 냈다. 닫고 회귀에 못 박음.

### 라운드 2 — 적색 게이트 5개 닫음

G3(번역 누락) · G5(폭 80 표 정렬 + 전각 폭) · G10(불변식 2건) · G12(태그·스키마) · G13(관측성 5건).
상세는 `fixes-round2.md` / `fixes-round2-ops.md`(병렬 작업자).

**주목**: 라운드 2 중 **벤치가 내 자기 회귀를 잡았다** — 자기해제 가드가 `commandLines` 를 3회 돌려
`cd-redirect` 가 1310ms(문턱 1000)를 넘겼다. 한 칸 캐시로 **375ms**.

### 다음 즉시 할 일 — 넷 중 셋은 **사용자만** 할 수 있다

1. **㉮ G7 훅 지연 실측** — `osascript -e 'quit app "OrbStack"'` 로 조용한 창을 만든 뒤
   `npm run bench:hook`. **3회 시도 모두 벤치가 「machine busy」로 인증 거부**했다(wall 델타
   −40.9 ~ +203.4ms 로 노이즈 지배, 음수가 그 증거). **in-process 는 3회 모두 +15.4~19.2ms 로
   안정·문턱 50ms 아래.** 38차부터 **네 세션째** 미결.
2. **㉯ API-04** — 같은 조용한 창에서 페이로드별 e2e 를 재고 그 실측으로 `MAX_BYTES`(현 4MB,
   `core/src/cli.ts`)를 다시 역산. **추측으로 정하지 마라** — 현 상한이 「1MB 당 1초」라는 낡은
   실측에서 나왔고 그게 이 결함의 원인이다.
3. **㉰ `git push --tags`** — 태그가 로컬에만 있어 원격 사용자는 아직 되돌아갈 곳이 없다.
   그리고 **작업 커밋도 아직 안 했다**(워킹트리에 라운드 1·2 변경 전부).
4. **㉱ LOGIC-02 잔여 — 제품 결정** — 에이전트가 `Write` 로 활성 웨이브 지시서를 덮을 수 있다
   (백업 없는 유일 파일). 막으려면 README 의 「`.harness/` 아래는 언제나 쓸 수 있다」를 함께
   고쳐야 한다. 광고를 바꿀지 파일을 열어 둘지는 사람이 정한다.

### 미해결·확인 대기

- **보안 계약 하나를 바꿨다(사용자 확인 필요)** — `policy.test.ts` 의 「인용부호 안의 언급도
  막는다 — 의도된 과차단」. 그 근거는 「좁히면 `bash -c` 가 열린다」였는데 **실측으로 반증**됐다
  (우회 20종 전부 계속 차단). 새 계약: **실행은 막고 기록은 막지 않는다.** 명령치환은
  인용부호 안에서도 실행이므로 계속 차단.
- **API-28**(env 리터럴 언급은 아직 과차단) · **SHIP-23**(배포본 테스트 수 기계 검증 없음) — LOW.

### 함정(이번 확인)

- **`core/src` 를 고치면 선행 대장(2026-08-21)의 `file:line` 인용이 밀린다.** `VAL-C` 가 잡는다.
  `docs/release-readiness/2026-08-27/evidence/reanchor.js` 가 HEAD 판 내용으로 자동 재앵커한다
  (README·CHANGELOG 도 범위에 넣어 뒀다). 소스를 고칠 때마다 돌려라.
- **테스트 파일을 추가하면 README 4개의 계량 표기를 함께 고쳐야 한다**(`doc-claims.test.ts`).
  그래서 라운드 2 회귀는 **한 파일에 모았다**.
- **게이트 산출물은 공백 제외 80자 이상**이어야 제출된다 — 테스트 픽스처가 짧으면 red.
- **역행(backtrack)에서는 드리프트 검사를 돌리면 안 된다** — 고치러 가는 길을 막아 사용자를
  가둔다. `UTIL-189` 회귀가 이것을 잡았다.
- **`npm test` 를 서브에이전트와 동시에 돌리지 마라** — 측정 위생. ⑨결정성은 중앙에서 한 창에.
