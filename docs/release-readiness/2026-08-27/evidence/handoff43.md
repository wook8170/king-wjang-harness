
## 2026-08-28 (43) — ★ **G7 통과** (유휴 창 실측) · OPS-06 봉인 · 라운드 3 진행 중

**정본.** 워킹트리 작업 중(**커밋 안 함**) · `origin/main` = `bacb4bc` · 로컬 태그 `v0.1.1`·`v0.1.2`.
**아티팩트**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### ★ G7 이 닫혔다 — 네 세션 만에

38차부터 미결이던 훅 지연 게이트를 **유휴 창에서 실측**했다. 방법: 벤치의 busy 판정 기준이
`load > 코어수 × 0.5`(이 머신 = **5.0**)임을 코드에서 확인하고(`scripts/bench-hook-latency.mjs:137`),
**부하가 그 아래로 내려갈 때까지 기다렸다가** 돌렸다(load 3.95, 「machine busy」 표기 **0**).

| | 폴백이 더하는 지연 | 판정 |
|---|---|---|
| realistic in-process | **+15.2ms** | PASS |
| realistic wall-time | **+32.9ms** | PASS |
| corrupt in-process | **+10.7ms** | PASS |
| corrupt wall-time | **+22.2ms** | PASS |

명령 파싱 6개 형태도 전부 PASS. 증거: `evidence/g7-bench-idle.log`.
**교훈: 「측정 불가」로 넘기기 전에 도구의 판정 기준을 읽어라 — 기다리면 되는 것이었다.**

### 이번에 추가로 닫은 것

- **[OPS-06] HIGH** — 거대 저널 + 손상된 state 조합에서 재생이 10초 예산을 넘겨 **훅이 죽고,
  죽은 훅은 통과**였다(감사 실측 532MB → 12.4초). 재생 전에 크기를 보고 상한(**128MB**, 실측
  43MB/s 에서 역산)을 넘으면 **재생을 포기하되 통과시키지 않는다** — 페이즈를 모르므로 가장
  제한적인 상태로 판정하고, 읽기와 harness 명령은 열어 둬 `doctor --repair` 로 빠져나갈 수 있다.
  회귀 테스트는 **희소 파일**로 크기만 만든다(600MB 를 실제로 안 쓴다). 변이 검증 red 확인.

### 진행 중

- **서브에이전트(라운드 3)**: `[OPS-08]` 저널 비밀 마스킹 · `[API-03]` config 미지 키 침묵 무시.
  작업 파일 `events.ts`·`config.ts`·`doctor.ts`·`loop.ts`, 테스트 `core/test/ops-round3-2026-08-27.test.ts`,
  보고서 `fixes-round3-ops.md`. **`hook.ts` 는 오케스트레이터 소유로 분리**.

### 다음 즉시 할 일

1. 에이전트 회수 → **전체 스위트** → README 4개 계량 갱신(테스트 파일·수가 또 늘었다).
2. **G7 재측정** — OPS-06 이 `hook.ts` 를 건드렸으므로 조용한 창에서 한 번 더(정상 경로엔
   영향 없지만 폴백 경로에 stat 한 번이 늘었다).
3. 대장·요약 갱신 → **open HIGH 0 이면 「출하 가능(GO)」 판정 가능.**
4. **커밋 + `git push --tags`** — 30+ 파일이 워킹트리에 있고 태그도 로컬뿐. **사용자 승인 필요.**
   커밋 메시지 초안: `evidence/commit-msg.txt`.

### 미해결·확인 대기

- **계약 변경 둘(확인 필요)**: ① 보안 — 「인용부호 안 언급도 막는다」 → 「실행은 막고 기록은
  막지 않는다」(근거였던 「좁히면 bash -c 가 열린다」가 실측 반증됨) ② 공개 — 종료코드 `2` 신설.
- **open HIGH 잔여**: API-02(루트 밖 쓰기 규칙이 두 표면에서 다름) · API-03·OPS-08(에이전트 처리 중).

### 함정(이번 확인)

- **도구가 「측정 불가」라고 할 때 그 기준을 코드에서 확인하라.** G7 은 네 세션 동안 「부하 때문에
  못 잰다」로 넘겨졌는데, 실제 기준은 `load < 5.0` 하나였고 기다리면 되는 것이었다.
- **`grep -c` 는 0건일 때 exit 1** 이다 — 백그라운드 작업이 「실패」로 보고돼 성공한 측정을
  놓칠 뻔했다. 판정을 종료코드로만 읽지 마라.
- **희소 파일(`ftruncate`)로 크기 조건을 테스트**할 수 있다 — 600MB 검사에 디스크를 안 쓴다.
- 상한(ceiling) 검사는 부하가 **비관적** 방향이라 부하 창에서 재도 되지만, 델타는 **양방향**이라 안 된다.
