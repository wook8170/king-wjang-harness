
## 2026-08-28 (48) — 47차의 판정 철회를 **실제로 반영·커밋** 완료

**정본.** 로컬 `main` = **`dc20b85`** · `origin/main` = `bacb4bc`(**push 안 됨**) · 트리 clean ·
1490/1490 green · tsc 0 · 대장 167행 lint 통과.
**아티팩트(갱신 게시됨)**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

47차는 「GO 가 성급했다」는 **진단**이었고, 이번에 그것을 문서·대장·게시물에 **적용**했다.

### 반영한 것
- **판정: 출하 가능(GO) → 조건부 출하 가능.** `ledger.md` 헤더·`00-summary.md` 판정 블록 동기.
- **DEP-11**: `verified`(code 추론) → **`open` / HIGH**. 사유를 행에 적었다 — 축⑩의 실측
  (SHIP-04, `npm ci` 로 68MB/73패키지)과 정면 충돌하고 **measured 가 code 를 이긴다**.
- **G3 → 미달**(SHIP-02 가 「광고↔실물 불일치 0」을 깬다) · **G9 → 재확인 필요**(도달 0 의 근거
  한 축이 DEP-11 이다). 게이트 **목표 칸은 건드리지 않았다** — 낮춘 것은 판정이지 기준이 아니다.
- 커밋 `dc20b85` 로 위 전부 기록. 커밋 메시지에 철회 사유를 남겼다.

### 다음 즉시 할 일 — 47차와 동일, 순서 그대로
1. **㉠ DEP-11 재현** — 깨끗한 HOME 에서 `claude plugin install` 후 캐시에 `node_modules` 가
   생기는지. 사실이면 DEP-11 을 `rejected`(감사 오류)로 확정하고 **G9 의 근거 문장을 다시 세운다**
   (「안 내려온다」 → 「내려오지만 실행 경로에 없다」를 **실측**으로).
2. **㉡ SHIP-02** — 문서대로 설치했을 때의 정확한 호출 방법을 확인해 README 를 고치거나,
   못 고치면 G3 미달을 확정한다.
3. **판정 재산정** → 4. **장부 위생**(백로그를 `deferred`+사유로) → 5. **push** → 6. CHANGELOG 계약 2건.

### 미해결·확인 대기
- 위 ㉠㉡ · CHANGELOG 미기재 2건(보안 규칙 전환 · 종료코드 `2`) · open 40건(MED 16 · LOW 22 · HIGH 1=DEP-11).

### 함정
- **`verified` + `code` 조합을 의심하라** — 이번 사고의 정확한 형태다. 같은 사안을 실측한 축이
  반대 결론을 냈는데 두 행이 나란히 살아남았다.
- 최종 검증에 **「게이트별로 그 게이트를 깨는 open 항목이 있는가」 기계 대조**를 넣어야 한다.
  축끼리만 대조하면 이번 부류를 못 잡는다.
