
## 2026-08-28 (42) — ★ 라운드 2 완주 · 적색 게이트 7개 중 **7개 닫음** · 남은 조건 **둘, 둘 다 사용자 몫**

**정본.** 워킹트리 작업 중(**커밋 안 함**) · `origin/main` = `bacb4bc` · **1471/1471 green · tsc 0** ·
로컬 태그 `v0.1.1`·`v0.1.2` 신규(**push 안 함**).
**아티팩트**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### 판정

**출하 불가(NO-GO) → 조건부 출하 가능(CONDITIONAL GO).** open BLOCKER **3 → 0**.
게이트 **13개 중 12개 충족**, G7 하나만 보류(측정 불가). 대장 161행 · `vpr lint` R1–R13 통과.

### 이번에 닫은 것 (라운드 2 후반)

| 조건 | 어떻게 |
|---|---|
| **㉯ API-04** | **상한 검사는 부하가 비관적 방향**이라는 걸 깨닫고 부하 창에서 측정했다. 상한을 실측으로 **4MB → 1MB** 재역산(1.09MB=4.4s 여유 2.3배 · 2.23MB=8.0s 여유 1.2배). **상한에서의 e2e 를 매번 다시 재는 회귀 테스트**를 붙였다 — 숫자만 주석에 적으면 또 낡는다(이 결함이 그 증거) |
| **㉱ LOGIC-02 잔여** | 「막을지 광고를 바꿀지」가 이분법이었다. **세 번째 길** — 쓰기를 막지 않고 `doctor` 가 **손상된 웨이브 지시서를 탐지**. 광고를 안 건드리고 데이터 손실만 닫았다 |

**자기 회귀를 벤치가 잡았다**: 자기해제 가드가 `commandLines` 를 3회 돌려 `cd-redirect` 가
1310ms(문턱 1000)를 넘겼다 → 한 칸 캐시로 **375ms**(PERF-04).

### 다음 즉시 할 일 — **둘 다 사용자만 할 수 있다**

1. **㉮ G7 훅 지연** — 조용한 창(`osascript -e 'quit app "OrbStack"'`) 뒤 `npm run bench:hook`.
   **3회 시도 모두 벤치가 「machine busy」로 인증 거부.** wall 델타 **−40.9 ~ +203.4ms** 로 요동
   (음수가 노이즈 지배의 증거) · **in-process 는 3회 모두 +15.4~19.2ms 로 안정·문턱 50ms 아래.**
   **G7 은 델타라서 부하가 양방향으로 움직인다** — API-04 에 쓴 「부하는 비관적」 논리가 안 통한다.
   38차부터 **네 세션째** 미결.
2. **㉰ 커밋 + `git push --tags`** — 라운드 1·2 작업 전부 워킹트리에 있고 태그도 로컬뿐이다.
   바깥으로 나가는 행위라 사용자 승인이 필요하다.

### 미해결·확인 대기

- **보안 계약 하나를 바꿨다(확인 필요)** — `policy.test.ts` 의 「인용부호 안의 언급도 막는다 —
  의도된 과차단」. 근거였던 「좁히면 `bash -c` 가 열린다」가 **실측으로 반증**됐다(우회 20종 전부
  계속 차단). 새 계약: **실행은 막고 기록은 막지 않는다.**
- **공개 계약 하나를 바꿨다(확인 필요)** — 종료코드 `2` 신설(판정이 「아니오」). 0 이 아니면
  기존 `cmd || exit 1` 은 그대로 걸리므로 호환은 유지된다.
- **API-28**(env 리터럴 언급 과차단) · **SHIP-23**(배포본 테스트 수 기계 검증 없음) — LOW, 백로그.

### 함정(이번 확인)

- **상한(ceiling) 검사와 델타(delta) 검사는 부하 취급이 다르다.** 상한은 부하가 비관적 방향이라
  부하 창에서 재도 되고, 델타는 양방향이라 안 된다. 이 구분이 API-04 를 닫고 G7 을 못 닫은 이유다.
- **`core/src` 를 고치면 선행 대장(2026-08-21) 인용이 밀린다** — `evidence/reanchor.js` 로 자동
  재앵커(README·CHANGELOG 포함). 소스를 고칠 때마다 돌려라. HEAD 에서도 무의미했던 2건은 수동.
- **테스트 파일을 추가하면 README 4개 계량을 함께 고쳐야 한다** — 그래서 라운드 2 회귀는 한 파일에.
- **`wave create` 는 `activeWave` 를 세우지 않는다** — `wave activate` 가 세운다.
- **게이트 산출물은 공백 제외 80자 이상**이어야 제출된다(테스트 픽스처 주의).
- **역행(backtrack)에서 드리프트 검사를 돌리면 사용자를 가둔다** — `UTIL-189` 회귀가 잡았다.
