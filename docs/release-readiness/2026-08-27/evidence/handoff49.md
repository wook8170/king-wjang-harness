# king-wjang-harness 진행상황 (핸드오프)

## 2026-08-28 (49) — ㉠㉡ 실측 처리 · **판정 회복(GO)** · 게이트×잔여 기계 대조 신설

**정본.** 로컬 `main` = `ef28988` + **미커밋 작업트리**(아래) · `origin/main` = `bacb4bc`(**push 안 됨**)
**1491/1491 green · tsc 0 · 대장 lint 통과(167행) · open 0 · deferred 37 · rejected 2.**
**아티팩트 갱신 실패 — 사용자 확인 대기**(사유는 「미해결」 절).

### 무엇을 했나

47·48차가 남긴 ㉠㉡ 를 **실측으로** 처리하고, 그 과정에서 게이트를 깨는 항목을 **둘 더** 찾아 닫았다.

- **㉠ DEP-11 = 감사 오류 → `rejected`.** 실기 HOME 의 플러그인 캐시 **4버전 전부** `node_modules`
  66MB/73패키지를 갖고 있다(총 609MB). `vitest 2.1.9`·`vite 5.4.21`·`esbuild 0.27.7` 실재 —
  npm audit 의 critical/high 근원이 그대로 내려온다. **G9 은 근거를 갈아 끼워 유지**:
  「안 내려온다」 → **「내려오지만 실행되지 않는다」**(node_modules 뺀 사본에서 CLI·MCP 정상 동작 ·
  진입점 4개의 외부 require 는 Node 내장뿐 · `npm ci` 는 `--ignore-scripts`).
- **㉡ SHIP-02 = 측정 범위 오류 → `verified`.** `harness` 는 **사용자 셸에서만** PATH 에 없다.
  **에이전트 셸에는 Claude Code 가 플러그인 `bin/` 을 얹어 준다**(실측). README 4언어에
  「`harness` 는 어디 있나」 문단을 넣어 닫았다(청정 env 로 실측한 PATH 한 줄 포함).
- **새로 찾아 닫은 G3 위반 2건** — ① `README:114` 「Self-contained … 0 vulnerabilities」가 설치
  경로에 대해 거짓 인상(4언어 정정) ② **API-11**: MCP `harness_wave_create` 스키마가 `goal` 을
  **선택으로 광고**하고 `isError:true` 로 거부 → `required:['goal']` + 회귀 테스트.
- **PERF-03** 이 낡아 게이트와 반대말을 하고 있었다(「G7 미실시」인데 G7 은 유휴 재측정으로 충족) → 닫음.
- **장부 위생**: `open` 36건 전부 `deferred` + **언제·왜**(v0.1.3 문서/공급망/관측성/UX · v0.2 입력검증).
- **CHANGELOG** 미기재 계약 2건 기재(종료코드 `2` = API-05 · 셸 규칙 전환 = **API-01**) + API-11.
- **절차 수정** — `99-final-verification.md` **§6 게이트 × open 기계 대조** 신설. 47차의 진짜 교훈
  (「축끼리만 대조했다」)에 대한 구조적 답이고, 이번에 3+1건을 이 절이 잡았다.

### 다음 즉시 할 일

1. **사용자 확인 2건**(아래 「미해결」) → 아티팩트 force 재게시 · `git push`.
2. 커밋(아직 안 했다). 변경: README×4 · CHANGELOG · `core/src/mcp.ts` · `core/dist/*` ·
   `core/test/mcp.test.ts` · 08-27 대장/요약/99-final/report.html · **08-21 대장(앵커 8건 재조정)** ·
   `evidence/recheck-dep11-ship02.log`(신규).
3. push 후 `deferred` 목록으로 v0.1.3 라운드 계획.

### 미해결 · 확인 대기

- **아티팩트 갱신 차단.** 같은 URL 재게시가 「라이브 버전을 먼저 통째로 Read 하라」로 막힌다.
  라이브 사본은 **커밋된 HEAD 판과 바이트 동일**함을 diff 로 확인했다(차이는 플랫폼이 주입한
  frame-runtime 래퍼뿐) — 즉 **병합할 남의 편집은 없다.** 그런데 그 파일을 Read 하려면 ~75k 토큰이
  든다(1행이 100KB 압축 스크립트). **`force:true` 는 사용자 명시 승인이 있어야 쓴다** → 승인 요청 중.
- **push 미실행** — 외부로 나가는 행위라 승인 대기. 46차의 비밀 스캐너 거부 이력 주의(원인은 제거됨).

### 함정

- **대장 인용 앵커는 소스가 밀리면 깨진다.** `core/test/ledger-summary-sync.test.ts` 가 잡는데
  **2026-08-21 대장만** 검사한다 — README·CHANGELOG·`core/src/*` 를 건드리면 그 대장의 앵커를
  다시 잡아야 한다(이번에 8건). **2026-08-27 대장에는 같은 lint 가 없다**(빈틈).
- **`claimed` 등급 + `open` 조합은 유통기한이 있다** — 기다리던 측정이 끝났는지 게이트 표와 대조하라
  (PERF-03 이 그 사례).
- **문서 수치를 바꾸면 다른 문서가 즉시 낡는다.** 테스트를 1개 추가해 1490→1491 이 되자 README 4개가
  전부 거짓이 됐다. `doc-claims.test.ts` 는 **4언어가 서로 같은지만** 보고 실제 스위트와 대조하지
  않는다(SHIP-23).
- **아티팩트 라이브본을 diff 로 확인하려 하지 마라** — 라인 1의 런타임 스크립트가 통째로 출력돼
  토큰을 크게 태운다. 크기·행수 비교(`ls -la`/`wc -l`)로 먼저 판단할 것.
