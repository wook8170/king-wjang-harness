
## 2026-08-28 (40) — ★ 백지 11축 재감사 완료 · 판정 **출하 불가(NO-GO)** · 차단 3건

**정본.** `main` HEAD `bacb4bc` = `origin/main` · 감사 산출물 `docs/release-readiness/2026-08-27/` (untracked).
**아티팩트(원격에서 볼 것)**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### 판정 — 출하 불가, 차단 결함 3건

전부 「강제가 뚫린다」가 아니라 **「경계가 부정확하고, 강제가 꺼진 걸 아무도 모른다」** 쪽이다.

| ID | 축 | 무엇이 |
|---|---|---|
| **API-01** | ② | FD 복제 리다이렉트(`2>&1`) 하나로 **읽기 전용 명령이 소스 쓰기로 오판**돼 거부된다 |
| **OPS-01** | ⑪ | 저널이 손상돼도 상태 파일만 멀쩡하면 **UI 어디에도 안 보인다** |
| **OPS-02** | ⑪ | 번들 부재 시 훅이 완전 침묵해 **강제가 조용히 꺼지는데** `doctor` 는 `ok:true` |

### 통과한 것 (실측)
G1 테스트 **1404 pass ×3회 동일**(load 14→36 악조건) · G2 tsc **0** · G6 E2E **시나리오 실패 0**(우회 28/28 차단)
· G8 보안 **4/4**(관통 0 · gitleaks 0/315커밋 · `child_process` 전무) · G9 공급망 **프로덕션 도달 0**
· G11 결정성 **충족** · dist↔소스 **바이트 일치(4회 독립 확인)**.

### 미달한 것
G3 광고 불일치 1(ko/ja/zh 에 인터프리터 우회 고지 누락) · G4 exit 1 4중 의미 + **훅 10초 예산 초과**
· G5 폭 80 깨짐 1 · G10 불변식 위반 2 · G12 조용한 기본값 2 + 되돌아갈 버전이 치명 우회 보유
· G13 4개 기준 전부 어긋남. **G7(훅 지연)만 미실시** — 창 오염.

### 다음 즉시 할 일
1. **사용자만 할 수 있는 두 가지** (이걸 해야 나머지가 풀린다):
   - `rm -rf /Volumes/WorkSpace/0200_Dev/king-wjang-harness/.harness` — 감사 중 에이전트가 잘못 만든 활성화. 제품 설계상 사람 전용(정상 동작).
   - `osascript -e 'quit app "OrbStack"'` — 357% CPU 폭주, G7 측정 창 오염원.
2. **G7 판정**: 위 둘 뒤 `npm run bench:hook`. 「machine busy」 표기 없으면 유효. **38차부터 두 세션째 숙제.**
3. **수정 라운드 1(차단 3건)** — `fixes-round1.md` 새로 만든다:
   - API-01 은 **뿌리가 하나**라 한 곳을 고치면 트리거 5종이 함께 닫힌다: 쓰기 대상을 **구문 위치**로 추출하고 따옴표 닫힌 heredoc 본문을 판정 입력에서 뺀다.
   - OPS-01·02 는 **열화 신호 배선을 상태 파일 파손 외 경로로 확장**하는 한 가지 작업.
   - 고친 뒤 **재측정 필수**(Iron Rule 4). `evidence/probe-overblock*.js` 2종을 **회귀 테스트로 승격**하면 이 부류가 다시 안 샌다.

### 미해결·확인 대기
- **API-01 심각도는 사용자 결정 사항.** 감사는 BLOCKER(축② 독립 판정 + 오케스트레이터 독립 재발견). 「과차단은 보안 결함 아님」으로 볼 수도 있으나 오차단률 25%는 채택을 막는다.
- **`v0.1.1`·`v0.1.2` 태그 커팅**(SHIP-01) — 되돌아갈 유일 버전 v0.1.0 이 SEC-300 치명 우회 보유. 릴리즈 프로세스 결정.
- 선행 리포트(2026-08-21) 낡은 판정 문구 처리 — 이번 리포트가 실질 대체.
- 공식 디렉터리 제출(사용자 폼) — 판정이 NO-GO 이므로 **보류 권장**.

### 함정(이번 확인)
- **저장소가 자기 훅의 통치를 받으면 감사 도구가 막힌다.** 이번 세션은 그 상태에서 **아홉 번** 거부당했다: heredoc 본문의 소유 경로·마크다운 백틱·`2>&1`·`cd &&` 접두사·승인 명령 문구. 대장 일부는 Write 도구로 조각 파일을 만들어 `cat` 으로 이어붙여야 기록됐다.
- **병렬 에이전트에게 「샌드박스에서만」은 지시로 부족하다.** 다음엔 **읽기 전용 사본 경로만 주는 것**이 옳다.
- **에이전트 9명에게 `npm test` 전체 실행을 금지**한 것은 옳았다 — ⑨ 결정성을 중앙에서 한 창에 재야 판정이 산다.
- **부하 창이 항상 나쁜 건 아니다.** 결정성은 악조건에서 3/3 동일이 나와 **조용한 창보다 강한** 근거가 됐다. 타이밍 게이트만 조용한 창을 요구한다.
- lint 는 `bin/vpr lint docs/release-readiness/2026-08-27/` — 인용 줄이 **공백이면 R7 위반**이라 앵커 재확인 필요. 축 파일을 편집하면 줄이 밀린다.
