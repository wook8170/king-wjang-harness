
## 2026-08-28 (45) — 커밋 완료 · **push 만 남음** (판정 출하 가능 유지)

**정본.** 로컬 `main` = **`a9a6e83`** · **`origin/main` = `bacb4bc`(아직 push 안 됨)** · 워킹트리 clean.
로컬 태그 `v0.1.0`·**`v0.1.1`·`v0.1.2`** / **원격 태그는 `v0.1.0` 하나뿐**(`git ls-remote` 로 직접 확인).
**판정: 출하 가능(GO) · 게이트 13/13 · 1490/1490 green · tsc 0 · 대장 165행 lint 통과.**
**아티팩트**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### 이번에 된 것
- 사용자가 라운드 1~3 전체를 **커밋**했다(`a9a6e83`, 33개 파일).
- heredoc 형태의 「루트 밖 쓰기」 문의를 독립 검증했다 — **결함 아님**(의도적 범위, `hook.ts:881`
  에 이유 명시 + `bashwrite.test.ts:116` 이 계약 고정). 동시에 **중요한 회귀 확인**이 됐다:
  오늘 넣은 heredoc 본문 마스킹이 **리다이렉트 대상 추출을 깨지 않았다**(루트 안 소스·저널은
  heredoc 으로도 전건 deny, 문서는 allow). 대장에 `SEC-16` 로 등재.

### 다음 즉시 할 일

1. **`git push origin main --tags`** — 이것 하나가 남았다. **태그가 원격에 없으면 SHIP-01 이
   실질적으로 안 닫힌다**: 원격 사용자가 되돌아갈 수 있는 유일한 버전이 여전히 SEC-300 우회를
   가진 `v0.1.0` 뿐이다.
2. **커밋 메시지가 라운드 3 을 안 담는다** — 제목이 「(라운드 1·2)」인데 실제로는 라운드 3
   (OPS-06 fail-open · OPS-08 비밀 마스킹 · API-03 · API-02 기각 · G7 실측)도 함께 들어갔다.
   초안이 라운드 3 «전»에 쓰였기 때문. push 전이면 `git commit --amend` 로 정정 가능.
3. **CHANGELOG 에 계약 변경 2건 미기재** — ① 보안: 「인용부호 안 언급도 막는다」 →
   **「실행은 막고 기록은 막지 않는다」** ② 공개: **종료코드 `2` 신설**(판정이 「아니오」).
   둘 다 사용자에게 보이는 변경이라 릴리스 노트에 있어야 한다.

### 미해결 (전부 출하 후 백로그)
MED 15 · LOW 22. 대표: ORCH-02(설계 트랙에서 프로젝트 밖 Write 차단 — 세션 스크래치패드가
막혀 오히려 저장소를 더럽히게 만든다) · API-28 · DEP-01 · SHIP-23.

### 함정
- **`git rev-parse origin/main` 은 로컬 추적 ref 라 낡을 수 있다** — push 여부는
  `git ls-remote origin refs/heads/main` / `git ls-remote --tags origin` 으로 **원격에 직접** 물어라.
  이번에도 그 차이로 「했다」와 실제 원격 상태가 갈렸다.
- 나머지 함정은 44차 섹션 참조(측정 창·재앵커·README 계량·희소 파일·상한 vs 델타).
