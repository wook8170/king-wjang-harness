
## 2026-08-28 (44) — ★★ **판정: 출하 가능 (GO)** — 게이트 13/13 · 차단 0 · HIGH 0

**정본.** 워킹트리 작업 중(**커밋 안 함**) · `origin/main` = `bacb4bc` · 로컬 태그 `v0.1.1`·`v0.1.2`(**push 안 함**).
**1490/1490 green · tsc 0** · 대장 164행 · `vpr lint` R1–R13 통과.
**아티팩트**: https://claude.ai/code/artifact/12260342-29cb-4be0-a786-1e31ff73dec2

### 판정 이동

**출하 불가(NO-GO) → 출하 가능(GO).** 착수 시 차단 3건 + 적색 게이트 7개 + open HIGH 4건이
전부 닫혔다. 잔여 37건은 MED 15 · LOW 22 로 **출하 후 백로그**.

### 마지막 라운드에서 닫은 것

- **G7 훅 지연** — 네 세션 미결이던 것. 벤치의 busy 기준이 `load > 코어수×0.5`(=5.0)임을
  코드에서 확인하고(`scripts/bench-hook-latency.mjs:137`) **부하가 내려가길 기다려** 측정했다
  (load 3.95, busy 표기 0, **PASS 전건**: realistic in-proc +15.2ms · wall +32.9ms).
- **[OPS-06]** 거대 저널 + 손상 state → 재생이 예산 초과 → **훅이 죽고 죽은 훅은 통과**였다.
  상한 128MB(실측 43MB/s 역산) 초과 시 **재생을 포기하되 통과시키지 않는다**. 읽기·harness
  명령은 열어 둬 `doctor --repair` 로 탈출 가능. 회귀는 **희소 파일**로 600MB 검사.
- **[OPS-08]** 저널 자유 텍스트에 비밀 마스킹(`appendEvent` 한 곳) + `init` 이 git 노출을 1회 고지.
- **[API-03]** config 미지 키를 `doctor` 가 **issue** 로 올린다(판정의 입력이라 warning 으론 부족).
- **[API-02] 기각** — 비대칭은 「루트 밖 원고 만들기」에서만 보이고 **위반이 완성되는 「프로젝트
  소스로 들여오기」는 10/10 전건 차단**(cp·mv·install·리다이렉트·tee·심링크·rsync·dd·python·node).
  구멍이 아니라 일관성 흠. 실사용 비용이 있는 쪽은 Write 쪽 과차단이라 ORCH-02(MED)로 남겼다.

### 다음 즉시 할 일 — **출하 «행위»**(결함 아님, 사용자 승인 필요)

1. **커밋** — 라운드 1~3 변경 30+ 파일이 워킹트리에 있다. 메시지 초안: `evidence/commit-msg.txt`.
   (전역 규칙상 기본 브랜치에서는 브랜치를 먼저 따야 하나, 이 저장소는 70+ 핸드오프 내내 main
   직접 커밋이 관행이다 — **사용자 판단 필요**.)
2. **`git push --tags`** — 태그 내용은 실측 확인됨(v0.1.2 에 SEC-300 수정 有, v0.1.0 에 無).
   원격 사용자가 되돌아갈 수 있으려면 push 가 필요하다.
3. **계약 변경 2건 고지** — ① 보안: 「인용부호 안 언급도 막는다」 → 「실행은 막고 기록은 막지
   않는다」 ② 공개: 종료코드 `2` 신설. 둘 다 CHANGELOG 에 적을 것.

### 미해결 (전부 백로그)

MED 15 · LOW 22. 대표: ORCH-02(설계 트랙에서 프로젝트 밖 Write 차단 — 스크래치패드가 막힌다) ·
API-28(env 리터럴 언급 과차단) · DEP-01(lockfile 버전 정렬) · SHIP-23(배포본 테스트 수 미검증).

### 함정(이번 확인)

- **도구가 「측정 불가」라 할 때 그 기준을 코드에서 읽어라.** G7 은 네 세션 동안 미뤄졌는데
  기준은 `load < 5.0` 하나였고 **기다리면 되는 것**이었다.
- **상한(ceiling) 검사는 부하가 비관적 방향**이라 부하 창에서 재도 되지만, **델타는 양방향**이라 안 된다.
  이 구분이 API-04 를 닫고 G7 만 기다리게 한 이유다.
- **`grep -c` 는 0건일 때 exit 1** — 성공한 백그라운드 측정이 「실패」로 보고될 수 있다.
- **희소 파일(`ftruncate`)로 크기 조건을 테스트**하면 600MB 검사에 디스크를 안 쓴다.
- **`core/src` 를 고치면 선행 대장(2026-08-21) 인용이 밀린다** — `evidence/reanchor.js` 로 재앵커.
- **테스트 파일·수가 늘면 README 4개 계량을 함께 고쳐야 한다**(`doc-claims.test.ts` 가 잡는다).
